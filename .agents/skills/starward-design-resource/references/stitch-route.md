# Starward 的 Stitch 补充

先读包内 [Stitch operations](../../../../node_modules/project-tiny-context-harness/assets/skills/design-resource/references/stitch.md)。连接发现、操作选择、超时恢复、原稿获取和交付沿用上游，本文件只补充 Starward 的输入与观察方式。

小程序每次打开或续改 Stitch 前，先从[项目 Stitch 状态](../../../../docs/design-resources/wechat-miniapp/stitch-status.md)取得**当前工作项目链接**，再按受影响页面的 `ADOPTED.md` 与待审资源 README 核对完整源和画板范围。旧项目 URL 即使出现在 raw/provenance、历史记录或浏览器最近项目中，也只证明来源身份，不能据此选择设计基线；项目标题或画板范围与当前入口不符时先回到入口核对。当前项目换代时同步更新状态页与资源入口，不在本 Skill 写死项目 ID。

- 按 [设计方法](design-method.md) 和 DESIGN.md 小程序 §1.3，把首屏重心、比例/密度、对象卡片、图标质感及适用交互组织成正向输入。业务图保留信息职责，视觉参考指出具体区域和用途；不把整张未认可候选当作风格规范。参考必须通过真实支持的上传/粘贴到达 Stitch，文字中的本地路径不是上传。
- 首稿用一致的手机内容视口查看，保留原始导出尺寸；不借设备外壳、装饰背景或不同缩放美化比较。产品画面外放候选名、比较说明与代表数据的非实时说明，产品画面内保留真实需要的信息结构。平台、字级、命中和状态范围从当前 owner 读取。
- 原请求包含精致页面时，在同一方向内主动修订已发现的整体比例、阅读层级、控件质感和状态连续性问题，再交付用户；数量/预算仍遵循当前任务。修改输入区分用户原话与 Agent 检查发现，明确保留项。探索期间临时保留原稿和修订用于核对，按[资源生命周期](../../../../project_context/context-maintenance.md#design-resource-lifecycle)定稿收敛和清理；不能把本地修复算作 Stitch 一次生成效果。
- 生成或修改结果不确定时，严格沿用上游读回与有界恢复，不因超时重复提交。已确认提交前失败，修正具体原因后最多一次安全技术重试；再次失败就报告。该限制不缩短仍在执行操作的正常读回，也不替代服务当前恢复规则。
- 用现有资源或任务目录保存有用的输入与版本。模型/模式、用量、HEAD/哈希仅在比较或追溯需要时记录；未知模型为 `unknown`，未知用量为 `null` 并说明。复做旧 Codex A/B 方法试验时才沿用原模型一致性与记录要求，默认设计工作不受旧试验门槛约束。
- 每次需求的设计资源候选完成后，留存边界包括 Stitch 本身。按照[资源生命周期](../../../../project_context/context-maintenance.md#design-resource-lifecycle)，先核对当前节点、本地可编辑源、旧版本和实际依赖，向用户确认本轮具体删除清单，再删除获确认的废弃 screen/旧修订及仅含废案的项目；读回剩余节点并更新本地导出和链接。共享项目保留其他有效设计。仅导出新稿、隐藏旧稿或改本地 ADOPTED 入口不算远端清理，工具不能删除时明确报告残留。
- 只使用当前任务授权的工具与额度，遵循项目试用/预算边界；工具选择不扩大付费、公开分享或生产迁移权限。需要 Figma 时做独立下游转换，沿用既有引用与检查器。
