# Open Design current candidate revision plan

> Task-local orchestration record only. It is not Design Authority, selected Source,
> handoff, or acceptance evidence.

## Locked current scope

- The current candidate implements the complete accumulated owner input: the retained
  portions of the prior eleven audit points plus the seven current requirement groups in
  `working-index.md#I18`; a later requirement replaces an earlier conflicting expression.
- Five stable Product Surfaces and 66 current material Controls form the complete candidate
  coverage and consistency envelope. They are not five screens to redraw mechanically.
- Current map discovery contains the map, the dedicated Search child, and the draggable
  spot-information panel. Standalone spot-detail and astronomy-summary pages are retired.
- `sky/detail` remains the only full-sky child. All nonconflicting My/Profile/Contribution
  UI remains reachable and is brought onto the compact current design-system hierarchy.
- The previous candidate is rejected evidence/diff baseline only. No former Finder sheet,
  old active-star treatment, linear time scale, standalone Detail/Night route, visible
  “演示数据”, visible release/version label, or old visual-system fallback may survive.
- Any newly discovered UI/UX choice beyond the accumulated owner input and its direct
  dependencies is decision-required and must stop the write turn.

## Exact provider binding

- Project: `starward-miniapp-field-signal-all-resources`.
- Conversation: `4f290527-9979-4b24-b92c-8365b470bf9d`.
- Agent / model / reasoning: `codex` / `gpt-5.6-sol` / `xhigh`.
- Session mode: `design`; skill: `frontend-design`.
- Design system: `user:starward-mini-program-sky-canvas-field-signal-revision`.
- Current provider body SHA-256: `98218fd2a884373222a1b0e9e7892c133bbb4f78aeedec2c17a2ccc1fd9eae41`.
- Current `COMMISSION.md` SHA-256: `d243ea93fd68d74405ed56f2e1500c5a8d7070b5bf4832b87410abba1a9050ad`.
- Plugin / applied snapshot: none; neither may be requested or accepted.

## Turn A — read-only style-application closure

- Initial client request ID: `c1a2f3b0-ae10-4fc1-95c4-2deb9dfaa66e`.
- Corrective client request ID: `d70bed44-79af-4f19-bfc3-d76c68017359`.
- Read the complete current `COMMISSION.md` and injected current design-system body before
  inspecting the five old candidate files as rejected evidence.
- Verify the exact source digests and distinguish current five-Surface/66-Control coverage
  from the accumulated owner-change diff.
- Close primary-content priority, compact-but-clear density, hierarchy/container treatment,
  visible-vs-hit geometry, current-only lineage, information architecture, feedback/motion,
  content/data boundaries, responsive layout, accessibility, and every Map/Search/Panel/
  curved-ruler/full-sky interaction as `existing-covered`.
- If any conflict, missing decision, or UI/UX choice outside owner authority is needed,
  return `decision_required` with the exact key and do not write.
- Otherwise return `closure_passed` and explicitly state that Turn B may proceed. Do not
  edit, create, delete, rename, or format any project file.

## Turn B — material current-candidate revision

- Client request ID: `f3c509ba-bfe3-463a-aef6-6b118e8470f5`.
- Start only after Turn A succeeds and all five candidate files are byte-identical to their
  Turn A precondition.
- Read the complete current Commission and injected design system again, then replace every
  affected old candidate expression. Do not create a compatibility path for retired logic.
- Keep one machine-readable, directly runnable interactive prototype:
  `index.html`, `assets/styles.css`, `assets/app.js`, `coverage.json`, `README.md`.
- Make all five stable Surfaces, 66 current Controls, applicable states, three themes,
  320/375/390/430 widths, safe areas, 200% text, reduced motion, and keyboard/accessibility
  audit paths reachable. Update coverage and README to the actual current files.
- Preserve the exact Favorite ritual, restrained notification rules, event-linked single
  opportunity rail, full-sky sensor/error semantics, and source/freshness/partial/error UI.
- Run file-local HTML/JS/JSON/static checks. Do not perform user selection, immutable
  snapshot, formal handoff, production edits, provider preview acceptance, or release.

## Retry and stop rules

- An uncertain transport retry reuses the exact client request ID for its logical turn.
- Never start Turn B while any task run is active or if Turn A mutated a candidate file.
- Provider success and packaging signals are not user acceptance.
- After provider-to-repository byte parity, project checks, and independent Browser review,
  stop at Design Resource Review & Selection. Do not read downstream DRA references before
  the owner's explicit approval.

## Turn B daemon interruption

- The first transport for client request `f3c509ba-bfe3-463a-aef6-6b118e8470f5`
  was interrupted by an Open Design daemon restart (`ECONNRESET`). The pre-restart run ID
  was `396d20ad-1519-4c91-bd77-ea8dbfa4a7ad`; it had no terminal result available after
  restart and therefore provides no success or acceptance evidence.
- The provider retained writes to `index.html`, `assets/styles.css`, and `assets/app.js`;
  `coverage.json` and `README.md` still had their old byte sizes at the post-restart read.
- The only admitted uncertain-transport retry reuses the same logical client request ID.
  It must read the current retained files and finish the same bounded revision; it may not
  broaden the design scope or create a second candidate identity.
- That idempotent retry read back the terminal reconstructed result
  `failed / DAEMON_RESTARTED` for run `396d20ad-1519-4c91-bd77-ea8dbfa4a7ad`; it did not
  create a second writer. Because the retained candidate is incomplete, one bounded
  completion turn may now use client request ID
  `b2688309-3cda-46ab-baac-150bfe3ad621`.
- The completion turn must preserve the retained current HTML/CSS/JS, finish only missing
  or mechanically defective current interactions, and replace stale `coverage.json` and
  `README.md`. It must not repeat visual exploration, acquire new design authority, create
  extra deliverables, or resurrect any retired route/control.
- The completion transport was also interrupted by a daemon restart before its terminal
  report. Post-restart inspection shows the intended current architecture is present and
  all listed retired controls/routes have zero occurrences, but `coverage.json` and
  `README.md` are still byte-identical to the rejected predecessor. The exact current DOM
  is missing only seven literal stable audit markers:
  `source-lift-focus-layer`, `map-analysis-focus-layer`, `map-time-control`,
  `my-account-header`, `my-profile-summary`, `sky-time-scrubber`, and
  `sky-orientation-time-ruler`.
- One final mechanical-only turn may use client request ID
  `3492afb6-a513-401c-b6a8-3d62977b17aa`. It may attach those seven stable keys to their
  already existing semantic elements, update coverage/README, and repair only a concrete
  syntax/reference defect exposed by its checks. It may not change layout, styling,
  interaction meaning, routes, or product scope.

## Turn A count correction

- Initial closure run `9888d600-474c-43b7-a65b-582cad38b436` succeeded technically and
  remained read-only, but correctly identified that this orchestration plan/prompt said 67
  while the complete canonical inventory contains 66 Controls.
- The canonical inventory was already complete: category counts are
  `5 + 15 + 18 + 5 + 9 + 6 + 8 = 66`. No Control key, owner category, product meaning, or
  UI/UX decision is missing; only the task-local total was wrong.
- The corrective closure uses the new client request ID above and the exact total 66. The
  first run is diagnostic evidence only and does not authorize Turn B.
