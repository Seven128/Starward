# 观察范围与计算复核

日期：2026-09-14，Asia/Shanghai。目标是用户指定的 journey/7685320728933093603?source=miniapp，没有枚举行程。

## 工具和操作

- 已连接用户 Chrome，经浏览器插件的 CDP（浏览器调试协议）读取请求方法、请求体、响应体、已加载脚本；并非仅凭截图调查。
- 初始目标页显示“行程安排未公开”，用户手动登录后可读。首次导航未完整覆盖所有初始请求，部分网络事件缓冲区发生截断；后续只禁用浏览器缓存刷新一次，成功取得行程详情与路线响应，之后恢复缓存设置。
- 关键路线请求发生于北京时间 18:09:33.880；打开交通方式面板的四模式请求发生于 18:11:09.801。对应 E05 的 UTC 时间分别为 10:09:33.880Z、10:11:09.801Z。
- 读取当前初始 HTML（10306 字符），检查 routeData、routeRequest、14424、2937、顺宝花园、__NEXT_DATA__、__NUXT__，未匹配。此检查只针对上述可见文本标记，不排除其他编码或状态机制；实际网络响应是主要证据。
- 查看过 WebSocket 相关事件，但事件覆盖有缺口，不能声称已排除 WebSocket。没有导出浏览器存储、Cookie 或完整网络日志。浏览器缓存禁用不影响对服务器缓存的未知判断。
- 仅检查已加载的行程、交通组件与地图相关脚本；没有下载整站资源。脚本存在 sourceMappingURL 引用，但现有证据已经足够，未获取源码映射文件。
- 网页只展开交通选择面板；代码显示选择另一方式会调用事件更新，未执行。没有改变起终点、顺序、出发时间，没有执行保存、删除、发布、购买。地点搜索、切换方式后的地图状态未验证。
- 通过 Windows 窗口工具实际查看并展开用户已打开的“圆周旅迹”小程序交通面板。只保留目标窗口截图和当前可见文字，没有小程序网络/脚本证据。未操作原生 App。
- 本地证据仅为脱敏摘录。调查过程曾有工具输出意外包含网站地图凭证，已停止该输出方式、改为脱敏；未复用凭证，也未将其保存到本交付目录。不能把本地证据已脱敏表述为整个工具会话从未发生敏感值输出。

## 终点复核

坐标顺序均为经度、纬度。行程地点声明 coordinate_type=GCJ02，即国内地图常用的偏移坐标体系。

|对象|末坐标|与顺峰山公园地点标记的近似直线距离|
|---|---|---|
|顺峰山公园标记|113.286112,22.819589|0|
|驾车 paths 末点|113.278543,22.827111|约1141米|
|步行 paths 末点|113.286432,22.819618|约33米|
|骑行 paths 末点|113.28717,22.81974|约110米|

距离按半径6371000米的球面大圆公式复算，仅用于检查点位差异，并非道路距离或测绘精度声明。不能据此判断哪一个才是正确入口。

地图组件把地点坐标添加在 paths 首尾。因此驾车 paths 末点到公园地点标记之间会补连接段。这是代码行为；不能把补线当作供应商提供的可驾车道路。

公交 paths 只有起终点两个坐标。已确认此次返回缺少公交沿途几何线；未选中公交，不宣称观察到了其实际地图重绘。

## 展示值复核

网页中文距离默认保留一位小数：Math.round(distance/100)/10，再 toFixed(1)。中文时间分别向下取整小时与剩余分钟。地图天数标签将当天 distance 累加后除1000，再 Math.round。

14424米→14.4千米；2937秒=48分57秒→48分钟；当天地图标签→14km。小程序显示14千米，与网页精度不同；没有读取小程序代码，不能断言它采用同一取整函数。
