# Deployment Current State

This on-demand deployment node records established, pending and explicitly unexercised remote-delivery facts. It is status, not a deployment receipt; structural rules remain in the sibling deployment nodes routed by `project_context/deployment.md`.

## Current Status

- `infrastructure/deployment/miniapp-api.Dockerfile` and `infrastructure/deployment/compose.yml` now define one immutable API/worker image and a pinned Caddy/PostGIS/Redis release stack with only the HTTPS edge exposed. Repository release builds, one-shot migration, API liveness/readiness/release identity, worker heartbeat/readiness and environment-bound release metadata exist. Docker is not installed in the current Windows verification environment. On 2026-08-26 the paid remote staging host built and exercised a current dirty-worktree disposable image after the Dockerfile was corrected to install only the API plus its two internal-workspace dependency closure in both build and runtime stages and to use Compose-native init without a mutable runtime APT fetch.
  Both dependency closures reported zero audited vulnerabilities, required runtime imports succeeded, Taro/Expo were absent, and the container ran as UID 1000. The disposable Compose run pulled the digest-pinned PostGIS and Redis images, resumed cleanly after an initial migration failure exposed and repaired missing self-registration in migration `005_decision_snapshot_separation`, applied all six migrations through `006_contribution_intake`, reached healthy API and worker readiness with exact release identity, kept API/PostgreSQL/Redis ports unbound from the host, stopped API and worker in one second, and returned both to healthy after restart. A real PostgreSQL dump was restored into an isolated temporary database, schema-checked, removed, AES-256-GCM encrypted and decrypted back to a valid dump on that host.
  No restore/recovery database remained, and the exact disposable project, volumes, image tag, source, candidate and test-backup directories were then removed. This is bounded current implementation evidence, not a committed registry artifact, full release-environment validation, public TLS check, staging deployment receipt, off-host backup or production recovery drill.
- `tools/deployment/**` now owns stable-secret/candidate-identity separation, immutable candidate descriptor generation, approved-image-repository binding, closed non-secret remote promotion requests, external environment validation, shell-free Compose execution, authenticated encrypted PostgreSQL backup with isolated temporary restore/schema verification, typed-confirmation database recovery with rename-based retained-original cutover and failed-readiness switchback, complete staging-receipt qualification, production exact-digest confirmation, fixed release sequencing and redacted success/failure receipts. `infrastructure/deployment/README.md` is the tracked operator entry.
  Current unit evidence covers candidate idempotence/non-overwrite, unknown-request rejection, placeholder/secret/lane/identity/repository rejection, encryption tamper detection, restore cleanup/mismatch, recovery zero-side-effect confirmation failure, schema mismatch, successful cutover, between-rename failure and post-cutover readiness switchback, staging command order, incomplete or mismatched qualification, stale or tampered backup rejection, production confirmation and release failure stop/receipt behavior; it does not prove remote storage durability, a live recovery drill, application rollback compatibility or a live deployment.
- A first genuinely empty environment requires the tracked one-time data-plane initialization before its initial empty-database backup. Discovering an existing project, container or volume forbids that bootstrap path and requires the normal existing-data boundary.
- `.github/workflows/harness.yml` continues to own Context/Harness gates separately. `.github/workflows/product-ci.yml` provides secret-free release-product CI for pull requests, `main` pushes and manual runs: locked install, a workspace-scoped Mini Program API production-dependency audit, deployment-owner tests, release-code SDK/type/unit checks, backend release builds, WEAPP release-owner tests, a structurally nondeployable CI-lane WEAPP build, isolated infrastructure checks and OCI image construction. It neither validates design-resource authority nor publishes, deploys or uploads. On 2026-08-26 a current-candidate GitHub-hosted Linux Product CI run passed the locked install, production dependency audit, deployment-owner tests, release SDK/type/unit tests, backend builds, WEAPP tests and structurally nondeployable bundle build;
  its Compose v2 infrastructure check then exercised isolated PostgreSQL/PostGIS and Redis state, integration behavior and backup/restore before production OCI image construction also passed. Bounded startup retries cover only explicit transient socket/PostgreSQL-startup failures and preserve immediate failure for authentication or configuration errors. The same release-code, backend, WEAPP and workflow-owner checks also passed locally; Docker-dependent infrastructure remains unavailable in the Windows environment. The tracked release/provisioning shell scripts parse successfully with POSIX `sh` on the selected remote host. `.github/workflows/backend-staging.yml` defines disabled-by-default configurable-registry publication plus automatic exact-digest staging deployment only after a successful trusted-repository `main` push Product CI run.
  `.github/workflows/backend-production.yml` defines disabled-by-default, environment-protected manual promotion of the same staging-qualified digest without rebuild. `.github/workflows/wechat-platform.yml` defines disabled-by-default protected preview/upload of an exact environment-bound bundle without rebuilding it. The repository's GitHub `staging` environment now permits only the `main` branch and contains verified host access, pinned SSH Secrets, approved TCR bindings and the registry password Secret. Repository-level `STARWARD_STAGING_CD_ENABLED=true` enables the selected `operator-preview` lane; its environment-owned base descriptor points to private API/provider/data configuration outside Git. Formal domain/WeChat release credentials, production environment protection and the dedicated WeChat runner remain unconfigured.
  The trusted-main IP lane has now completed GitHub image publication, exact-digest TCR pull and remote deployment with an independent post-run check. Exact run, revision, digest, candidate and receipt identities remain in GitHub and environment-owned operation records rather than hard-coded Context.
- `infra/miniapp/docker-compose.yml` remains a local demo/development PostGIS and Redis stack with local credentials. It is not a release topology and must never be exposed or promoted. `infrastructure/environments/staging.json` and `production.json` remain unresolved declarations rather than deployed state or secret owners.
- The production profile keeps contribution media disabled because no production object-store adapter exists. `apps/wechat-miniapp` still defaults ordinary development builds to loopback and the tracked DevTools configuration disables URL checking for development. The repository release-bundle owner now requires an explicit lane/API origin, rewrites and verifies the generated project identity, and makes CI output structurally nondeployable; no staging/production AppID, HTTPS origin or platform credential has yet been bound, so no current bundle is a production-domain candidate.
- Current external-service authority remains the individual, owner-only, non-commercial profile in `project_context/global.md`. The operator-only development preview below is the sole remote application runtime: workstation access uses SSH and phone-debug access uses a token-guarded HTTPS IP edge. Public-user operation, domain allocation, ICP filing, Mini Program filing, subject change, platform review, production credentials and cloud release qualification remain outside this preview. Business registration, domain and filing work are paused for the current IP-inner-test task.
- On 2026-08-26 the owner purchased and paid CNY 109 for one Tencent Cloud Lighthouse instance in Shanghai for one year, expiring 2027-08-26. The provisioned host is running Ubuntu on the selected 4-core/4-GB, 3-Mbps, 40-GB-SSD, 300-GB-monthly-traffic plan and is the first paid footprint. Its provider instance identity, public IP, login material and generated credentials remain external secrets/operations data and are not stored in Context. The selected rollout sequence is to use this first host only as staging during setup, then fully decommission its staging state and reprovision it from a clean base as the persistent production host before public launch. It must not be promoted in place with staging data, volumes, credentials, queues, caches, backups or process state.
  Production promotion remains blocked until a second, separately owned staging host exists and re-establishes the complete staging receipt for the exact candidate. The proposed later staging host may be purchased through an authorized third party's Tencent account, but must remain non-production and satisfy provider access-filing eligibility plus the independent identity, data, secret and release-evidence boundaries below. Future promotional eligibility, account entitlement and prices remain action-time procurement facts rather than durable architecture.
- On 2026-08-26 that host also completed the tracked fresh-host bootstrap and an independent read-only acceptance pass. Verified runtime state is Ubuntu 24.04 x86-64, Node.js 24.19.0, Docker Engine 29.7.2, Docker Compose 5.5.0, active Docker service, the dedicated `starward-deploy` user with Docker access, the declared staging directory owners/modes, and active host firewall rules for SSH plus HTTP/HTTPS only. The Tencent Cloud instance firewall separately permits 22/TCP, 80/TCP, 443/TCP and 443/UDP; its default ICMP rule remains provider-owned. Direct Docker Hub endpoints were unreachable from the host, so the current Tencent Cloud officially documented Docker registry mirror was configured as provider-local daemon state and verified through `docker info`; pinned image digests remained unchanged.
  A dedicated externally held deployment key was enrolled and key-based SSH access was exercised. The private key, host key, endpoint and provider identifiers remain operational secrets/data outside Context. Root-mediated, fail-closed staging initialization generated distinct PostgreSQL, Redis and backup-key material plus one QWeather Ed25519 keypair outside the repository; an independent deploy-user read pass verified target type, root-write/deploy-group-read permissions, credential shape, database/Redis connection consistency and private/public key agreement without emitting secret values. The owner has since completed QWeather personal-developer registration and accepted its current terms, and a dedicated staging project, account-specific API Host and JWT credential now bind the generated public key.
  The credential is restricted to the staging server's public IP and only the weather-forecast and weather-alert API families; finance/console-statistics permissions remain disabled. Direct server-side JWT probes proved both the current-alert API and the Weather API v1 24-hour hourly endpoint with a real 24-row provider response and documented units; the legacy 72-hour grid request remained outside the current free subscription ceiling. No paid/standard QWeather subscription, top-up or 72-hour forecast entitlement has been authorized or completed. No stable release-class base descriptor, WeChat AppSecret, system-trusted domain TLS endpoint or formal release receipt exists yet; the live operator preview below intentionally uses a separate non-qualifying descriptor and local-test API lane.
- The owner-only preview reuses the existing staging PostgreSQL/Redis volumes, migrations, API and worker. Its current edge implementation is `compose.operator-preview.yml` plus `Caddyfile.operator-preview`: Caddy alone publishes 443/TCP, manages a publicly trusted short-lived IP certificate even when a client omits SNI, and requires a run-random header before proxying. Workstation and phone development builds use different API origins and must be produced sequentially because they share the generated-output root. Exact deployed revision/image and generated bundle fingerprints belong in environment-owned preview manifests and current runtime observations, not a hard-coded Context revision. Normal stop/resume preserves mutable volumes; a candidate update reuses those volumes and never repeats fresh-environment initialization.
  This lane is not a qualified staging release, disaster-recovery proof or production candidate. Official CLI access requires the user to enable the DevTools service port and complete login; a disabled port prevents even a reliable CLI login-state query. QR generation, physical-phone use and user confirmation remain separate observations required for device acceptance, which the owner has explicitly deferred from this deployment-only delivery.
- On 2026-08-26 TCR Personal Edition was initialized in its current mainland control-plane region, Guangzhou. The globally unique private namespace `starwardanywhere` and private repository `ccr.ccs.tencentyun.com/starwardanywhere/starward-miniapp-api` are now the single approved initial image repository. The protected GitHub `staging` environment has its non-secret host, account-id and repository bindings plus the fixed registry password Secret; the Shanghai `starward-deploy` identity completed `docker login`, and its resulting Docker config is owner-only mode `0600`. The password remains external secret state and is not stored in Context or the repository. Personal Edition's shared-service concurrency and service-level limitations remain accepted for the initial single-host profile.
  The exact merged `main` revision was manually archived, checksum-verified on the Shanghai host, built with the tracked Dockerfile, published after one bounded retry of a transient response-header timeout, resolved to an immutable registry digest, pulled from TCR by that digest and inspected for the exact revision, non-root runtime user and amd64 architecture. This establishes the initial TCR push/digest-resolution/Shanghai-pull path only; it is not trusted-main GitHub publication, a staging deployment or a release receipt.
