# WeChat Miniapp Development, Test, And Acceptance Environment Context

This root-owned, on-demand verification Context is the single durable owner for the WeChat Mini Program environment topology, owned paths, isolation boundaries, evidence-promotion rules, bootstrap decisions, and stable routing to its selected visual target. Product/screen responsibilities live in `project_context/areas/main/product-surface-contract.md` and `screen-contracts/wechat-miniapp.md`; exact visual values live in `DESIGN.md`. This file never treats planned code, a generated bundle or a historical run as proof that a runtime exists or passed.

## Reservation Status And Scope

**V2.0 产品面、工程 owner 与设计权威已采用；本轮 Long-Task 已建立真实小程序 workspace、同源 H5 诊断载体和 WeChat DevTools 当前会话采集器。Context 只记录边界，实际存在性与通过状态仍由代码和当前候选检查证明。**

- `apps/wechat-miniapp/**`, `packages/miniapp-contracts/**`, `workers/miniapp-api/**`, `tests/acceptance/miniapp/**` and `tools/miniapp/**` are the implemented owners for the current Demo candidate. Their presence, generated bundles and historical artifacts never imply a successful current run; the implementation index routes readers to the current entry points.
- `docs/wechat-miniapp-v2-source.md` is the dedicated current Source and binds the immutable V2.0 product/technical plans, selected DESIGN target, formal handoff and frozen resource closure. The native `docs/source-plan.md` remains authoritative only for its native-App delivery and cannot constrain this independent Mini Program back to auxiliary parity.
- Root `DESIGN.md` owns `target.system.wechat-miniapp-soft-instruments-2026-08-05` as an independent Mini Program profile. Its production projection may be generated under the Mini Program workspace, but cannot import Mobile/App/Admin target values, `packages/ui-system/**`, legacy `docs/design-*` exports, or HTML prototypes as a token source.
- Formal handoff preflight proves only immutable input identity/completeness. No package, command, DevTools import, screenshot or report is accepted until the sole Long-Task Final Gate reruns its declared checks on one final snapshot.

## Selected Mini Program Design Authority

- Stable target: `target.system.wechat-miniapp-soft-instruments-2026-08-05`.
- Canonical owner and anchor: `DESIGN.md#wechat-mini-program--soft-instruments-v1`; root `DESIGN.md` remains the single authored exact-value source.
- Frozen selected implementation-resource closure: `docs/design-resources/miniapp-selected-source-2026-08-06-v1/**`; its canonical `fact-manifest.json` SHA-256 is `91b4261b8bb0572473bf179cabf1af3992b3d0be3639a4ac9c397665b2df92b3`. The closure includes all twelve APP/MAP artifacts, every `metadata/*.artifact.json`, the design-system snapshot, selection/QA and reconciliation documents, resource-integrity/proof/render manifests, and both frozen Inspector/Oracle executables; sampling, screenshot-only reading, provider-managed temporary paths and older draft resources are not equivalent inputs.
- Formal implementation handoff: `docs/design-resources/miniapp-selected-handoff-2026-08-06/miniapp-complete-product-selected-v1.md` SHA-256 `d1e9513b93dc75319f6c2c2d37a242898803c16bf2266fabbecf44aa330d5ba3`. Its target is `reference + constraint`, so preflight proves resource identity/completeness but never production visual, interaction, accessibility, responsive or runtime conformance.
- Provider/editable upstream lineage: Open Design `user:soft-instruments`; project files, not provider state, are canonical.
- Coverage: complete route/page hierarchy, map/detail/night/my surfaces, shared components and controls, independent day/night/observation roles, responsive/mode/state matrices, semantic assets, interaction/motion, accessibility and recovery behavior.
- Boundary: V2.0 Source owns product semantics, `DESIGN.md` owns exact visual tokens, and the frozen resource/handoff closure constrains their page/component/state projection. None alone proves implementation, and HTML artifacts remain references rather than production code or a second token source.
- Dependency direction: the Mini Program package and its single generated token adapter consume the named `DESIGN.md` profile. They cannot import the App runtime token module, originate visual values, or edit the selected source snapshot in place.

### Selected-resource-first implementation rule

- Before material UI implementation or repair, map each affected production route, component, control, state, mode and viewport to every applicable APP/MAP resource and metadata record, then read that complete dependency closure. A convenient screenshot, one prototype page, aggregate design label or handoff preflight cannot substitute for the underlying layout, state, motion, accessibility and responsive constraints.
- Implement through real Mini Program routes and shared production owners. Exact colors, typography, spacing, radii and mode values come only from `DESIGN.md`; product text, actions, data and failure semantics come only from V2.0 Source/Context; frozen HTML is never copied into a WebView or treated as runtime fact.
- The first independently runnable production slice is the preferred early design-feedback point. Compare current production screenshots and interaction observations at declared viewports/modes/states against the selected-resource bindings, repair gaps before broadening, and rerun every affected comparison on the final candidate. Historical Open Design QA and handoff preflight remain input evidence only.

## Ownership And Dependency Direction

- This file is the unique source of truth for cross-layer Mini Program environment and evidence topology. `project_context/context.toml`, `project_context/global.md`, `project_context/architecture.md`, and the main verification index only route readers here; they do not duplicate its rules.
- Existing `project_context/areas/main/verification/development-loop.md`, `acceptance-runtime.md`, and `android-native.md` continue to own React Native/Web/Android App paths. Their commands, ports, artifacts, design targets and acceptance results cannot be projected onto the Mini Program.
- Mini Program source, runner, acceptance harness and selected service adapters consume this topology. Implementation code, generated config, CI or reports cannot become reverse authority.
- Do not create a separate Context workspace mirror merely because `apps/wechat-miniapp` becomes an npm workspace. Its durable product/screen facts are already owned by the main cross-workspace Area and this root verification node; add a sparse workspace Context only if later facts cannot be expressed without duplication.

## Four-Layer Environment Topology

### 1. Development

- `npm run dev:miniapp` is the primary interactive Mini Program entry: it health-starts the pinned PostGIS/Redis development services, then starts the modular-monolith API, Outbox/BullMQ worker and Taro weapp watch compiler against one development-only loopback binding, waits for the API and initial bundle, opens the repository-native project in WeChat DevTools, and owns teardown of the API/worker/compiler process trees. Infrastructure containers remain warm for the next inner-loop session. `--no-open` keeps the same source/watch loop without opening the GUI; `--api-port <port>` selects an otherwise free development port; `--memory` is an explicit unit/debug fallback that cannot supply persistence or acceptance evidence. Product edits remain in tracked source even while DevTools is open.
- Use the cheapest faithful feedback path first: changed-module checks through `npm run check:miniapp:fast`, then the Taro same-source H5 renderer where its boundary is applicable. A separately maintained browser imitation of the Mini Program is forbidden.
- For UI work, the cheap loop starts from the selected-resource mapping rather than the current screen. Use the same-source H5 lane for rapid multi-viewport layout/accessibility localization and the warm WeChat DevTools session for native component/API, package, route and lifecycle behavior; neither lane may silently relax a selected-resource constraint merely because the current implementation differs.
- Edit only tracked Taro source, shared contracts and owned configuration. `dist/h5/**`, `dist/weapp/**` and files opened inside WeChat DevTools are generated consumers, never an authoring surface or a second implementation truth; `dev:h5` and `dev:weapp` watch modes may regenerate them for feedback.
- Use weapp/WeChat DevTools development feedback when a change touches WeChat components or APIs, routing, permissions, storage, lifecycle, package configuration, or behavior H5 cannot faithfully exercise. This is still development feedback, not acceptance.
- WEAPP production code may use only JavaScript capabilities supported by the bound Mini Program base library or an app-owned, tested compatibility helper. A Node/browser host global such as `structuredClone` cannot be assumed from H5 or unit-test success; `packages/miniapp-contracts/**` owns the explicit `UserPreferences` snapshot copy used by the Mini Program synchronization path.
- Every development session uses disposable API, data, file, outbox, Provider-ledger, session, and output roots plus one bounded process/port owner. A warm process is reusable only inside its identified session and never becomes acceptance evidence.
- Development can prove only fast local feedback for the exercised renderer and inputs. It cannot produce a browser, DevTools, device, release, or product-acceptance conclusion.

### 2. Deterministic Test

- Unit, domain, contract, adapter, state-machine, and component tests use deterministic fixtures and a run-unique writable root. Uncontrollable platform or Provider behavior is injected behind the same future production boundary rather than replaced with a second implementation.
- Real paid Providers, production accounts/data, irreversible external side effects, shared mutable services, and nondeterministic clock/network/location dependencies are forbidden. Success, error, timeout, permission denial, network failure, retry/idempotency, recovery, cancellation, and cleanup are exercised where applicable.
- These checks can establish deterministic semantics and failure behavior inside their declared harness. They cannot prove a WeChat renderer, DevTools runtime, permission prompt, map/location integration, device lifecycle, or packaged build.

### 3. Browser / Tool Acceptance

- First freeze a settled candidate and let the project-owned H5 acceptance server cleanly regenerate the same-source Taro H5 projection. It owns dynamic loopback ports and one bounded API/server process set for that invocation; `dist/h5/**` remains an ignored generated consumer and cannot be shared by concurrent formal H5 runs. Run the complete applicable portable journey; a development watch session, externally installed Playwright CLI or partial smoke run is not formal browser acceptance.
- Only after deterministic and full browser checks are green and no relevant input is stale, cleanly regenerate `apps/wechat-miniapp/dist/weapp/**` and open the exact current-candidate `apps/wechat-miniapp/project.config.json` through one exclusive formal invocation. Final Gate materializes its physical Harness snapshot under one unique `C:\Dev\.starward-tmp\run-<id>` root; aliases, junctions, `SUBST`, canonical-checkout substitution and copied production roots are forbidden. The Harness process uses that root as `TEMP/TMP`, while WeChat DevTools children use the physical user temporary directory so tool-owned temp state cannot collide with snapshot cleanup. The project-scoped, version-pinned Harness compatibility overlay only strengthens exact-root removal retries and must fail on an unknown package version or source shape; it cannot change assertions, counterfactual meaning or Final-Gate acceptance semantics.
- Harness runners receive the declared minimal environment rather than an interactive shell clone. Infrastructure code therefore cannot depend on Docker Desktop discovering its Compose plugin through undeclared Windows variables such as `ProgramFiles`. The project-owned `tools/miniapp/docker-compose-runtime.mjs` is the single invocation adapter: Windows uses the installed standalone Compose v2 executable, other platforms use the standard `docker compose` subcommand, and both infrastructure startup plus backup/restore reuse that owner. A canonical-shell pass is only local feedback; the complete infrastructure path must also pass from a physical Harness snapshot under the minimal environment.
- WeChat DevTools caches projects by AppID/project identity and may initially open a new physical snapshot with an empty project-state AppID even when the tracked config is correct. The runner overlays only ignored `project.private.config.json` with a deterministic project name unique to normalized snapshot root plus candidate fingerprint and pins base library `3.17.1`. For a newly materialized snapshot it first runs a non-product registration session: bind at least one native watcher and require every observed `wxfilewatcher_x64.exe` to target the exact physical candidate, rewrite `project.config.json` with the exact same bytes to trigger DevTools' supported dynamic config reload, require `Tool.getInfo.SDKVersion === 3.17.1`, close through `App.exit`/`Tool.close`, then prove complete process/port/watcher teardown. That session makes no journey or product-acceptance claim. Canonical interactive development does not need this isolated registration step.
- Formal setup and evidence sessions then run from clean tool processes. Setup moves to the network-neutral auth route, exactly resets/reads back the canonical empty DAY/MY seed, proves the production Settings DAY control and live `theme-day` class, records diagnostics, waits for runtime-event quiescence and closes; its state is discarded. Evidence independently resets again before journeys. Degradation keeps the BFF healthy until the named fault window. Runtime observers detach only after quiescence; unhandled rejections fail the run. The Windows launcher preserves installed `cli.bat` semantics, pins IDE HTTP port `23977` and automation port `9420`, observes callback ports, and closes every exact `--cli` root process plus every watcher and declared/observed port for a stable window.
- DevTools may rewrite tracked `project.config.json` into byte-different but JSON-semantically identical formatting. Restoration is allowed only after the evidence IDE/watchers are fully closed and only when canonical JSON equality proves that no value changed; otherwise ownership is lost and the run fails without overwriting. The runner then restores the exact original public bytes and exact prior private bytes/absence, and only afterward computes the candidate after-fingerprint. Complete generated-bundle and candidate fingerprints must equal their pre-session values. Final evidence is written only after these restorations and all teardown pass. Historical JSON/screenshots, CLI success, a live socket, hand-edited generated output or a retry against another checkout cannot be promoted to evidence.
- A settled browser build plus isolated API/data can prove only the portable journey actually observed in the browser. It cannot prove WeChat component/API semantics, package limits, permissions, storage, lifecycle, DevTools import/build, or a real device.
- A settled weapp build plus WeChat DevTools can prove only the exact tool-side WeChat runtime behavior actually observed for the bound candidate, tool version, project configuration, fixtures, and environment. It cannot inherit unobserved browser claims or prove representative-device behavior.

### 4. Representative-Device Acceptance

- Produce a device-acceptance preview or experience build from one settled candidate in the dedicated device environment. Every selected representative device must use the same exact device-lane build identity; this does not permit reuse of the DevTools lane's AppID/key, writable state, secrets, or project directory.
- Verify the selected permissions and denial paths, network profiles and recovery, location behavior and privacy, map behavior, cold start, warm start, foreground/background and termination/relaunch lifecycle, storage/readback where applicable, cleanup, and measured performance on the declared device population.
- Device model/OS, WeChat version, base-library version, network/location conditions, sample population, workload, metrics, budgets, comparators, and tolerances remain decision-required. No performance or capacity claim exists until a project-owned measurement binds all of them.
- Device-specific conclusions cannot be inferred from Browser or DevTools results. Results from one device, OS, WeChat version, permission state, or network profile cannot be generalized to an undeclared population.

## Candidate Promotion, Freshness, And Cost Control

1. Develop and repair with deterministic checks plus the cheapest faithful hot renderer. Coalesce one coherent change batch before promotion.
2. Freeze one candidate identity covering all acceptance-relevant Taro source, dependency lock, project/build/base-library configuration, fixture/data contract, selected assets, API contracts, acceptance scenarios, verifier/launcher closure and tool environment. After those bytes and the active Long-Task Authority are final, run `npm run prepare:miniapp:final-candidate` exactly once to collect the current delivery carrier; commit that carrier without changing any bound input afterward. The runner computes this identity before collection and rechecks it before emitting evidence. A normal collection/Check is a `complete_candidate` and requires exact equality with the full carrier manifest and aggregate digest. Cross-surface evidence binds native and H5 observations to that one validated `source_snapshot.sha256`; platform-specific bundle, screenshot, command-output or log hashes remain independent artifacts and cannot impersonate a shared state version. A Harness-declared counterfactual runs in a selective `counterfactual_projection`: every present file must still match the carrier, and the Source/spec/verifier/frozen-launcher/package minimum closure must be present, but files deliberately omitted by the Harness sandbox are not mistaken for candidate deletion. Projection status derives only from the declared carrier mutation; it can demonstrate exactly designated assertion failures and preserved liveness but can never be accepted as a complete candidate. Stale carrier detection runs before Source parsing or expensive execution. Final-Gate Checks never refresh the carrier in place because that would erase Harness-owned mutations; a changed input or incomplete closure fails stale rather than inheriting a previous pass.
3. Run deterministic checks, then one complete applicable H5 browser acceptance pass for that settled candidate.
4. If the candidate remains settled, generate/import weapp once and run the complete applicable WeChat DevTools acceptance pass. A failed tool pass is a collection point for batched repair, not a rebuild trigger per finding.
5. After Browser and DevTools obligations are fresh, generate the dedicated device preview/experience build and run the declared representative-device matrix with the same exact device-lane build.
6. A relevant candidate, environment, fixture, API, toolchain, AppID/config, or build-input change invalidates every dependent downstream result. A platform-only change need not rerun an unaffected browser result only when the future runner proves the browser fingerprint unchanged; DevTools and device obligations remain mandatory for that platform boundary.
7. Test-only reports, screenshots, logs, and other excluded non-production outputs must not force a package rebuild, but they also never make evidence fresh. Exact production/build and evidence-freshness input sets must be declared separately when commands are implemented.
8. Do not package after each edit, after each hypothesis, or merely because another cheap check ran. Package only at a stable promotion boundary or when a defect can be reproduced only in the packaged/tool/device boundary.
9. Record build, import, startup, exercise, and teardown durations only in disposable run output. The topology is cost-ordered, but it makes no claim that H5, weapp packaging, DevTools, or device verification currently takes any particular time or is globally optimal without measurement.

The stable command ladder is `npm run dev:miniapp` for the warm native inner loop, `npm run check:miniapp:fast` for deterministic changed-candidate feedback, `npm run test:miniapp:h5` for the complete portable multi-viewport lane, `npm run test:miniapp:native` for an isolated fresh WeChat DevTools collection, `npm run test:miniapp:current` for their ordered conjunction, and `npm run prepare:miniapp:final-candidate` for the one post-freeze Long-Task carrier collection immediately before candidate commit and Final Gate. The preparation result is a bound input and counterfactual carrier, not acceptance evidence; Final Gate independently reruns every declared Check. The Long-Task verifier must invoke and independently interpret fresh commands from this ladder; it cannot treat the aggregate script name, a historical report, manual QR scan, or DevTools GUI state as proof.

Formal acceptance is the conjunction of fresh, applicable observations from their own layers on the bound candidate. Missing journeys, commands, environments, tools, devices or external confirmation remain `unverified` or `decision-required`; no weaker layer upgrades the claim.

## Reserved Paths

| Path | Reservation and current status | Future ownership and persistence rule |
| --- | --- | --- |
| `apps/wechat-miniapp/**` | Implemented Mini Program source/workspace root. | Taro/React/TypeScript production source and tracked configuration. Existence never implies a successful build or acceptance. |
| `apps/wechat-miniapp/dist/**` | Generated weapp/H5 output root; it may be absent before a build and is recreated from current source. | Generated only, never durable Context or tracked source. Formal H5 and weapp lanes own distinct output subroots; one invocation exclusively owns its subroot and concurrent writers to that subroot are forbidden. |
| `apps/wechat-miniapp/project.private.config.json` | Implemented machine-local WeChat project configuration used by manual sessions and as a reversible formal-session overlay. | Exact-ignore, machine-local only. Formal collection may replace only its session-owned bytes with a unique current-snapshot project identity and pinned acceptance SDK, must verify readback and restore the exact prior bytes or absence, and must fail rather than overwrite a concurrent external change. It must never be committed, copied into reports, or treated as shared configuration authority. |
| `tests/acceptance/miniapp/**` | Implemented same-source H5 acceptance-code root using the exact project-owned Playwright dependency. | Trackable current-candidate scenario/oracle code. Code presence cannot prove a run passed. |
| `tests/acceptance/playwright-report/miniapp/**` | Reserved, currently absent generated acceptance-report root. | Run-specific generated output only; never committed or promoted into Context. Retention/redaction remains decision-required. |
| `tools/miniapp/**` | Implemented platform-runner/tool root, including clean H5 serving, deterministic asset checks, current-session DevTools collection, the shared Docker Compose invocation adapter, the version-pinned Harness compatibility overlay and the physical-snapshot proof wrapper. | Trackable orchestration, health, fingerprint, DevTools import, process and scoped-cleanup code. Windows infrastructure execution must remain independent of undeclared Docker plugin-discovery environment; canonical-shell success cannot substitute for a physical minimal-environment Harness-snapshot pass. Compatibility changes must preserve or strengthen anti-degradation and fail closed on unknown package/source shape. Only implemented commands registered in package scripts count as capabilities. |
| Dynamic loopback H5/API ports plus ignored `apps/wechat-miniapp/dist/h5/**` | Implemented H5 acceptance process boundary. | One formal invocation owns the clean generated output and its dynamic ports/processes; concurrent formal H5 runs against the same output root are forbidden. |
| `apps/wechat-miniapp/project.config.json` plus `dist/weapp/**` | Implemented canonical DevTools acceptance root and clean generated bundle. | A formal runner exclusively owns the DevTools session and weapp output. Same-byte public-config refresh is allowed only to trigger supported tool reload; byte-only DevTools normalization may be restored only after semantic equality and ownership checks. Exact original public bytes, candidate fingerprint and bundle fingerprint must be restored/equal before success. |
| `C:\Dev\.starward-tmp\run-<12-hex>\ty-context-*` | Implemented external physical Final-Gate snapshot root. | One proof invocation owns one exact resolved root. DevTools child `TEMP/TMP` stays outside it; leaked Harness snapshots fail the wrapper, and cleanup may retry only that validated invocation root. |
| Repository-external absolute paths for preview QR codes, upload private keys, AppID/key material, and other secrets | Exact paths and values are intentionally absent and must not be recorded in Context. | Inject through a future approved local/CI secret mechanism. Never place secret values, QR artifacts, cookies, tokens, precise device identifiers, or complete sensitive fields in source, logs, reports, or Context. |

Additional future run roots may use `%TEMP%/starward-miniapp-dev-*`, `%TEMP%/starward-miniapp-test-*`, and `%TEMP%/starward-miniapp-device-*`. They are logical reservations, not existing directories or commands. Every `*` resolves to an unambiguous run identity; exact naming and retention remain decision-required.

## Isolation And Resource-Lifecycle Invariants

- Development, Deterministic Test, Browser Acceptance, DevTools Acceptance, and Device Acceptance never share a writable database, file root, outbox, Provider ledger, session secret, AppID/key, port, process owner, mutable cache, or generated-output directory. A layer for which AppID/key is not applicable records that boundary as N/A instead of borrowing another layer's credentials.
- Each layer has a distinct environment identity and run namespace. Immutable source and read-only fixtures may be shared only by exact digest; each writable consumer receives an isolated copy or isolated service namespace.
- A run owns its exact child-process tree, ports, temporary roots, emulator/tool/device session, and report output. Startup fails closed on an unknown listener, mismatched owner, stale descriptor, dirty non-resettable state, missing secret binding, or ambiguous project identity.
- Success, failure, timeout, cancellation, and tool interruption all execute bounded teardown. Cleanup may remove only explicitly resolved run-owned temporary paths and processes; it must not broadly delete repository roots, shared caches, another run, another environment, or user data. Interrupted leftovers are disposable and cannot be reused without exact owner/fingerprint validation.
- Provider/API calls use lane-specific adapters, credentials, quota ledgers, idempotency identities, and writable state. Deterministic Test always uses isolated substitutes; any real sandbox/free Provider in other layers requires explicit authorization, provenance, cost and side-effect policy. Production credentials, paid traffic, public redistribution, and irreversible actions are not implied.
- Private config, secrets, QR codes, generated bundles, screenshots, traces, raw logs, device identifiers, and run reports are runtime artifacts, not durable facts. Context may name their approved class or reserved location but never embeds their contents or a historical pass/fail result.
- Precise location, permission state, account/session identity, and device metadata are minimized and redacted in reports. The future acceptance owner must define retention and deletion before representative-device evidence is collected.

## Failure And Recovery Ownership

The following responsibilities are mandatory before the corresponding carrier can be considered runnable; current existence is reported by the implementation index and checks, not inferred here.

| Boundary | Future owner | Required behavior |
| --- | --- | --- |
| User-visible miniapp runtime | `apps/wechat-miniapp/**` runtime owner | Distinguish initial/loading/ready/empty/partial/stale/error/permission-denied; own permission request and denial, network/tool/API timeout presentation, retry/cancel, lifecycle restoration, local-state recovery, and user-visible cleanup result. |
| Environment and platform runner | `tools/miniapp/**` runner owner | Allocate and verify run identity, ports, processes, temp roots, environment config and candidate fingerprint; enforce startup health, timeouts, cancellation, complete child-process teardown, and scoped cleanup. |
| Acceptance scenarios and reports | `tests/acceptance/miniapp/**` acceptance owner | Establish/reset deterministic state, exercise positive and negative journeys, bind observations to candidate/environment/tool/device identity, assert recovery and cleanup, redact outputs, and place only disposable reports under the reserved report root. |
| API and Provider behavior | `workers/miniapp-api/**` composition plus `packages/miniapp-contracts/**` and Mini Program client ports | Own request timeout, retry/backoff, idempotency/conflict, Provider degradation, outbox/ledger consistency, cost/quota enforcement, and restart readback. Demo fixture adapters are explicitly `SAMPLE_DATA` and cannot impersonate provider-backed success. |
| Representative-device session | Future device-acceptance runner plus named human/device operator | Prepare the declared device/network/permission states, verify the exact device-lane build, perform lifecycle transitions, record bounded observations, and revoke/clean local credentials and artifacts after the run. |

An app-level error label does not prove the adapter timed out or recovered; a runner exit code does not prove cleanup; an API receipt does not prove the UI handled it. Each owner must expose the furthest independently failing boundary it claims.

## Remaining Decisions And External Inputs

- Exact package/dependency/base-library versions, commands, fingerprints and failure contracts are pinned by the active Contract and lockfile during bootstrap, then recorded from code truth in the implementation index and verification Context.
- H5 acceptance is limited to semantics implemented by the same Taro source. WeChat map/canvas/sensor/permission/lifecycle/package behavior requires weapp + DevTools and declared physical-device observations; H5 cannot fill those rows.
- The local WeChat DevTools CLI is `C:\Program Files (x86)\Tencent\微信web开发者工具\cli.bat`; its current tool version/login state and observed project-path identity must be captured by the runner. Formal acceptance pins and verifies base library `3.17.1`; no previously imported GUI project, socket, watcher, private config or historical tool result is a passing fact.
- Environment-specific AppIDs, keys, project config, upload identity, preview/experience channel, repository-external absolute secret/QR locations, rotation/revocation, CI secret mechanism, and redaction policy. No lane may reuse another lane's AppID/key.
- Exact endpoint compatibility, fixture catalogue and adapter reuse are implementation obligations under the declared shared owners; no current API behavior may redefine V2.0 or leak native-App-only platform semantics.
- Exact browser, DevTools and device journey matrices, negative cases, observation/comparator policy and machine/external split are frozen in the active Contract before implementation.
- Representative device/OS/WeChat-version population, selection rationale, network and location profiles, performance/capacity workload, metrics, sample count, baseline/budget, comparator/tolerance, and owner of any required human confirmation.
- CI lane, runner OS/image, tool installation/licensing/login feasibility, parallelism and mutable-resource allocation, artifact/report retention, redaction, failure diagnostics, and promotion policy. There is no current miniapp CI lane.

## Active Bootstrap Admission

The active Long-Task may convert the owned paths into current capability only in this order:

1. Define and update the owning product/surface Context for the selected Mini Program responsibilities, consume `target.system.wechat-miniapp-soft-instruments-2026-08-05`, and map every affected production surface to the complete `miniapp-selected-source-2026-08-06-v1` plus formal handoff closure before implementation; do not borrow App/Admin artifacts, sample only screenshots or invent another visual authority.
2. Create a real package manifest and intentionally register the workspace, dependency locks, versions, commands, and implementation owner; then update the implementation index from code truth.
3. Install exact ignore rules before generating `dist/**`, `project.private.config.json`, QR/secret material, temporary projects, or reports. Confirm all secret and sensitive-artifact paths resolve outside the repository where required.
4. Implement the isolated runner, health/fingerprint protocol, deterministic reset, failure/recovery seams, teardown, and scoped cleanup before registering a CI or acceptance lane.
5. Bind every environment to distinct mutable resources and credentials, then prove the promotion/staleness rules with project-owned checks. Only actual runnable commands may be added to verification Context.
6. Update this Context and its triggers whenever a reservation becomes current, a command/version/path/owner changes, or a new evidence boundary is adopted. Do not create a ghost Area merely because the source directory was bootstrapped.

## Design Rationale And Alternatives

- A root, on-demand Context keeps the absent cross-workspace capability discoverable without inflating the default Area or pretending a workspace exists. Placing these facts in the current Mobile development loop would wrongly inherit Expo/Android commands and evidence; scattering them across architecture and verification files would create duplicate truth.
- The cost-ordered ladder preserves fast feedback while retaining platform-specific proof. Always packaging after each edit wastes an unmeasured expensive boundary; browser-only acceptance misses WeChat semantics; DevTools-only acceptance slows portable repairs and still misses real-device behavior; shared mutable environments destroy attribution and recovery confidence.
- A plausible future challenge is adding a second miniapp target or CI-hosted DevTools lane. It lands as another isolated environment binding and promotion obligation here, with workspace-local detail only if durable facts warrant it; it must not duplicate product truth or reverse the dependency into current App/Admin owners.
- No numeric speed, reliability, capacity, or compatibility improvement is claimed. Future optimization requires measured workload, environment, baseline/budget, comparator/tolerance, and a project-owned measurement.

## Repository Verification Boundary

- `npm run context:validate`, `make validate-context`, `make validate-harness`, `npm run context:doctor`, and the package-managed source-parity check can validate Context structure, routing, installation health, and applicable source-workspace parity.
- Repository review must confirm any current Mini Program workspace, acceptance and tool paths match their owners; generated/private artifacts remain ignored; the implementation index matches code truth; the selected visual target remains rooted only in `DESIGN.md`; and no historical output is promoted into Context.
- Context checks do not build, test, run or accept the Mini Program. Only the active Contract's current-candidate checks and sole Final Gate may do so.
