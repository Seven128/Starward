# Mini Program Workflow Authority And Scope

This on-demand verification node owns the reserved scope, selected-design routing and cross-owner dependency direction for the Mini Program development/test/acceptance environment.

## Reservation Status And Scope

**当前已采纳方案的产品面、工程 owner 与 selected 设计约束直接作用于唯一现有实现；真实小程序 workspace 和 WeChat DevTools 当前会话采集器沿用既有 owner。Mini Program 不保留 Web/H5 产品实现或验收代理；资料版本仅表示来源沿革，不建立开发版本。Context 只记录边界，实际存在性与通过状态仍由代码、当前候选检查及声明的 External Confirmation 证明。**

- `apps/wechat-miniapp/**`, `packages/miniapp-contracts/**`, `workers/miniapp-api/**` and `tools/miniapp/**` are the implemented owners for the one current candidate. Their presence, generated bundles and historical artifacts never imply a successful current run; the implementation index routes readers to the current entry points.
- `docs/wechat-miniapp-v2-1-1-source.md` is an immutable Source inventory and provenance carrier. It SHA-binds the accepted proposal inputs and complete 97-row `selected-requirement-dispositions.json` census; its embedded Soft Instruments target and selected-v3 resource identities are historical visual provenance superseded by the current Field Signal system selection. Its product, technical, data, state, safety and interaction meanings remain controlling under correction/disposition precedence. The filename and proposal labels do not identify a parallel implementation or make a Long-Task active; the native `docs/source-plan.md` remains authoritative only for its native-App delivery and cannot constrain this independent Mini Program back to auxiliary parity.
- Root `DESIGN.md` owns `target.system.wechat-miniapp-sky-canvas-field-signal-2026-09-02` as the independent active Mini Program profile. Its production projection may be generated under the Mini Program workspace, but cannot import Mobile/App/Admin target values, `packages/ui-system/**`, legacy `docs/design-*` exports, old selected page resources or HTML prototypes as a token source. Selection alone does not claim that the current production projection has migrated or conforms.
- Byte-bound Mini Program technical Source uses the repository LF rules for JSON and source TS/TSX/SCSS so Windows and CI checkouts do not change its meaning-preserving digests. A real Source change still requires capability review and a new immutable technical binding; hash comparison is never normalized or bypassed by verification.
- Formal handoff preflight proves only immutable input identity/completeness. No package, command, DevTools import, screenshot or report proves delivery until the active workflow reruns every applicable check on one final snapshot.

## Selected Mini Program Design Authority

- Stable target: `target.system.wechat-miniapp-sky-canvas-field-signal-2026-09-02`.
- Canonical owner and anchor: `DESIGN.md#wechat-mini-program--sky-canvas-field-signal`; root `DESIGN.md` remains the single authored exact-value source.
- Immutable selected system source: `docs/design-resources/miniapp-design-system-2026-09-02-sky-canvas-field-signal/selected-source/DESIGN.md` SHA-256 `a3868d68649e51951f8ae9f9e7a4fa7a08a9270aa491f3f463d04fede655be2e`; complete manifest SHA-256 `d719dd753422112c4759cd77d0d9da3b7d40d5dd87b38fd3d327835f739f8bde`; package digest `253fbcbfaa083aa897eca2faf5e4eb6f3b99e69da7f485d485f89881adcc8276`; source-index SHA-256 `727114ee2f72f6a68a8bd0d25c4d20470ae8b0d6a0ff2bcff6d0067e367543c1`.
- Historical page-resource closure: `docs/design-resources/miniapp-selected-source-2026-08-22-v3/**`, `target-miniapp-sky-canvas-current-constraint` and their formal handoffs remain immutable audit provenance with inactive visual styling, geometry, composition, interaction presentation and fidelity constraints. The current interaction-resource constraint is instead `target-miniapp-field-signal-i21-selected-constraint-2026-09-03`, whose unique adoption record, immutable locator and digests are owned by `areas/main/screen-contracts/wechat-miniapp.md#selected-design-adoption-record`. It constrains the five current Surfaces, nine routes, 62 material Controls and declared source-matrix conditions without becoming product/data/state truth or a pixel-exact target.
- Provider/editable upstream lineage: Open Design `0.21.1`, design-system ID `user:starward-mini-program-sky-canvas-field-signal-revision`, project `ds-starward-mini-program-sky-canvas-field-signal-revision`, conversation `0bcc669b-29b2-476b-b93d-f502d7b09917`. Revision `bccaa010-a3ef-4895-9fb0-4c874239fdac` is a pending, noncanonical lifecycle record whose proposed body predates the final reviewed project bytes; the immutable reviewed project snapshot is the selected Source.
- Coverage: system-level day/night/observation roles, four-layer whitespace and visual-occupancy rules, mobile typography/control proportions, semantic component families, focus/press/motion/accessibility rules and 320/375/390/430 CSS px review come from the active system. I21 adds the selected composition, interaction and component-realization constraint for the complete five-Surface/nine-route/62-Control package. Product meaning and responsibility remain in the current Mini Program Screen Contract rather than being copied into either design target.
- Boundary: the current accepted Source under correction/disposition precedence owns product semantics, `DESIGN.md` owns exact visual tokens, the Screen Contract owns page/control/state responsibility, and the I21 selected package constrains the concrete composition and interaction realization. Its handoff preflight proves only immutable input identity, complete Fact closure and feasibility-input consistency; no HTML, manifest, prototype, screenshot or preflight result is production code, a second token/business/data authority or production-conformance evidence.
- Resource retention: the current Field Signal Open Design project plus the immutable I21 selected package are the editable-upstream/selected-snapshot pair named by the Screen Contract. A visible or semantic revision creates a new bounded project/snapshot and moves the one canonical record; it never overwrites I21 in place. The older Sky Canvas project and retired page resources remain audit provenance only and cannot be revised, reused or treated as fallback. `target.system.wechat-miniapp-sky-canvas-2026-08-25` is historical provenance only and no exception authorizes a parallel product implementation.
- Dependency direction: the Mini Program package, its single generated token adapter and any pinned `@taroify/core` component adapters consume the named `DESIGN.md` profile. A library may provide generic mechanics only; it cannot import the App runtime token module, originate visual/product/state values, bypass the existing component/icon/state owners, or edit the selected source snapshot in place.

### Selected-resource-first implementation rule

- Before material UI implementation or repair, map each affected production route, component, control, state, mode and viewport to the active Field Signal profile, current Mini Program Screen Contract and the complete I21 formal handoff/canonical dependency closure named by its adoption record. Read the implementation mapping and feasibility input without sampling. Retired handoffs, convenient screenshots, aggregate labels or preflight alone cannot substitute for those owners or prove production conformance.
- Implement through real Mini Program routes and shared production owners. Exact colors, typography, spacing, radii and mode values come only from `DESIGN.md`; product text, actions, data and failure semantics come from the current accepted Source/Context under its correction/disposition precedence; frozen HTML is never copied into a WebView or treated as runtime fact.
- The first independently runnable production slice is the preferred early design-feedback point. Compare current production screenshots and interaction observations at declared viewports/modes/states against the selected-resource bindings, repair gaps before broadening, and rerun every affected comparison on the final candidate. Historical Open Design QA and handoff preflight remain input evidence only.

## Ownership And Dependency Direction

- This file is the unique source of truth for cross-layer Mini Program environment and evidence topology. `project_context/context.toml`, `project_context/global.md`, `project_context/architecture.md`, and the main verification index only route readers here; they do not duplicate its rules.
- `project_context/deployment.md` separately owns remote staging/production, public domains, release promotion, production secrets, migration, backup/restore, rollback and production observability. This file may qualify the Mini Program candidate and experience/device evidence but cannot deploy or publicly release it.
- Existing `project_context/areas/main/verification/development-loop.md`, `acceptance-runtime.md`, and `android-native.md` continue to own React Native/Web/Android App paths. Their commands, ports, artifacts, design targets and acceptance results cannot be projected onto the Mini Program.
- Mini Program source, runner, acceptance harness and selected service adapters consume this topology. Implementation code, generated config, CI or reports cannot become reverse authority.
- Do not create a separate Context workspace mirror merely because `apps/wechat-miniapp` becomes an npm workspace. Its durable product/screen facts are already owned by the main cross-workspace Area and this root verification node; add a sparse workspace Context only if later facts cannot be expressed without duplication.
