# Mini Program Workflow Paths And Lifecycle

This on-demand verification node owns reserved paths, environment isolation, process/resource lifecycle and failure/recovery rules across all Mini Program feedback and acceptance lanes.

## Reserved Paths

| Path                                                                                                                | Reservation and current status                                                                                                                                                              | Future ownership and persistence rule                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `apps/wechat-miniapp/**`                                                                                            | Implemented Mini Program source/workspace root.                                                                                                                                             | Taro/React/TypeScript production source and tracked configuration. Existence never implies a successful build or acceptance.                                                                                                                                                                                                                                                                                                                           |
| `apps/wechat-miniapp/dist/weapp/**`                                                                                 | Generated WEAPP output root; it may be absent before a build and is recreated from current source.                                                                                          | Generated only, never durable Context or tracked source. One invocation exclusively owns the output root and concurrent writers are forbidden.                                                                                                                                                                                                                                                                                                         |
| `apps/wechat-miniapp/project.private.config.json`                                                                   | Implemented machine-local WeChat project configuration used by manual sessions and as a reversible formal-session overlay.                                                                  | Exact-ignore, machine-local only. Formal collection may replace only its session-owned bytes with a unique current-snapshot project identity and pinned acceptance SDK, must verify readback and restore the exact prior bytes or absence, and must fail rather than overwrite a concurrent external change. A guarded AppID migration may update an existing file only when its exact prior identity still matches the tracked public config; it must never be committed, copied into reports, or treated as shared configuration authority. |
| `tools/miniapp/**`                                                                                                  | Implemented platform-runner/tool root, including deterministic asset checks, current-session DevTools collection, guarded AppID migration, authenticated admin CLI and the shared Docker Compose invocation adapter. | Trackable orchestration, health, fingerprint, DevTools import, process and scoped-cleanup code. Only implemented commands registered in package scripts count as capabilities. The AppID migration owner requires an explicit expected-current/new identity pair, defaults to a read-only plan, redacts values from output, detects tracked/local identity drift and rolls back only its own bytes on a partial write. It never creates a platform account or migrates secrets/OpenID data. |
| `tools/miniapp/build-release-bundle.mjs` plus `artifacts/miniapp/release/**`                                        | Implemented owner for environment-bound production-mode WEAPP compilation and its disposable fingerprint manifest.                                                                          | The command exclusively owns one generated-output build at a time, binds and verifies the generated project identity, and scans compiled output for lane/AppID mismatches. `ci` output is deliberately non-deployable and uses a reserved `.invalid` API origin; staging/production require separate AppID identity and real HTTPS origin. Generated bundles and manifests remain ignored runtime artifacts rather than Source or acceptance evidence. |
| `apps/wechat-miniapp/project.config.json` plus `dist/weapp/**`                                                      | Implemented canonical DevTools acceptance root, tracked current Mini Program AppID identity and clean generated bundle.                                                                      | A formal runner exclusively owns the DevTools session and weapp output. Same-byte public-config refresh is allowed only to trigger supported tool reload; byte-only DevTools normalization may be restored only after semantic equality and ownership checks. A deployable bundle must use exactly this tracked AppID and must fail when a protected lane variable names another identity. Exact original public bytes, candidate fingerprint and bundle fingerprint must be restored/equal before success. |
| `E:\Dev\.starward-tmp\run-<12-hex>\ty-context-*`                                                                    | Implemented external physical Final-Gate snapshot root.                                                                                                                                     | One proof invocation owns one exact resolved root. DevTools child `TEMP/TMP` stays outside it; leaked Harness snapshots fail the wrapper, and cleanup may retry only that validated invocation root.                                                                                                                                                                                                                                                   |
| Repository-external absolute paths for preview QR codes, upload private keys, AppID/key material, and other secrets | Exact paths and values are intentionally absent and must not be recorded in Context.                                                                                                        | The implemented platform workflow materializes its protected environment key only in a dedicated runner temporary path and removes it after the operation; local invocation must use the same repository-external absolute-path rule. Never place secret values, QR artifacts, cookies, tokens, precise device identifiers, or complete sensitive fields in source, logs, reports, or Context.                                                         |

Additional future run roots may use `%TEMP%/starward-miniapp-dev-*`, `%TEMP%/starward-miniapp-test-*`, and `%TEMP%/starward-miniapp-device-*`. They are logical reservations, not existing directories or commands. Every `*` resolves to an unambiguous run identity; exact naming and retention remain decision-required.

## Isolation And Resource-Lifecycle Invariants

- Development, Deterministic Test, DevTools Acceptance, and Device Acceptance never share a writable database, file root, outbox, Provider ledger, session secret, AppID/key, port, process owner, mutable cache, or generated-output directory. A layer for which AppID/key is not applicable records that boundary as N/A instead of borrowing another layer's credentials.
- Each layer has a distinct environment identity and run namespace. Immutable source and read-only fixtures may be shared only by exact digest; each writable consumer receives an isolated copy or isolated service namespace.
- A run owns its exact child-process tree, ports, temporary roots, emulator/tool/device session, and report output. Startup fails closed on an unknown listener, mismatched owner, stale descriptor, dirty non-resettable state, missing secret binding, or ambiguous project identity.
- Success, failure, timeout, cancellation, and tool interruption all execute bounded teardown. Cleanup may remove only explicitly resolved run-owned temporary paths and processes; it must not broadly delete repository roots, shared caches, another run, another environment, or user data. Interrupted leftovers are disposable and cannot be reused without exact owner/fingerprint validation.
- Provider/API calls use lane-specific adapters, credentials, quota ledgers, idempotency identities, and writable state. Deterministic Test always uses isolated substitutes; any real sandbox/free Provider in other layers requires explicit authorization, provenance, cost and side-effect policy. Production credentials, paid traffic, public redistribution, and irreversible actions are not implied.
- Private config, secrets, QR codes, generated bundles, screenshots, traces, raw logs, device identifiers, and run reports are runtime artifacts, not durable facts. Context may name their approved class or reserved location but never embeds their contents or a historical pass/fail result.
- Precise location, permission state, account/session identity, and device metadata are minimized and redacted in reports. The future acceptance owner must define retention and deletion before representative-device evidence is collected.

## Failure And Recovery Ownership

The following responsibilities are mandatory before the corresponding carrier can be considered runnable; current existence is reported by the implementation index and checks, not inferred here.

| Boundary                         | Future owner                                                                                            | Required behavior                                                                                                                                                                                                                                           |
| -------------------------------- | ------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| User-visible miniapp runtime     | `apps/wechat-miniapp/**` runtime owner                                                                  | Distinguish initial/loading/ready/empty/partial/stale/error/permission-denied; own permission request and denial, network/tool/API timeout presentation, retry/cancel, lifecycle restoration, local-state recovery, and user-visible cleanup result.        |
| Environment and platform runner  | `tools/miniapp/**` runner owner                                                                         | Allocate and verify run identity, ports, processes, temp roots, environment config and candidate fingerprint; enforce startup health, timeouts, cancellation, complete child-process teardown, and scoped cleanup.                                          |
| Acceptance scenarios and reports | `tools/miniapp/run-wechat-devtools-session.mjs` plus the real WEAPP route owners                        | Establish/reset deterministic state, exercise positive and negative journeys, bind observations to candidate/environment/tool/device identity, assert recovery and cleanup, and redact disposable outputs.                                                  |
| API and Provider behavior        | `workers/miniapp-api/**` composition plus `packages/miniapp-contracts/**` and Mini Program client ports | Own request timeout, retry/backoff, idempotency/conflict, Provider degradation, outbox/ledger consistency, cost/quota enforcement, and restart readback. Test fixture adapters are explicitly `SAMPLE_DATA` and cannot impersonate provider-backed success. |
| Representative-device session    | Future device-acceptance runner plus named human/device operator                                        | Prepare the declared device/network/permission states, verify the exact device-lane build, perform lifecycle transitions, record bounded observations, and revoke/clean local credentials and artifacts after the run.                                      |

An app-level error label does not prove the adapter timed out or recovered; a runner exit code does not prove cleanup; an API receipt does not prove the UI handled it. Each owner must expose the furthest independently failing boundary it claims.
