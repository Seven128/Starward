# Screen Contracts: Starward

## Authority Boundary

- `product-surface-contract.md` owns cross-surface responsibility and main/drilldown placement.
- This canonical index and its registered mobile/owner-operations detail Context own stable route identity, semantic order, region/overlay ownership, navigation/state invariants, and the inventory of material Control Keys.
- `DESIGN.md` owns exact authored tokens, the independently scoped native App and WeChat Mini Program system targets, and legacy-target applicability. `screen-contracts/wechat-miniapp.md` binds the Mini Program target to five explicit Product Surfaces and its complete material Control inventory, including the current Map/Search/spot-panel topology; this binding does not transfer exact values out of `DESIGN.md` or turn reference resources into runtime proof. The copied resources under `docs/design-targets/**` remain rollback and semantic-traceability archives only.
- `docs/source-plan.md` remains the native App delivery Source. `docs/wechat-miniapp-v2-1-1-source.md` is the independent Mini Program delivery Source and SHA-indexes the two immutable corrected V2.1.1 proposals, 97-row disposition census and selected-v3 design closure. Their scopes coexist; old V2.0 projection, current code and screenshots cannot redefine either contract.

## Contract Routing

- `project_context/areas/main/screen-contracts/mobile.md` owns the shared mobile Screen Contract and all twelve mobile Surface Keys/83 Control Keys.
- `project_context/areas/main/screen-contracts/wechat-miniapp.md` owns the five Mini Program Surface Keys, route/subpackage model, complete material Control inventory and state/interaction/accessibility invariants.
- `project_context/areas/main/screen-contracts/operations.md` owns the shared owner-operations Screen Contract and both operations Surface Keys/12 Control Keys.
- The three detail Context are registered on-demand in `project_context/context.toml`; this index remains the single stable Screen Contract entry and does not duplicate their platform-specific facts.

## Visual Target Registry And Coverage Boundary

- `target.system.starward-blue-skeuomorphic-2026-07-29`: active system-level authority recorded in `DESIGN.md`; controls the blue planning system, near-black navy night system, strict six-value black/warm-red observation system, disciplined equipment skeuomorphism, typography, spacing, component appearance, state posture, accessibility posture, and motion posture. It does not prescribe screen-specific pixels or prove production/native behavior.
- `target.system.wechat-miniapp-sky-canvas-field-signal-2026-09-02`: active Mini Program visual-system authority recorded in `DESIGN.md`; it owns the independent day/night/observation palette, four-layer whitespace logic, compact type/control hierarchy, semantic component families, motion, accessibility and asset posture. It is bound only to the explicit Mini Program Surface/Control keys in `screen-contracts/wechat-miniapp.md`, never to native App/Admin controls. The superseded `target.system.wechat-miniapp-sky-canvas-2026-08-25` is historical audit provenance only and cannot seed current work.
- `target-miniapp-drift-correction-selected-constraint-v3`: immutable historical implementation handoff at `docs/design-resources/miniapp-selected-handoff-2026-08-22-v3/miniapp-drift-correction-selected-v3.md`. Its semantic traceability remains readable, while its visual styling, exact geometry and fidelity constraints are inactive after the current Field Signal system selection.
- Retired Mini Program screen/interaction resource: `target-miniapp-sky-canvas-current-constraint` is canonically recorded as historical evidence in `screen-contracts/wechat-miniapp.md` and cannot be a current generation, implementation or verification binding. Current Mini Program page/control/state/interaction meaning comes directly from that Screen Contract and its Product Surface under the active Field Signal system. `target-operations-sky-canvas-current-constraint` remains separately current and canonically recorded in `screen-contracts/operations.md`. Historical published handoffs under `docs/design-resources/miniapp-design-system-2026-08-25-sky-canvas/**` prove no pixel or production conformance.
- `target.mobile-product-pages-v2`: immutable rollback archive at `docs/design-targets/mobile-product-pages-v2/index.html`; its historical composition is inactive. Route identity, five-tab meaning, and screen responsibility remain Context-owned.
- `target.mobile-controls-v3`: immutable rollback/semantic archive at `docs/design-targets/mobile-controls-v3/implementation-contract.json`; its 83 stable Control Keys remain traceability identifiers, while its visual values, geometry, and styling are inactive.
- `target.ops-product-pages-v1`: immutable rollback archive at `docs/design-targets/ops-product-pages-v1/index.html`; its historical workspace composition is inactive pending separate regeneration.
- `target.ops-controls-v2`: immutable rollback/semantic archive at `docs/design-targets/ops-controls-v2/implementation-contract.json`; its 12 stable Control Keys retain semantic traceability, not current visual authority or backend proof.
- Nine mobile Control Keys retain downstream native/media proof obligations: `map-filter-sheet`, `map-marker-density-surface`, `selected-spot-sheet`, `spot-media-gallery`, `observation-timeline-editor`, `sky-time-scrubber`, `orientation-follow-toggle`, `orientation-calibration-sheet`, and `ar-mode-toggle`.
- All twelve operations Control Keys retain downstream backend endpoint/authentication/authorization proof obligations. These are implementation/proof work, not design gaps and not permission to simulate success.

## Verification Contract

- Authority integrity: `npm run design:system:verify` must prove the native App adoption and exact DESIGN→runtime token projection, and independently prove the selected Mini Program package membership/digests, canonical source projection, three-mode palette/contrast/observation closure, four-layer whitespace logic, semantic component inventory, provider lineage and App non-inheritance. `npm run design:targets:verify` must separately re-hash the unchanged rollback archives and prove their 14 Surface Keys/95 unique Control Keys remain semantically indexed; it cannot reactivate their visuals or bind them to the Mini Program. `make validate-context` separately protects this Screen Contract and its routing.
- Semantic and state checks: project-owned unit/component/integration tests must observe the committed state and truthful failure/degradation path, not only rendered labels.
- Visual checks: native App work checks its production tokens and mode/material rules against its own DESIGN profile. Mini Program production checks must project only its active DESIGN profile, exercise the current Screen Contract across declared responsive/mode/state/accessibility conditions and use current runtime observations; the retired page resource, any resource integrity inspection and implementation screenshots cannot become current acceptance by themselves.
- Target-runtime checks: Android native runtime is required for current critical mobile paths. iOS implementation remains required, but macOS/Xcode/iOS Simulator and live iOS runtime checks are deferred and must be reported unverified; browser/Expo Web, Android, static structure, and historical results cannot prove iOS. Responsive desktop browser is the product runtime for owner operations.
- Durable paths: stateful and artifact-producing flows must write the declared database/file/native/external sink, recreate the service/app against the same isolated data, read it back, and fail counterfactually when the write/boundary/readback is removed.
- External boundary: representative outdoor/device tuning, licensed production media, expert/legal/store/public-production approval, paid activation and production provider promotion remain explicitly pending unless separately confirmed; only those marked as blocking the current owner-only target may block `target_profile_usable`.
