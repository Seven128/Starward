# Acceptance / Runtime Verification Context: main

This on-demand verification Context owns repeatable authority checks and formal product-runtime evidence boundaries. It states what the listed paths can falsify; passing prose, static shape, or a command alone never establishes product acceptance.

## Owner

- Owning area: main.

## Authority And Runtime Verification Paths

- WeChat Mini Program acceptance is routed to `project_context/development-workflow.md`, the real WEAPP route owners under `apps/wechat-miniapp/**`, and `tools/miniapp/**`. The production root is the Taro weapp project; warm WeChat DevTools is the faithful development lane and an isolated fresh DevTools session is the tool-side current-candidate collector. A browser/H5 projection is forbidden: it is neither a product implementation, design baseline, responsive/accessibility lane nor acceptance proxy.
- A current candidate binds production source, dependency lock, project/base-library configuration, fixtures/data contracts, API contracts, `DESIGN.md`, both corrected V2.1.1 proposals, the complete 97-row selected disposition census, the historical selected-v3 provenance closure, the current `target-miniapp-sky-canvas-current-constraint` Source/handoff/feasibility closure, runner/verifier/launcher code and environment identity. UI implementation must map affected real routes/components/states/modes/viewports to the current closure; historical Open Design QA, handoff preflight, canonical HTML or screenshots cannot prove production conformance.
- Under the current Default Workflow, no historical Long-Task carrier, Receipt or `prepare:miniapp:final-candidate` result is current acceptance evidence. Deterministic checks, infrastructure tests and the isolated native collector must each run against the final unchanged candidate; a controlling-input change invalidates dependent results. Frozen historical Long-Task verifier/spec bytes remain provenance only until a future explicitly selected workflow performs its own protected revision.
- Cross-journey consistency means every named WEAPP surface was observed from one validated candidate-state identity. Native bundle, screenshot, command-output and log hashes remain attributable method artifacts and cannot be substituted for that shared source/build identity.
- Production builds cleanly recompute the Taro main/subpackage graph with persistent webpack caching disabled; watch builds may retain development caching. A compiler exit is insufficient without generated-bundle equality and native runtime observation. DevTools compile-cache commands are not an acceptance prerequisite or a substitute for a clean build because they do not necessarily restart a newly registered Mini Program runtime.
- WeChat actionable buttons that need an explicit accessible name use the existing shared `SoftButton` family, or an owning raw `Button`, with Taro's project-enabled local CompileMode so the production WXML emits a native `aria-label`. Icon-only controls retain a real visible glyph child. Hidden label text, CSS pseudo-element glyphs, or Taro runtime aria props without generated-WXML verification cannot substitute; the WEAPP build and affected native journeys must be rerun after this boundary changes.
- Formal native collection runs only from the direct physical canonical workspace `E:\Dev\Starward` or a direct physical Harness snapshot under one validated `E:\Dev\.starward-tmp\run-<id>` root; aliases, junctions, `SUBST`, copied production roots and canonical-checkout substitution are rejected. Harness snapshot `TEMP/TMP` and DevTools child `TEMP/TMP` are separated. The project-scoped compatibility overlay is pinned to Harness `0.11.0`, changes only exact-root cleanup retries in the current workspace-snapshot and counterfactual-sandbox owners, and fails on unknown package/source shape; it cannot alter assertions, counterfactuals or Final-Gate meaning.
- Formal infrastructure execution must also succeed from that physical snapshot under the Harness-declared minimal environment. `tools/miniapp/docker-compose-runtime.mjs` owns the cross-platform Compose command boundary for both service startup and backup/restore: it uses standalone Compose v2 on Windows so Docker Desktop plugin discovery does not depend on omitted interactive-shell variables, and the standard `docker compose` form elsewhere. Passing the same commands only in the canonical developer shell is insufficient evidence.
- For a new physical snapshot the collector first creates a reversible private-config identity, binds every observed native watcher to the exact candidate, performs a same-byte `project.config.json` write to trigger the tool's supported dynamic reload, requires SDK `3.17.1`, and closes that non-product registration session through `App.exit`/`Tool.close` plus complete process/port/watcher teardown. Formal setup/evidence sessions then run from fresh tool processes. Setup proves canonical DAY/MY reset/readback and the production Settings DAY control; evidence independently resets before journeys; degradation opens only a named fault window after healthy reset. Runtime observations must quiesce before detach.
- DevTools may normalize public project-config formatting. After the evidence IDE, callback/control/automation ports, every `--cli` root and every watcher are closed, the collector may restore exact original public bytes only if canonical JSON proves all values unchanged; semantic change or ownership loss fails without overwrite. It also restores exact prior private bytes/absence, then computes candidate/bundle after-fingerprints. Final evidence is emitted only after those equalities and all cleanup pass. Concurrent formal writers are forbidden, and a prior session JSON or `status: passed` field is not an observation.
- Runtime collection fails on every Mini Program exception, assert and unknown console-error fingerprint. The sole toolchain classification is the exact WeChat automator opaque envelope `{"type":"error","args":[{}]}` with its pinned 28-byte SHA-256 identity; it is retained as a named counted event rather than discarded. Phase-wide, level-wide, message-substring and arbitrary error ignores are forbidden. A DevTools/base-library change must revalidate or remove this exact classifier and cannot broaden it by analogy.

- npm run design:lint invokes the official Google design.md CLI through its Windows-safe `designmd` binary alias and checks the authored visual-system document structure.
- npm run design:system:verify checks both independently scoped system adoptions. For the native App it checks the exact DESIGN→runtime color/type/spacing/radius projection, role equality, six-value observation palette, and rollback classification of older page/control targets. For the WeChat Mini Program it checks selected resource/handoff digests, canonical DESIGN table equality, all eighteen roles across day/night/observation, warm-red closure, required contrast pairs and explicit non-inheritance from the App profile; it does not by itself prove production runtime projection conformance.
- npm run design:targets:verify checks the unchanged rollback-target hashes, 14 Surface Keys, 95 Control Keys, page/control set equality, Source Plan equality, and their continued DESIGN index presence. It protects integrity and semantics only; it is not a current visual-fidelity check.
- npm run test:acceptance:mobile runs the full mobile browser suite with its isolated server lifecycle when a warm development session is not appropriate.
- python C:/Users/777/.codex/skills/.system/skill-creator/scripts/quick_validate.py .codex/skills/uiux_design checks the project interaction Skill structure and metadata.
- npx --yes impeccable detect docs/design-system/brand.html performs an auxiliary scan of the retained legacy export only; it cannot evaluate or constrain the active system.

## Required Preparation

- Network access may be required the first time the Google design.md or Impeccable CLIs are resolved through npx. On Windows, use the `designmd` alias exposed by `npm run design:lint`; invoking `design.md` from the repository root collides with the `DESIGN.md` file association.

## Runtime Acceptance Boundaries

- Primary-navigation acceptance must cold-start the production activity, press all five tabs, and observe canonical route/screen identities `/tonight`, `/map`, `/trips`, `/sky`, and `/me`. It must give at least two tabs distinguishable nested-route and scroll/canvas positions, switch away and back, and prove independent restoration; a shared root `ScrollView`, anchor jump, conditional destination swap, detached deep link, or selected-tab styling alone cannot pass.
- The same navigation check exercises a nested deep link that activates its owning tab, invalid/stale-link recovery to that tab root, route-owned overlay closure, owning-tab stack pop, and native Android Back behavior without synthetic cross-tab history. iOS retains the same contract while its current live runtime evidence remains deferred.
- Formal browser/API acceptance must use a run-unique data root or an explicit deterministic reset and must keep idempotency identity stable when non-semantic freshness fields change. A prior interrupted or completed run must neither conflict with a later isolated run nor silently reuse its domain result.
- Final Mini Program product evidence uses a run-unique PostgreSQL/PostGIS database, Redis namespace and private media root. Any formal spot, contribution, pending upload or plan visible in that evidence is created through authenticated production HTTP/UI owners and read back from those durable stores; `MEMORY_TEST`, development fixture mode, compiled catalog fixtures and screenshot-only state injection are diagnostic-only and cannot qualify the current candidate.

## Expected Signals

- Design lint completes without schema/structure errors.
- System verification confirms DESIGN.md is the sole exact-value authority for two independent profiles. Native App runtime tokens match only the App profile and retain its role-isomorphic six-value observation system. The complete selected-v3 Mini Program 16-resource closure, formal handoff and 97-row disposition census retain their registered digests; the canonical Mini Program section exposes the same eighteen roles in day/night/observation, satisfies its contrast pairs, stays inside black/warm-red observation closure, and contains no App token-source or target dependency.
- Project interaction Skill validation completes without frontmatter, naming, or resource errors; its authority statement keeps DESIGN.md/Source Plan/Context upstream.
- Retained legacy brand, light-kit, and dark-kit HTML files can resolve their local font and supporting artifact paths, but remain inactive visual inputs.
- Every repo-local rollback target retains its registered SHA-256 identity; the mobile 12/83 and operations 2/12 sets remain unique and equal between page coverage and control contracts without becoming current style baselines.
- Every Outcome acceptance run uses at least two materially different inputs through production entry points and proves the result changes with those inputs.
- Stateful and artifact-producing flows prove the applicable database/file/object/native sink write and read it back from a fresh service instance, process, or app restart; in-memory continuity is insufficient.
- Native and external boundaries expose an invocation/result seam so automated tests can substitute an isolated adapter, while the production runtime either invokes the real adapter or presents an explicit unavailable/degraded state.
- Success, failure/timeout/permission denial, duplicate/idempotent retry, and conflict behavior are observed where applicable. Removing the write, adapter call, restart readback, or input dependency must make the owning Outcome Check fail.

## Acceptable Warnings

- Impeccable findings are review signals, not a Tiny Context gate.
- Google design.md may report authored mode/material color roles as unused because its component frontmatter supports only a small single-mode sub-token vocabulary. These warnings are acceptable only when there are zero lint errors and `npm run design:system:verify` proves the native App projection plus the selected Mini Program authority tables, role closure, contrast contract and profile isolation. The check intentionally does not call the Mini Program profile a runtime projection while no runtime exists.
- Static Open Design kits are reference surfaces and cannot by themselves prove production application behavior.
- Automated WeChat DevTools checks can prove only their declared tool/runtime observations; interruption, velocity handoff, haptics, system-gesture competition, outdoor red-light luminance and representative-device frame behavior remain unproven unless the dedicated device lane measures them.
- Representative outdoor/device validation and China production promotion may remain future external gates for the current owner-only personal trial. This postpones only the external evidence; it does not waive production-path integration, local persistence, adapter invocation, honest degradation, emulator tests, or counterfactual machine evidence.
- The current target profile requires Android native runtime evidence. iOS application code, native-project integration, platform adapters, interaction/accessibility behavior, and build readiness remain implementation obligations, while macOS/Xcode/iOS Simulator execution and iOS build/runtime evidence are deferred. This deferral is an explicit unverified state, never a passing result; Android, browser, static, or historical evidence cannot substitute, and a future profile must re-enable the iOS runtime Checks before claiming iOS usability.
