# Acceptance / Runtime Verification Context: main

This on-demand verification Context owns repeatable authority checks and formal product-runtime evidence boundaries. It states what the listed paths can falsify; passing prose, static shape, or a command alone never establishes product acceptance.

## Owner

- Owning area: main.

## Authority And Runtime Verification Paths

- WeChat Mini Program acceptance is routed to `project_context/development-workflow.md`, the real WEAPP route owners under `apps/wechat-miniapp/**`, and `tools/miniapp/**`. The production root is the Taro weapp project; warm WeChat DevTools is the faithful development lane and an isolated fresh DevTools session is the tool-side current-candidate collector. A browser/H5 projection is forbidden: it is neither a product implementation, design baseline, responsive/accessibility lane nor acceptance proxy.
- Current evidence identifies the production source, dependency lock, runtime configuration, data/API contracts and relevant verification code. Use current DESIGN.md and Screen Contracts for UI requirements; prototype packages and historical resource digests are not acceptance prerequisites. Verify all current Mini Program routes from app.config.ts, including their controls and failure states.
- Historical Long-Task carriers and Receipts are not current acceptance evidence. Deterministic checks, infrastructure tests and the isolated native collector run against the final unchanged candidate; relevant input changes invalidate dependent results. Retained verifier/spec files are standalone project diagnostics and ordinary provenance, with no protected revision or future Harness workflow prerequisite.
- Cross-journey consistency means every named WEAPP surface was observed from one validated candidate-state identity. Native bundle, screenshot, command-output and log hashes remain attributable method artifacts and cannot be substituted for that shared source/build identity.
- Production builds cleanly recompute the Taro main/subpackage graph with persistent webpack caching disabled; watch builds may retain development caching. A compiler exit is insufficient without generated-bundle equality and native runtime observation. DevTools compile-cache commands are not an acceptance prerequisite or a substitute for a clean build because they do not necessarily restart a newly registered Mini Program runtime.
- WeChat actionable buttons that need an explicit accessible name use the existing shared `SoftButton` family or an owning raw `Button`, and their production WXML plus affected native journeys must verify the emitted accessible name. Icon-only controls retain a real visible glyph child. Hidden label text, CSS pseudo-element glyphs, or unverified Taro runtime props cannot substitute.
- Starward keeps Taro 4.2.1 experimental CompileMode disabled project-wide. Adjacent successful builds produced different template-prefix assignments and an Android TabBar-only blank application body while the same prepared bytes rendered elsewhere; generation-level hashing did not detect that renderability defect. Clean production builds and physical-feedback snapshots reject `*-templates.wxml`. Reassess only after a pinned Taro/runtime upgrade passes deterministic output comparison and representative Android WEAPP rendering, including the native Map page and shared controls.
- Mini Program calendar serialization must not require `Intl.DateTimeFormat.prototype.formatToParts` on a supported phone. Normal runtimes retain IANA Gregorian parts, including historical Shanghai and arbitrary-zone DST semantics. If `Intl` construction or `formatToParts` is unavailable, only the current spot-contract zones `Asia/Shanghai` and `Asia/Hong_Kong` may use their exact modern UTC+8 rule within 2000–2100; historical dates and every other zone still fail closed. Initial Map/Search timezone detection defaults to Shanghai only when runtime detection is unavailable, and presentation-only weekday labels use UTC calendar arithmetic. Reassess this bounded fallback if supported spot zones or observation-year coverage expand.
- Current Mini Program UI acceptance must prove from one cold production candidate that small/medium/large retain the same spot-document node identities and order while only viewport extent changes; valid media reveals before near-top Map chrome fades and no-media retains only the compact handle band; only the named handle rectangle initiates extent drag; `bottomPresentation` never exposes panel and layer simultaneously and layer→marker retargets directly to the new medium panel; Search text/rect remain unchanged with compact suggestions; the arrowless ruler moves under real horizontal drag and snaps to a real slice; and the single Settings mode owner traverses day↔night↔observation by tap, drag and accessible selection without an observation color leak. Static HTML markers or screenshots alone cannot prove these interaction/state invariants.
- Contribution UI acceptance must enter the same production form from both a formal-spot panel and My, observe the supplied-context difference without a second form owner, and prove one keyboard-safe scroll document, compact row/top-label reflow at 320px with standard text, no page horizontal scroll or visible scrollbar chrome, conditional new-place location, inline first-invalid focus, bounded media progress/failure/retry with draft and media identity preserved, duplicate-submit prevention, the same idempotency identity across retry and one pending receipt read back after restart. A design candidate, component-library resemblance, local-only state or screenshots cannot establish these runtime invariants.
- Current UI acceptance covers the selected bounded Taro/Starward substrate, one DESIGN projection in day/night/observation, 320–430px with standard text (large-text adaptation is currently paused by the user), IME/safe-area behavior, screen-reader names/states, reduced motion, overlay scroll locking and panel handle/nested-scroll gestures. If a future compatible UI dependency is adopted, bind its reviewed version in the lockfile, verify license/peer compatibility and imported entrypoints, measure WEAPP bundle delta, and exclude unused suites and competing icons. `@taroify/core@1.0.6` remains rejected by the current selection. H5 docs, package metadata, compilation or resemblance cannot establish WEAPP compatibility; any realization must retain the same Control owner, exact design and state store.
- Formal native collection uses the direct physical canonical workspace `D:\dev\Starward`; aliases, junctions, `SUBST` and canonical-checkout substitution are rejected. DevTools child TEMP/TMP remains separately owned. The retired Harness snapshot/compatibility wrapper is not an execution path; project runners retain exact candidate fingerprinting and owned-process cleanup.
- Formal infrastructure execution must also succeed under the project runner's minimal declared environment, independently of interactive-shell variables; a retired Harness snapshot is neither required nor admitted. `tools/miniapp/docker-compose-runtime.mjs` owns the cross-platform Compose command boundary for both service startup and backup/restore: it uses standalone Compose v2 on Windows so Docker Desktop plugin discovery does not depend on omitted interactive-shell variables, and the standard `docker compose` form elsewhere. Passing only in the developer's full shell environment is insufficient evidence.
- The collector validates its canonical physical workspace and separate physical child TEMP/TMP before allocating run resources or starting infrastructure/build/tool processes. It creates a reversible private-config identity, binds native project watchers to the exact candidate under the strict project/internal watcher classification owned by `project_context/development-workflow/candidate-acceptance.md`, performs a same-byte `project.config.json` write to trigger supported dynamic reload, and requires SDK `3.17.1` in fresh setup/evidence sessions. Setup proves canonical DAY/MY reset/readback and the production Settings DAY control; evidence independently resets before journeys; degradation opens only a named fault window after healthy reset. Runtime observations must quiesce before detach and every session retains complete process/port/watcher teardown. There is no separate snapshot-registration phase.
- DevTools may normalize public project-config formatting. After the evidence IDE, callback/control/automation ports, every `--cli` root and every watcher are closed, the collector may restore exact original public bytes only if canonical JSON proves all values unchanged; semantic change or ownership loss fails without overwrite. It also restores exact prior private bytes/absence, then computes candidate/bundle after-fingerprints. Final evidence is emitted only after those equalities and all cleanup pass. Concurrent formal writers are forbidden, and a prior session JSON or `status: passed` field is not an observation.
- Runtime collection fails on every Mini Program exception, assert and unknown console-error fingerprint. The sole toolchain classification is the exact WeChat automator opaque envelope `{"type":"error","args":[{}]}` with its pinned 28-byte SHA-256 identity; it is retained as a named counted event rather than discarded. Phase-wide, level-wide, message-substring and arbitrary error ignores are forbidden. A DevTools/base-library change must revalidate or remove this exact classifier and cannot broaden it by analogy.

- npm run design:lint invokes the official Google design.md CLI through its Windows-safe `designmd` binary alias and checks the authored visual-system document structure.
- npm run design:system:verify checks current App tokens against the App profile and generated Mini Program tokens against its independent profile, including contrast and observation palette constraints. It does not prove layout or runtime behavior.
- `design:targets:verify` is a legacy diagnostic for frozen native/operations prototype packages. It is not required for current UI development or acceptance; use the owning Screen Contract and production tests for current page/control coverage. Do not update prototype hashes to satisfy it.
- npm run test:acceptance:mobile runs the full mobile browser suite with its isolated server lifecycle when a warm development session is not appropriate.
- python C:/Users/777/.codex/skills/.system/skill-creator/scripts/quick_validate.py .codex/skills/uiux_design checks the project interaction Skill structure and metadata.

## Required Preparation

- Network access may be required the first time the Google design.md or Impeccable CLIs are resolved through npx. On Windows, use the `designmd` alias exposed by `npm run design:lint`; invoking `design.md` from the repository root collides with the `DESIGN.md` file association.

## Runtime Acceptance Boundaries

- Primary-navigation acceptance must cold-start the production activity, press all five tabs, and observe canonical route/screen identities `/tonight`, `/map`, `/trips`, `/sky`, and `/me`. It must give at least two tabs distinguishable nested-route and scroll/canvas positions, switch away and back, and prove independent restoration; a shared root `ScrollView`, anchor jump, conditional destination swap, detached deep link, or selected-tab styling alone cannot pass.
- The same navigation check exercises a nested deep link that activates its owning tab, invalid/stale-link recovery to that tab root, route-owned overlay closure, owning-tab stack pop, and native Android Back behavior without synthetic cross-tab history. iOS retains the same contract while its current live runtime evidence remains deferred.
- Formal browser/API acceptance must use a run-unique data root or an explicit deterministic reset and must keep idempotency identity stable when non-semantic freshness fields change. A prior interrupted or completed run must neither conflict with a later isolated run nor silently reuse its domain result.
- Final Mini Program product evidence uses a run-unique PostgreSQL/PostGIS database, Redis namespace and private media root. Any formal spot, contribution, pending upload or plan visible in that evidence is created through authenticated production HTTP/UI owners and read back from those durable stores; `MEMORY_TEST`, development fixture mode, compiled catalog fixtures and screenshot-only state injection are diagnostic-only and cannot qualify the current candidate.

## Expected Signals

- Design lint completes without schema/structure errors.
- System verification confirms the two independent DESIGN.md profiles match production tokens. Actual WEAPP observations establish layout, interaction and state behavior; a resource inventory or hash cannot establish those results.
- Project interaction Skill validation completes without frontmatter, naming, or resource errors; its authority statement keeps DESIGN.md/Source Plan/Context upstream.
- Every Outcome acceptance run uses at least two materially different inputs through production entry points and proves the result changes with those inputs.
- Stateful and artifact-producing flows prove the applicable database/file/object/native sink write and read it back from a fresh service instance, process, or app restart; in-memory continuity is insufficient.
- Native and external boundaries expose an invocation/result seam so automated tests can substitute an isolated adapter, while the production runtime either invokes the real adapter or presents an explicit unavailable/degraded state.
- Success, failure/timeout/permission denial, duplicate/idempotent retry, and conflict behavior are observed where applicable. Removing the write, adapter call, restart readback, or input dependency must make the owning Outcome Check fail.

## Acceptable Warnings

- Impeccable findings are review signals, not a Tiny Context gate.
- Design-lint vocabulary limitations may produce warnings for authored mode/material roles; investigate errors and use current token consistency and contrast checks for the supported profiles.
- Automated WeChat DevTools checks can prove only their declared tool/runtime observations; interruption, velocity handoff, haptics, system-gesture competition, outdoor red-light luminance and representative-device frame behavior remain unproven unless the dedicated device lane measures them.
- Representative outdoor/device validation and China production promotion may remain future external gates for the current owner-only personal trial. This postpones only the external evidence; it does not waive production-path integration, local persistence, adapter invocation, honest degradation, emulator tests, or counterfactual machine evidence.
- The current target profile requires Android native runtime evidence. iOS application code, native-project integration, platform adapters, interaction/accessibility behavior, and build readiness remain implementation obligations, while macOS/Xcode/iOS Simulator execution and iOS build/runtime evidence are deferred. This deferral is an explicit unverified state, never a passing result; Android, browser, static, or historical evidence cannot substitute, and a future profile must re-enable the iOS runtime Checks before claiming iOS usability.
