# WeChat Mini Program Spot Information And Sky Invariants

This on-demand subdomain node normatively continues `Cross-Control And State Invariants` for continuous spot information, astronomy evidence, full-sky orientation and the source-lift focus family.

## 云观星采用资源

<!-- ty-context-controlling-source domain="design" path="docs/design-resources/wechat-miniapp/sky/ADOPTED.md" -->

日期与时间尺资源沿用[公共观测时间 owner](shared-state-and-recovery.md#shared-observation-time-resource)及其当前引用；本页不重复声明共享资源。

2026-09-09采用[云观星唯一入口](../../../../../docs/design-resources/wechat-miniapp/sky/ADOPTED.md)。默认轻标签，选中目标的信息卡参考同包焦点态；不是新的方向操作或模式开关。天空占满视口，时间/日期沿用公共组件，跨午夜同步；天体详情仅由点选/列表显式打开。方向未授权、校准、过期/不可用不伪装当前姿态；真实跟随仍由既有平台传感器owner负责。返回所进入的观星点及原上下文，不固定回我的。示例坐标/姿态/天体测量fixture和Web样本不是生产数据或真机精度证明。

## Cross-Control And State Invariants

- `sky-orientation-canvas` is a pointing window into the sky, not a whole-hemisphere planisphere. For the inherited observing coordinates and committed time, real star and target directions use one three-dimensional view transform and one explicitly defined angular field of view with the actual canvas aspect ratio. The phone's back-facing direction determines the view center; pitch, yaw and roll change that view basis rather than translating a compressed circular sky. Angular separation, handedness and aspect ratio must remain geometrically consistent; off-frustum and behind-view objects are not clamped into apparent on-screen star positions. Owner correction on 2026-09-06 confines star charts to 云观星; the spot information panel contains no static or immersive star chart.
  On-site comparison with the naked-eye sky is meaningful only when the observing coordinates/time match the observer's location/time. Remote cloud observation continues to use the selected formal spot or authorized pending proposal and shared time; this requirement does not silently switch to phone GPS or introduce a second location/time owner. Exact apparent 1:1 angular size depends on physical screen size and viewing distance and cannot be claimed from logical pixels alone. A declared field of view and measured pointing accuracy are separate requirements: sensor reference frame, compass north reference, calibration, sample freshness and platform differences must be established rather than hidden by visual offsets. Geometry tests cover cardinal directions, pitch/roll, view boundaries and viewport aspect ratios; real-device comparison must establish pointing correspondence and disclose remaining uncertainty. Simulated pose, a visually pleasing screenshot or catalog presence alone does not establish alignment. This is a rendered star chart and does not imply camera AR permission or imagery.

- `map-spot-information-panel` is the sole current owner of formal spot basic information and astronomy information; private draft/pending views reuse its document under the Map identity/capability contract. Its large-state document order is stable: valid licensed media when present → identity → objective route/access/facility/safety evidence → guides and field/source actions → visible 天文 chapter heading outside and immediately above the first date/time card → date/time card with borderless curved ruler → lunar module → grouped weather → light-pollution/target/professional/source facts. Missing/invalid/unlicensed media omits the region entirely and changes no later semantic order. The handle hint overlays the compact white identity-header band inside this same document; neither becomes viewport-fixed. `基本信息` and `天文信息` are section labels and navigation targets in one scroll owner, not tabs, separately mounted panels or standalone routes. Current Mini Program presentation does not render `TripDecision`, “谨慎出发”, recommendation language, primary/backup recommended windows or a best-opportunity rail. Content that is unavailable remains in truthful loading/empty/partial/stale/error structure; it is never filled with representative values, and generic status copy is suppressed unless its concrete effect changes a displayed fact, action or recovery.
- `map-spot-panel-section-nav` is one-level, compact, left-aligned text navigation for 基本信息/天文 in the same continuous document. It is hidden at the basic-information start, appears when the astronomy boundary reaches the reading top, stays sticky while needed, and hides on returning to the start. A short rounded gradient underline moves between labels; no icons, filled segmented background or equal-width expansion. Scroll and activation share one section owner without remounting or shifting document geometry. Activating an available chapter aligns its heading in the panel scroll owner, with interruptible movement and logical focus after assistive activation; reduced motion skips travel. It never changes outer Map scroll, camera, selected spot or route/hash.
- `TripDecision` and `SkyOpportunity` remain server-owned domain outputs and may continue to serve other accepted consumers, replay and safety boundaries. The current Mini Program information panel does not consume them as a recommendation/conclusion surface. It may display the underlying Source-backed objective measurements, formal alerts, access/open/road/legal facts and individual celestial event facts without combining them into travel advice. The client cannot recreate the hidden recommendation logic, infer a recommended interval from the ruler or present an all-night average as a selected-time fact.
- Astronomy weather evidence consumes the BFF's single normalized composition. The displayed weather facts use the declared primary timeline and identifies an Open-Meteo substitution only when that fallback materially changes interpretation; it never merges models by selecting the clearest result. `sky-professional-matrix` exposes total/low/mid/high cloud, applicable model-consistency and provider validity without converting missing fields to zero. Active material official alerts are visible hard blockers. If the alert feed is unavailable or expired, sky-only evidence may remain readable, while the missing safety input remains explicit and cannot relax the existing action checks; no combined trip conclusion is shown. Provider/model names, run/valid time, limitations and contributors to a fused value remain reachable through `data-source-disclosure`; they do not become a permanent “部分数据” badge, implementation sentence, debug ID or user-visible product-version copy.
- Owner revisions through 2026-09-08 supersede the old production expression of `spot-favorite-action`. Its label remains `想去`, with one rounded five-point main star, outline when inactive. Activation immediately starts exactly one clockwise full revolution with slight scale reduction and fill transition. Exactly two smaller subordinate meteors travel in from upper-left toward lower-right while fading from transparent to visible; their entry begins with the action, independently of day/night background timing. The dominant main star has a yellow-green fireball tail attached behind it, tapering upward-left to express downward-right travel; the two smaller meteors remain subordinate. Star and tail share one coordinate system, with a single interruptible motion owner. The icon/label gap matches sharing and cloud-stargazing actions, and the upright main star aligns with the label; subordinate artwork extends away from the text without clipping, and smaller star heads remain clear of the main meteor trail. The default compact rounded blue-sky/white-cloud background crossfades in place to a lighter muted night sky. Cancellation takes over the live presentation, reverses the same single revolution counterclockwise from its live progress, moves and fades subordinate meteors/tails back out, and restores the outline/scale without queuing. Exact adopted geometry, color and timing live in the three-extent resource reached through the Screen Contract’s sole Map adoption entry.
  The effect is causal and non-looping, with no autonomous twinkle or repeated burst. Reversal takes over the live presentation without jumping, replaying a queued animation or changing layout. Accessible pressed/selected/busy state and a full44px target remain independent of decoration. Reduced motion preserves the selected outline/fill distinction without spinning or satellite travel; Observation retains geometry but maps every star/tail to closed warm-red roles. Favorite success emits no floating acknowledgement; failed commits restore the authoritative relation with adjacent recovery. Search `想去` and panel favorite state remain one relation.
- `spot-navigation-action` is one quiet, visually centered, right-facing icon-only route handoff in the basic-information section, with a programmatic navigation name. It performs the existing safety/blocker check before external navigation and is not duplicated inside facility cards or the lower action bar. The identity area carries no generic “地点资料可用”/“今晚条件会变化” chips or derived departure recommendation; meaningful evidence belongs to objective freshness, access, route, facility and risk content. `spot-share-action` shares only an allowed formal-spot summary after explicit activation. The visible `我要反馈` button uses `spot-contribution-entry` to open the existing contribution route with the formal spot identity; it never implies instant canonical publication.

## Basic Information Composition

- Record existence, field coverage and transport availability are separate. A missing/non-public formal record does not create a formal-spot panel; authorized draft/pending records have their own typed private panel state under the Map identity contract. A known formal record with partial details still displays its usable identity and facts; loading/error/stale weather or detail requests do not mean that the spot does not exist. Confirmed absence of a facility is not unknown availability. Design-only resources may use clearly declared, isolated synthetic scenarios to show realistic content density; these values never enter production fallbacks or formal records.
- `基本信息` is the visible basic-information chapter label (formerly `概览`). It still locates the same continuous document; this rename does not introduce another route or change section identity. The current input owners are `SpotSummary`, `SpotDetail`, `RouteOverview`, `FacilityEvidence`, `AccessAndSafetyState`, `FactEvidence`, `RepresentativeMedia` and `SourceSummary` in `packages/miniapp-contracts/src/types.ts`; an all-missing design example must not narrow that product contract.
- The section accommodates place identity/address and relevant terrain/access metadata, route estimates versus straight-line distance, walking/last-road/parking guidance, opening hours, facility availability and conditions, legal access/restrictions, night safety and material evidence. Actual data controls optional content; ordinary missing values, loading, failure, stale, closed/prohibited and explicit danger retain distinct meaning. Serious restrictions and hazards stay readable at this layer and affect actions; they cannot be hidden behind more-details or inferred safe from missing data.
- Parking and toilets are two full-width cards stacked vertically, each owning its status and any available useful description, distance, hours or usage condition. They never form a side-by-side pair. Opening hours remain a plain text row; other facility types continue to use their appropriate concise factual expression. Card expansion does not enlarge the shared font system or duplicate route navigation.
- Available, authorized facility imagery belongs to that specific spot and facility, not the first generic site image. Its thumbnail forms the facility card background with restrained image-only blur and a locally protective readability overlay; text and icons remain sharp, while a meaningful area of the photo stays clearly recognizable instead of being washed almost white. Activating the image-backed card opens the corresponding clear original/gallery. The card may contain multiple photos and uses the shared image viewer in [Shared state and recovery](shared-state-and-recovery.md#shared-image-viewer-and-disclosure), including horizontal paging and downward scale-to-dismiss. The clear image expands spatially from the activated card crop, with image blur/readability wash fading away; closing contracts to that same card. Opening, reversal and close take over the live presentation, including interruption before decode/settle; preserve a stable source placeholder, prevent underlying scroll, support Back/Escape and restore trigger focus. Reduced motion substitutes a short fade without geometric enlargement. Invalid/decode-failed media retains a truthful recoverable state. Closing returns to the same spot, extent and scroll position; missing/invalid media retains factual content on a plain surface and has no false image action. Reuse `RepresentativeMedia` provenance/rights and existing media review; the current `FacilityEvidence` and contribution upload contracts require an explicit facility association when implemented.
- Show a readable source sentence near its facts, using real provider/contributor and verification/update information when available. A shared line is valid only for facts with the same provenance; distinct facility/route/weather sources remain individually traceable. Basemap attribution is not the source of all site facts. Missing provenance is stated honestly, without invented names or dates. Full provenance may have a clearly named details entry; the basic source sentence is not three vague links or an inert `href="#"`.
- `场地资料` was the deeper site-evidence route, not a separate source provider. Surface it as `更多场地信息` only when there are additional facility/road/access/safety details beyond the summary; preserve this route rather than adding another information hub. Real guides remain optional supporting content with author/source and safe return. `我要反馈` is a distinct operable lightweight button, routed to the existing contribution owner with spot context, authorization and provisional-record rules.
- Licensed, applicable, available site media precedes identity in the document and is visible only toward large. A single image uses a wide crop; multiple images form one horizontal strip with a visible continuation cue and accurate count, opening the shared viewer at the tapped index. A media list is not a nested Tab or separate sheet. The white identity/drag band follows this media and scrolls with the document. Small/medium consume no media height; no-media consumes no placeholder. Preserve the continuous media height/opacity reveal before near-top map-chrome fade and its reverse sequence. `RepresentativeMedia` attribution/rights/identity remain required; a design-only illustrative photo is not a verified site image.
- `云观星` and active `想去` share a muted night-sky background family, with different sparse star positions. Star brightness continuously and gently varies at staggered rhythms within these buttons; the foreground favorite ritual remains a single reversible action, not a looping meteor. This explicit owner-approved decorative exception is not astronomical data, and changes neither navigation nor spot/Observation Context validity. All three actions have background surfaces and no outer borders; sharing uses a quiet complementary color owned by DESIGN. Decorative animation pauses offscreen/background and becomes static under reduced motion; Observation uses the closed warm-red palette.

## Astronomy Evidence Composition

- Base the panel on the selected `SkyReport.hourly` row, matching `targetFrames`, spot light-pollution evidence and weather/source owners. Each complete information module occupies one full-width rounded neutral card; related groups inside use aligned values, spacing, optional dividers and one semantic group icon, without per-metric icons, colored subcards or nested tabs. Model consistency belongs beside forecast provenance, not precipitation values. Cloud layers share aligned columns; temperature/humidity/dew point, wind/gust/direction, precipitation/probability/visibility and lunar facts form related compact groups. Targets are name/direction/altitude objects for that slice; do not substitute `suitableFor` recommendations. Material alerts and their recovery remain readable at the affected layer. Optional professional provenance can expand without duplicating the time owner or making every scalar a full-width card/row.
- Preserve measurement meaning: `visibilityKm` is visibility, not a measured transparency value; current public `HourlySkyRow`/`SkyReport` has no independent transparency/seeing measurement and has not yet exposed structured lunar phase/rise-set events. Lunar phase and rise-set are now required product additions described below; this current implementation gap must not be reinterpreted as a permanent product exclusion. Required future capabilities need attributed BFF outputs rather than invented client values. `LightPollutionEstimate` is a versioned satellite night-light estimate with `calibratedSkyClass:false`, not exact Bortle or field SQM. Missing these capabilities must not erase other usable weather, lunar or target facts.
- `sky-time-scrubber`, `sky-professional-matrix` and `sky-target-list` share the one formal spot/time context with Map and full-sky orientation. Every astronomy value is attributable to that context and keeps source, validity/update time and partial/stale/unavailable meaning reachable without repeating it as always-visible noise. Tier-A semantic icons improve scanning for twilight, cloud, moon, rain, wind, sources and targets, while aligned tracks/matrices remain value owners and no metric becomes an isolated decorative card or recommendation rail.
- The shared `curved-time-ruler` component family backs `map-time-control`, `sky-time-scrubber` and `sky-orientation-time-ruler`. It renders only real discrete timeline slices and is raised closer to the facts or sky projection it changes. The selected tick is fixed on the center axis, largest, highest-contrast and fully labelled; tick height, scale, opacity and vertical offset diminish symmetrically toward both edges to form a shallow arc. Horizontal drag must move the actual ruler beneath the fixed center, preview a real slice per frame, and release snap/commit one valid slice; cancellation returns to the committed slice. The visual component exposes no left/right arrows, border, card background or shadow.
  The implementation boundary reuses the Taro/WEAPP enhanced horizontal `ScrollView` lifecycle, hidden-scrollbar capability and scroll-end events; the project-local layer owns curved tick rendering, center projection and valid-slice snap. Generic Picker/DateTimePicker/Slider controls and React-Native-only ruler packages are not equivalent owners. Programmatic role, min/max/current and discrete step are exposed; keyboard/assistive increment and decrement move one real slice without visible arrow chrome. Reduced motion removes inertial or spring settle but retains direct tracking and a short state swap. Horizontal ruler input wins over panel vertical dragging after directional intent crosses the gesture threshold; diagonal uncertainty holds until one owner wins, then never transfers mid-gesture.
- `sky/detail` makes `sky-orientation-canvas` the full available viewport from top to bottom safe area. One quiet leading Back action, object labels and `sky-orientation-time-ruler` overlay the canvas. There is no boxed title/spot-time header or top target/location action; inherited typed place/time remain the single context and are expressed lightly near the existing date/time region, not in top chrome. Ordinary sensor-follow success remains in programmatic state and accessible naming, not a persistent “方向跟随中” label. Only permission, calibration, low-accuracy, stale or unavailable states that change use become concise visible feedback. The background star field is not an image or generated ornament: it consumes the BFF's deterministic, magnitude-limited projection of a versioned and checksum-bound real astronomical catalog for the same authorized observing location and each real time-ruler slice. Catalog stars are rendered by measured brightness/color data without persistent labels or recommendation semantics. Visible `SkyTarget` objects retain necessary direction/altitude/state. Explicit catalog-object selection for information follows the lazy detail contract below; it does not turn every background star into a recommendation target. Missing, corrupt or unavailable catalog/scene data never falls back to a static picture, random stars or representative coordinates, and the remaining target list and professional evidence retain their own truthful availability.
- `sky-orientation-sensor` is sensor-follow-only. After explicit permission it consumes foreground platform device pose (`alpha`, `beta`, `gamma`) and, when absolute direction is required, the existing compass owner; it exposes calibration/accuracy and updates view orientation continuously. Hiding, backgrounding or leaving stops listeners, and pose trajectories are never persisted. It has no manual toggle, stepper, direction drag or separately editable heading. Before permission, `sky-orientation-recovery` uses one concise stable-geometry panel with a compass/orientation icon, one friendly title, one short privacy/usage sentence, one compact primary action and a quiet defer route. Denied, calibrating, low-accuracy, stale and unavailable reuse the geometry and change only necessary copy/action while leaving the sky dominant.
  When the stream is denied, unavailable, stale or below accepted quality, the canvas stops claiming a current pose. `sky-orientation-object-list` remains only as a screen-reader semantic equivalent, user-opened compact disclosure or degraded fallback; it is not a persistent section below a reduced sky. The client never synthesizes motion or silently falls back to manual input. The curved time ruler overlays the lower sky using Design Authority surfaces; reduced transparency makes that surface opaque, and reduced motion removes interpolation/settling while preserving direct pose and time feedback.
- `source-lift-focus-layer` remains a reusable family for other accepted products but has no current Mini Program surface owner. The former Map `观测条件` use is retired: its selected local time/layer/objective value now live inside `map-layer-selector`, so there is no lifted source card, geometry-preserving placeholder, companion map dock or second close path in this route.
- The Map TOTAL_CLOUD layer and panel astronomy use the same shared observation-date/time component, including its calendar entry, previous/next available local date and return-to-tonight actions. Full-sky orientation reuses the same ruler core and committed Observation Context. This is one implementation in the Mini Program shared components layer, not separate page-specific copies; the component receives real availability and callbacks from its owning consumer and cannot import a feature/page or create another time store. Date semantics are owned by the Lunar Facts And Date Selection section below.

- Place information grouping, compact metric layouts and content-dependent reflow follow [Information design](information-design.md).


- Supporting field/guides/source routes display their own evidence directly. They do not mount another overview/guides/site tab hub; the Map panel owns the place overview and its 基本信息/天文 section navigation. Retain selected spot/context, source traceability and safe return to the Map panel.

## Lunar Facts And Date Selection

- Owner direction 2026-09-08 adds a lunar module to astronomy: a semantic phase picture/name, illumination at the selected instant, current lunar altitude and separately labelled moonrise and moonset events carrying local dates/times. Eight reusable symbolic states are new, waxing crescent, first quarter, waxing gibbous, full, waning gibbous, last quarter and waning crescent. They summarize a continuous phase; illumination alone cannot distinguish waxing from waning. BFF astronomy supplies phase angle/direction and illumination for each actual sample; the client only maps approved phase assets, never fabricates a lunar cycle from the date or cycles icons for decoration. Symbol orientation is not a live sky-orientation claim.
- In panel astronomy, every smallest selectable time-ruler tick carries its corresponding lunar phase icon below its time label. The Map TOTAL_CLOUD consumer uses the same date/time component without the lunar-icon slot or an empty placeholder; all remaining typography, tick/curve/center-axis geometry, date controls and interaction semantics remain shared. Time typography stays regular/light with selected emphasis through contrast and modest size rather than heavy bold. The date-selection cluster is centered on the same axis as the ruler, with return-to-tonight not shifting that center; nearby ticks may legitimately repeat. Missing phase in the astronomy consumer gets an explicit unknown equivalent, not an invented moon. Moonrise/set are event instants with observer coordinates, timezone, calculation provenance and search interval; no event in the interval is different from data failure. Cross-midnight events show their date/次日. Do not turn a 1.5-day search result into a same-calendar-day event or infer crescent direction from illumination alone.
- Date and time remain one Observation Context owner. Product target is local today −7 through +15 observation nights inclusive (23 dates), bounded by actual supported date/sample coverage. A compact date label with calendar entry supports direct selection, previous/next available local date and return to tonight; this is a date input, not a second tab rail. The calendar distinguishes selected, today and unavailable dates. Preserve the selected local clock time when valid after changing date; otherwise select and announce an available sample, never silently borrow stale frames. Changing date updates lunar/weather/targets/source together and cancels older pending responses. Failed requests retain explicit failure/recovery without relabelling old values as the new date.
- The existing night owner uses local noon → next local noon; the domain retains that starting-night key, while the visible date/calendar highlight follows the selected sample’s actual local calendar date, advancing and reversing at midnight without changing its sample or rebuilding the current night track. Past nights are historical/model weather where available, not real-time observations; computed astronomy remains its own evidence. Product −7/+15 target is not proof of production availability or permission to increase service spend. Date availability and individual field/model coverage must come from the BFF/runtime capability, with honest per-field absence and provenance rather than disabling useful astronomy whenever weather is missing.
- Current code gap: runtime-config accepts only24/72-hour QWeather primary windows; Open-Meteo requests the chosen local date through its next date and is already a declared evidence/fallback owner. Public service capacity supports through future calendar day15 (including today in its16-day maximum), with possible missing weather after midnight in that final observation night, but the current app has no complete date-capability/calendar/lunar event contract. Extend existing astronomy-core/AstronomyService/miniapp-contracts owners in implementation; no parallel service or client ephemeris. The adapter already calculates moonRise/moonSet internally; normalize event intervals and expose them rather than duplicating calculations in the Mini Program.

Sources: [NASA lunar phases](https://science.nasa.gov/moon/moon-phases/), [Astronomy Engine](https://github.com/cosinekitty/astronomy/blob/master/source/js/README.md), [QWeather hourly](https://dev.qweather.com/docs/api/weather/weather-hourly-forecast/), [Open-Meteo coverage](https://open-meteo.com/en/docs). Service capacities and deployed entitlements are distinct; exact current limits remain with runtime configuration/provider response.

Owner follow-up selects −7/+15 nights rather than +20: ephemeris computation can extend further, but the current weather providers do not support a uniform20-day forecast. Keep final-night cross-midnight gaps explicit; do not extend weather by copying the last available forecast.

The shared date/calendar is a civil-date input: choosing September 9 while the clock is 00:30 selects September 9 00:30 from the September 8 night record, never September 10. Available calendar dates derive from actual sample dates (including cross-midnight portions); absent clock samples select and announce a genuinely available time. The footer may name the starting observation night to explain cross-midnight grouping. Date, weekday, calendar highlight and accessible full timestamp update together during preview, commit and cancel.


审核中提案补充：Map“新增观星点”合同新增当前提交者可见的 pending proposal 地图/信息组件状态。此输入以 proposal_id 和已提交坐标传递给同一信息/云观星职责，不能伪造 formal spot_id。原正式点限定在此明确范围内扩展；审核中隐藏想去与分享，天空事实仍需真实可用数据和权限验证，缺失不借用示例。新增表单构图和取消到访时间由 Map add-spot 采用包覆盖，正式点反馈按 [Shared state](shared-state-and-recovery.md) 的正式资料编辑合同回填、对比并冻结。

正式点本人有审核中反馈时：标题旁“我的反馈”tag 打开冻结反馈，组件右上时钟图标标识审核中；正式字段和想去/分享仍读取正式点。草稿点没有“我要反馈”，通过编辑动作进入“编辑观星点”；新增审核中提案仍隐藏想去/分享。这三种身份和本人反馈状态不得混用。

## Selected location and truthful sky coverage

Cloud stargazing displays the selected location’s name near the existing date/time region, with an accessible full location/timestamp and timezone. Keep the full-screen canvas, light labels and existing date control; do not restore a boxed/top title or new mode toggle. This context is always the selected place/time, not a claim to match the device’s current physical location. Showing it requires no new location request and must not depend on detecting remote viewing. Pending proposal identity remains visible as pending and follows Map publication/authorization transitions.

Production sky usability requires recognizable important bright-star coverage in the actual versioned catalog and BFF frames, not just checksum/count and synthetic astrometric fixtures. Sirius, Vega and Polaris are minimum named regression examples at appropriate location/time/visibility; they are not the complete promised star list. Distinguish bright-source omissions from capacity-limited fainter-star selection; G<=5.5 in a TOP-limited query is not completeness to that magnitude. Missing key bright references cannot be resolved by disclaimer alone. The architecture owner defines provenance/deduplication and unified-pack boundaries; exact acquisition/capacity implementation remains unimplemented by this Context revision. Weather, terrain obstruction and theoretical horizon visibility remain distinct.

## Panel continuity and bounded deep-sky imagery

2026-09-09 user revision: small is the hard lower drag stop, not a swipe-to-dismiss state. Its clipped document accepts upward expansion to medium; medium/large use the continuous document scroll owner, with extent dragging confined to the visible header band. Explicit Back and intentional exposed-map activation retain their existing dismissal semantics. Large joins the navigation/header boundary with no exposed or hit-testable map seam; header and safe-area pixels are not map-dismiss targets.

Opening or retargeting a panel must not expose an empty shell, close/reopen flash or intermediate old selection. Keep a coherent visible presentation until the requested identity/content or its truthful loading state is ready, then commit together; superseded work cannot replace a newer selection or reopen a dismissed surface. Design iframe preparation is a prototype accommodation, not a requirement to duplicate production content trees.

Cloud-sky zoom must reveal progressively resolved, attributable galaxy/nebula survey imagery where coverage exists, not merely enlarge rendered star dots or a blurred bitmap. Use one angular camera/projection for star catalog, solar-system targets and sky-registered imagery; zoom changes field of view, not coordinates, time or sensor-direction ownership. Image footprints, celestial reference frame, epoch and reprojection must be explicit. Archived, processed survey images are not current weather, live camera output or a naked-eye visibility promise. Missing coverage/resolution stays missing; no generated imagery may substitute for scientific detail.

The zoom limit follows useful survey resolution plus measured rendering/pointing limits; the preliminary 1–3× control exploration is not a final cap. At high magnification, sensor jitter and retention of the intended target require evaluation before selecting any hold/focus interaction; do not silently add manual heading control. Retain coarse imagery while finer tiles load, bound concurrent requests and memory, cancel superseded work, and expose material loading/error/maximum-resolution state without clearing the sky. Night/observation themes must also constrain image luminance and preserve a clear processed-image source disclosure.

## Explicit celestial information (2026-09-09 revision)

Point selection and the user-opened object list open the same centered dark information modal, never a bottom sheet. It inherits the selected place/time, identifies the star/planet/galaxy/nebula honestly, and shows a readable name or catalog ID, available aliases, sourced introduction, relevant known facts and unobtrusive attribution. One tap on a resolvable object is sufficient. Keep all-sky labels sparse; many rendered objects do not require permanent names or a full paragraph each. A galaxy is not mislabeled a star, constellation or event to fit an existing type.

Use the user-selected dark liquid-glass adaptation of My’s plan card: high-transmission graphite tint, moderate background blur that softens fine detail while transmitting colors and broad shapes and restrained refractive/reflective edges from the shared material owner. Keep a compact title, smaller coherent body/parameter hierarchy and a 44px close target; a smaller content-sized modal and coherent restrained type are required; font selection/loading belongs to the design resource, not to a claim that a named font is installed. Reduced transparency or unsupported blur uses a readable opaque dark fallback. Center the content-sized dialog within safe available screen space; long text scrolls inside with header/close reachable. Dim the sky, isolate background hit testing and accessibility focus, and return focus to the triggering target or object-list entry on close. Back closes this modal first and restores the same sky context; it must not dismiss the underlying place. Warm-red observation mode applies to this modal too. Do not add brightness-heavy illustrative imagery without real licensed source coverage.

Keep loading, available introduction, basic-facts-only, and retryable failure in the same stable modal. Show known identity/facts during loading; distinguish missing editorial content from request failure. No source means no invented description, measurements or borrowed image. Introductions describe the object, while azimuth/altitude explicitly belong to the displayed selected-place/time sample. Hold that sample identity while the modal is open; closing resumes existing direction/time ownership without a new location request or time reset. Image zoom availability and introduction availability are independent.

This is intended behavior, not a completed production feature. The current production scene/catalog types and canvas/target list do not supply universal hit testing plus a lazy introduction contract. The candidate demonstrates named targets with one sourced Vega introduction and basic-only alternatives; it does not establish all-star or galaxy production coverage. Scalable identity, query and caching boundaries are owned by [runtime/domain architecture](../../../../architecture/runtime-and-domain.md#celestial-object-selection-and-lazy-information).

No-image panel corners: retain both top round corners even at large extent. The rounded-away pixels at the large panel's top boundary belong to its presentation shield, not to exposed-map dismissal; the header boundary remains continuous. Smaller extents retain intentional exposed-map navigation and their established drag/scroll rules.
