# WeChat Mini Program Screen Contract: Starward

## Authority And Product Boundary

- This Context owns the durable route, region, Control, state, navigation, interaction and accessibility responsibilities of the independent WeChat Mini Program delivery. Product and technical meaning comes from the current accepted proposals under their correction/disposition precedence and complete selected census indexed by `docs/wechat-miniapp-v2-1-1-source.md`; those filenames and version labels identify immutable provenance, not a development branch. Exact visual values come only from `DESIGN.md#wechat-mini-program--sky-canvas-v1`.
- The earlier formal handoff `docs/design-resources/miniapp-selected-handoff-2026-08-22-v3/miniapp-drift-correction-selected-v3.md` and its frozen resource closure remain immutable historical traceability/rollback inputs. Their visual styling, exact geometry and fidelity constraints are inactive after the later Sky Canvas selection. They do not prove production conformance and cannot replace this Screen Contract, current Source or runtime checks.

## Selected Design Adoption Record

- Active system record key: `target.system.wechat-miniapp-sky-canvas-2026-08-25`; its sole exact-value owner is root `DESIGN.md`, anchor `#wechat-mini-program--sky-canvas-v1`. This Screen Contract binds its Surface/Control responsibilities to that system without copying token values.
- Immutable system source: `docs/design-resources/miniapp-design-system-2026-08-25-sky-canvas/selected-provider-design-system.md`, SHA-256 `03c300a6cfd1b23e0b84b72baaa26081eef0f958de515b75413be771029499b1`; source index SHA-256 `a602a572b93d3aa1b0e51e320b4c25e14267d43b131109844c7accb4e5efbc2b`; editable upstream Open Design `user:starward-sky-canvas-candidate-c`.
- Selection basis: the owner explicitly selected “天空画布 Sky Canvas” on 2026-08-25 after visual-direction review. Corrected Product V2.1.1, corrected Technical V2.1.1 and the complete selected requirement census still own non-visual semantics; the later visual-system selection supersedes only conflicting Soft Instruments and selected-v3 visual rules.
- Current page/interaction resource: `target-miniapp-sky-canvas-current-constraint`, interpretation `constraint`, selected by the owner on 2026-08-25 after the complete DRA feedback loop. Canonical entry `docs/design-resources/miniapp-design-system-2026-08-25-sky-canvas/selected-source/miniapp-entry.html` SHA-256 `3da0696143dcd647869a4d8d0710e477c8f94a2309e00fcf4d2f0a5eaff4b1f2`; immutable handoff `docs/design-resources/miniapp-design-system-2026-08-25-sky-canvas/technical-binding-2026-08-29-compass/handoff/miniapp-sky-canvas-current.md` SHA-256 `17288e6ccc7092a5be6b1ea3bfc0ad73d0b7bea893b0e0bf1ffdc7c172426834`; Fact manifest SHA-256 `78e569a26f268454ae968bf715ecec37d6dfa23af01b33b72479973e2f4ae3bb`; feasibility SHA-256 `48468b55e0f95fd50941761b4a7fb5dc0e39de244da6055281617bb671ff9875`. It applies to the declared Map/Finder, Spot Detail, astronomy/orientation, My/Plan/Settings and contribution surfaces/states as an implementation constraint, not a pixel-exact target.
- The current technical-binding successor preserves the selected canonical entry, resource/Fact/condition closure and all design meaning. Its substrate snapshot includes the shared compass lifecycle dependency in the orientation feasibility basis. Former `selected-handoff/miniapp-sky-canvas-current.md`, `technical-binding-2026-08-29/**` and `technical-binding-2026-08-29-lf/**` packages remain immutable historical bindings, not active technical input. Future technical repackaging must revalidate current capabilities and dependencies while retaining the complete selection identity; visible or semantic design changes still require review through the editable upstream.
- Current page-resource editable upstream: Open Design project `starward-sky-canvas-core-2026-08-25`. Revise only that one current project during a future feedback loop, then create/review a new immutable selection identity and update this record; never overwrite the current selected Source/handoff or preserve an ordinary parallel old candidate.
- Historical record: `target-miniapp-drift-correction-selected-constraint-v3`, its handoff and 16-resource closure remain immutable traceability/rollback inputs, but their visual styling, exact geometry and fidelity constraints are inactive. Product requirements such as formal-spot completeness, real account isolation, real-data default, user contribution moderation, shared time context and sensor-follow-only orientation remain controlling through Source and this Contract.
- Page-resource fixture rule: the selected constraint may use illustrative astronomy, weather, map, contribution and operations values only when the review resource identifies them as non-live fixtures. The product viewport still expresses the production data contract through source/freshness/completeness states and never presents a fixture as current provider truth. Selection authorizes declared composition/state/interaction constraints only; it does not promote fixture values, browser behavior, native capability or production acceptance.
- Verification boundary: native map/canvas/sensor, production layout/pixel/accessibility/motion/interaction, real provider facts, coordinate authorization and real WeChat identity/isolation require independent final-candidate verification through their owning runtime lanes. Provider or screenshot success cannot close them.
- The Mini Program is an independently completable product carrier under `apps/wechat-miniapp/**`. It does not replace, prove, or inherit the native React Native App. Shared domain/contracts/adapters may be reused only through their declared boundaries.
- The carrier is WEAPP-only. A generated or separately maintained Web/H5 page is not a product surface, visual baseline, verification authority or alternate route implementation; UI repair starts from the complete selected design closure and is observed in the real WeChat runtime.
- Within that carrier there is only one current product implementation. Version names in corrected proposals and protocol paths are provenance or wire identifiers, not separate development targets: implementation changes the existing owner directly, removes superseded behavior, and must not preserve parallel old/new page compositions, stores, repositories, service graphs or acceptance lanes. User-facing pages and ordinary admin copy name the current feature or state without proposal/version labels; an external API version remains confined to its protocol boundary.
- A newly recorded location remains a visibly incomplete operator candidate until the server-owned formal-spot completeness policy passes. The operations surface must show its exact missing evidence, permit edits against the same record, and keep publication unavailable while any requirement is missing; the Mini Program formal map/detail/night owners must never receive that candidate through a formal-spot DTO or synthesize its missing facts.
- Authenticated users and administrators are the intended manual intake sources for current field facts. User reports, corrections, proposed places and uploaded media remain contributor-scoped provisional records; only authenticated operations may review and merge them into the single canonical spot/evidence owner, and that merge must rerun the unchanged publication gate before a formal spot becomes visible.
- Primary navigation is exactly Map and My. Night Sky has no global destination and is reachable only from a formal Spot Detail context carrying a valid `spot_id`. An ordinary POI or current location cannot synthesize that context.

## Route And Package Ownership

- Main package: `pages/map/index`, `pages/my/index`, `pages/auth/index`, permission rationale and error/recovery pages. Only Map and My appear in the persistent primary navigation.
- Permission settings report permission availability, never a successful position fix. Only an explicit one-shot location action may acquire a position; an unknown setting is not a refusal. Returning to the default trial region restores the default viewport and removes the prior selected place, Finder query, Observation Context, overlay and pending focus restoration through the existing Map state owner, while retaining committed filters, preferences, favorites, plans and search history. Results from location or map-point requests started before that reset must not restore the old place/context or publish a stale success.
- Spot package: `spot/detail`, `spot/guides`, `spot/field`, `spot/sky`, and `spot/data-source`. `spot/detail` owns the fixed header and Overview/Guides/Site content contract, including its media; `spot/sky` is its explicit child page rather than a global destination or an in-place replacement panel. Every detail section and the Night child preserve the same formal `spot_id`.
- Sky package: `sky/detail` and `sky/targets`. `sky/detail` is the sensor-following `方位天空` child entered only from the astronomy owner `spot/sky`; `sky/targets` owns target drilldown. Both inherit the same Observation Context, formal `spot_id`, selected date/time, timezone and data/algorithm revision from `spot/sky`; missing or invalid context fails closed. The retired standalone decorative sky map and page-level observation-mode route are not current product paths.
- Content/user package: `article/detail`, `plan/detail`, `contribution/index`, and `settings`. Routine favorite browsing is the Finder `想去` partition rather than a separate My/Favorites route. Feature-disabled profile-link/import surfaces are absent from the current route tree and cannot remain as a versioned compatibility product.
- Deep links activate the owning Map or My root before entering a subpackage route. Back closes route-owned overlays first, then pops the subpackage stack, and never invents a third primary destination.

## Stable Product Surfaces

### `miniapp-map-discovery`

- Primary question: Which formal stargazing spots should I filter or compare from simplified information on this map, and which one should I inspect next?
- Main surface owns: one-time location request with manual fallback; viewport restoration; one conventional floating Search field; one compact, wrap-safe row of immediately committed quick filters beneath Search; and one non-modal, map-parallel `map-spot-finder-sheet` with exactly three extents `closed`, `peek` and `expanded`. The clean default map is `closed`. The first unselected→selected quick-filter transition or committed query may auto-open `peek`; subsequent changes update the same projection in place. The Sheet's named handle is both the drag region and the explicit 88rpx tap/accessibility toggle, while its visible bar remains quiet. `peek` exposes a compact first group of advanced-filter choices and the first useful result; `expanded` exposes the complete advanced draft editor, search suggestion/result context and the two collapsible `想去`/`其他观星点` result partitions with city headings. Quick filters live only beneath Search and never repeat inside the Sheet. The Source-defined 18 terminal choices remain one filter truth: quick choices commit immediately, while advanced choices retain one opening snapshot, draft, conditional revert/commit actions and discard-on-close semantics. An ordinary base map consumes the remaining viewport and shows formal-spot markers by default. One compact in-map `观测条件` status bar shows only the active analysis/key metric/local time and opens `SourceLiftFocusLayer.mapCoupled` with one optional analysis overlay at a time (`NONE`, `LIGHT`, `TOTAL_CLOUD`, or user-facing `今晚观测条件` backed by SkyOpportunity), exactly one visible selected local time in its non-scrolling compact control surface, and the same physical map. Compact marker callouts, one shared Finder-result/marker/callout selection and explicit Spot Detail or external-navigation handoff remain unchanged.
- It forbids: a standalone visible Finder launcher, a visible “展开筛选/收起筛选” button, a redundant “找今晚的观星点” heading/explanation block inside the Sheet, a default-open Sheet, duplicated quick filters inside the Sheet, gesture-only access to an essential extent, or a full-screen/focus-lift Finder that removes the map from the comparison task; separate peer point-list or Favorites triggers; nested Finder child Modals or simultaneously open suggestion/filter disclosures; suggestions displaced from the Search field or remaining visible after true focus departure from the field-plus-overlay group; a permanently expanded analysis dashboard above the map; a scrollable or visibly scroll-barred observing-condition control surface; a Favorite/star metaphor for observing conditions; a second/remounted map, duplicate selected-time store or duplicate visible selected-time text in the analysis focus state; exposing the ordinary base map or default formal markers as peer layer tabs; duplicating light/time as another bar or peer tab; hiding scrollbar chrome by disabling or clipping the owned scroll needed to reach long filters/results; permanent pan-arrow or zoom-stepper chrome where native drag/pinch applies; full Observation Context, detailed formal-spot cards, complete route/facility/safety evidence or other drilldown content on the map; Finder-result activation entering Detail before the map callout; ordinary POIs masquerading as formal spots; stale draft restoration; straight-line distance described as a route; filters split across competing stores; or product-design rationale, routine map-gesture instruction, route/debug/reviewer metadata and sample-disclosure chrome inside the user phone surface. User-relevant source, freshness, uncertainty and recovery copy remain product content and are not removed with review metadata.

### `miniapp-spot-detail`

- Primary question: Is this named formal spot suitable, reachable, trustworthy and safe tonight?
- Main surface owns: a fixed identity/header action hierarchy with the spot name, a visually borderless Favorite icon state inside its full accessible hit region, and a distance/route row with one quiet trailing `去这里 →` text action rather than a filled block; Overview/Guides/Site content with an animated segment indicator/content handoff; representative media first with source/license/site status; hard-blocker-aware Tonight conclusion; route/facility/provenance evidence; plan actions; one quiet `反馈现场情况` handoff carrying the formal `spot_id`; and one explicit child-page entry to `spot/sky`.
- It forbids: generic or unexplained availability/change chips in the identity header, safety blockers averaged into a positive recommendation, missing facts rendered as zero, unlicensed or non-site media presented as site truth, duplicated static requests when only dynamic date context changes, an instantaneous un-signalled segment swap, and a repeated Favorite/Night/navigation button row inside the arrival/facilities content.

### `miniapp-spot-night`

- Primary question: What can be observed from this formal spot and how can the user orient and execute without losing dark adaptation?
- Main surface owns: an astronomy-information parent page with understandable summary, attributable selected-time data, source/freshness/completeness feedback, icon-assisted professional aligned bands and matrices, structured target recommendations, a versioned 2D sky map and one shared time scrubber; one explicit `方位天空` child entry carrying that same formal spot/time context; sensor-following orientation with permission, calibration, accuracy, retry and unavailable states; one concise rounded permission/recovery panel that keeps the sky dominant; a non-canvas celestial-object list for accessibility and degraded use; and the closed warm-red observation presentation with offline freshness after it is explicitly entered from My Settings.
- It forbids: a local observation-mode entry control; a global/current-location or independently seeded sky route; example targets or prototype fixtures presented as live facts; a manual orientation mode, direction `+`/`−` buttons, drag-to-heading control or fabricated heading when the sensor is unavailable; AR/full deep-sky catalogue outside the current accepted scope; white flashes in observation mode; and making the astronomy information, target details or recovery actions sensor-only.

### `miniapp-my-library`

- Primary question: What is my account state, and how do I reach my plan or settings without turning the root into a second content browser?
- Main surface owns: a conventional account-center root with a title-only header, one Tier-A Settings gear action, concise login/profile summary and a small grouped routine-entry list; quiet Plan and field-feedback/correction entries with standalone recoverable child routes; contributor-visible pending/review counts; and a standalone Settings child route with objective-fact-preserving preferences and the explicit enter/exit control for the closed warm-red observation presentation.
- It forbids: peer My/Favorites/Plan/Settings tabs; the subtitle “收藏、计划与显示偏好”; any Favorite count, row, list or duplicate favorite-browsing route on My; copied e-commerce orders/coupons/membership/promotion modules or third-party brand assets; page horizontal scrolling; plan/official sample article cards on My home; a duplicate spot detail; and dynamic-provider failure deleting the static favorite relation retained by Finder/Detail.

### `miniapp-profile-content`

- Primary question: How can the user manage external profile links and import their own stargazing post while preserving rights, lineage, moderation and point identity?
- Main surface owns: neutral external link records, URL validation and copy-first fallback; `SOURCE → EDIT_DRAFT → ASSOCIATE_SPOT → PREVIEW → SUBMIT`; rights attestation; manual import; parser/license gates; editable draft protection; formal `spot_id` association or independent `spot_proposal_id`; moderation and EXIF/location sanitization.
- It forbids: platform affiliation claims, dangerous schemes, automatic parsing without allowlist/license/SSRF controls, parser retry overwriting edited fields, a proposal creating a formal `spot_id`, and unreviewed public UGC.

### `miniapp-contribution-intake`

- Primary question: How can an authenticated user submit a field report, correction or proposed place without publishing an unreviewed fact or leaking private location/media metadata?
- Main surface owns: an existing formal-spot context when supplied by Detail; deliberate report kind and affected fact topics; observation time; concise evidence narrative; explicit rights and precise-location consent; bounded media selection/upload; durable draft and upload state; idempotent submit/retry; and the current user's pending/approved/rejected history.
- It forbids: automatically confirming a submitted claim, collecting current location for an existing-spot report, retaining raw EXIF, accepting unsupported MIME or oversized files, submitting without explicit rights for media, disclosing another contributor's data, and creating a formal spot or changing its published facts before owner review plus canonical completeness reassessment.

## Stable Control Inventory

The following keys are the complete material Control inventory for the current accepted scope. Repeated list rows/markers reuse the owning family key rather than creating hidden semantics.

- Shell/shared: `mini-primary-navigation`, `display-mode-switcher`, `data-source-disclosure`, `notification-feedback`, `page-state-recovery`.
- Map: `source-lift-focus-layer`, `map-spot-finder-trigger`, `map-spot-finder-sheet`, `map-finder-search-field`, `map-finder-query-overlay`, `map-finder-quick-filters`, `map-finder-sheet-handle`, `map-finder-advanced-filters`, `map-finder-result-list`, `map-location-control`, `map-analysis-time-bar`, `map-analysis-focus-layer`, `map-layer-selector`, `map-time-control`, `map-marker-card-coordinator`, `map-selected-spot-callout`.
- Spot detail: `spot-header-actions`, `spot-favorite-action`, `spot-navigation-action`, `spot-segment-tabs`, `spot-night-entry`, `spot-contribution-entry`, `spot-media-gallery`, `spot-tonight-decision`, `spot-route-summary`, `spot-facility-evidence`, `guide-article-viewer`.
- Spot Night: `sky-summary-tabs`, `sky-professional-matrix`, `sky-target-list`, `sky-time-scrubber`, `sky-map-canvas`, `sky-orientation-entry`, `sky-orientation-sensor`, `sky-orientation-recovery`, `sky-orientation-object-list`.
- My/library: `my-account-header`, `my-settings-action`, `my-profile-summary`, `my-grouped-entry-list`, `my-plan-entry`, `my-contribution-entry`, `plan-editor`, `settings-form`, `observation-mode-control`.
- Profile/content: `profile-link-editor`, `profile-link-open-copy`, `import-source-rights`, `import-draft-editor`, `import-spot-association`, `import-preview-submit`.
- Contribution: `contribution-kind-control`, `contribution-spot-context`, `contribution-topic-control`, `contribution-observed-at`, `contribution-location-consent`, `contribution-media-upload`, `contribution-submit`, `contribution-status-list`.

## Cross-Control And State Invariants

- `mini-primary-navigation` owns only Map/My. My has no second tab set: `my-account-header` exposes the title and one named 44px Settings gear action, `my-profile-summary` owns login/account state, and `my-grouped-entry-list` contains only a limited number of routine rows. `my-plan-entry` pushes `plan/detail`, `my-contribution-entry` pushes `contribution/index`, and `my-settings-action` pushes `settings/*`. Back restores the My root's useful scroll position and the logical opener focus. Favorite browsing remains in Finder `想去`, while the same underlying favorite relation remains operable from Spot Detail; Plan, Contribution and Settings do not create another favorite or spot truth.
- `spot-contribution-entry` and `my-contribution-entry` converge on one contribution owner. Detail passes only its formal `spot_id` and display label; My opens general intake. A draft, upload session and submitted report are identity-scoped server records with explicit revision/idempotency state. Approval means accepted for canonical owner processing, not that the report itself became a published fact; only an audited operations merge followed by a fresh complete publication assessment can change formal output.
- `map-spot-finder-trigger` is the logical Map entry owner rather than a standalone visible button. Its user-visible carriers are `map-finder-search-field` query commit and an unselected→selected action in `map-finder-quick-filters`; either can open the same `map-spot-finder-sheet` to `peek` when it is `closed`. The Sheet keeps `extent = closed | peek | expanded` separate from `open_reason = quick_filter | query | manual`, retargets from its live presentation extent and queues nothing. Clearing the last auto-open reason may return an untouched `peek` to `closed`; once the user manually expands or manipulates the Sheet, filter changes update in place and do not unexpectedly dismiss it. Back/Escape and downward drag step `expanded → peek → closed` before route navigation. Closing never clears committed query/quick filters or the selected map spot.
- `map-finder-search-field`, `map-finder-query-overlay`, `map-finder-quick-filters`, `map-finder-sheet-handle` and `map-finder-advanced-filters` derive one result projection. Search owns exactly one conventional Search symbol. Its history/fuzzy-suggestion overlay is geometrically anchored immediately below the field and above the map/Sheet; focus leaving the field-plus-overlay group fades and closes it on the target state-swap timing, while pointer/keyboard suggestion selection commits before blur dismissal. Choosing a suggestion or confirming a query updates query/results, may open `peek`, and never enters Detail. Quick filters commit immediately, remain visible only beneath Search and use an adaptive wrap-safe compact row with hidden scrollbar chrome when a bounded overflow implementation is unavoidable. The Sheet has no explanatory header or textual expand button. `map-finder-sheet-handle` is a programmatically named 44px/88rpx control with expanded state; tap toggles the next extent, direct drag follows the grab offset, and reduced motion preserves the same state graph without large travel. `map-finder-advanced-filters` lives inside the Sheet, uses an adaptive compact grid rather than a fixed two-column wall, and retains full hit regions at every width. Opening its editor snapshots the committed set into one draft. Only when the draft differs, compact named revert and commit actions appear; revert restores the opening snapshot and commit atomically replaces the committed set. Closing the whole Sheet discards an uncommitted advanced draft. Query, committed filters, partition-expanded state and useful result scroll position survive dismissal/reopen within the Map route.
- `map-finder-result-list` has exactly two first-level partitions: `想去` contains favorited formal spots and `其他观星点` contains the remaining matching formal spots. Each padded partition header is a full 44px operable row, exposes an accessible expanded state and result count, and ends with an optically centered 18–20px chevron that rotates without shifting the title. City names are group headings or sticky labels inside the partition, not another disclosure layer. The bounded result owner retains touch/wheel/keyboard scroll and restores useful position while hiding scrollbar chrome. Empty/no-results, stale-summary, partial and dynamic-provider failure states preserve static spot/favorite identity and expose a safe recovery.
- Activating a Finder result closes the Sheet, recenters the existing map only as needed, commits the one selected formal spot and opens the same compact callout. It does not enter `spot/detail`; only a later valid callout press-out, keyboard or accessibility activation navigates. The selected marker/callout receives non-color-only feedback and the callout becomes the logical focus target.
- The ordinary base map is not a layer choice. Formal-spot markers are the default point collection. At normal text size the compact header, one Search row, map origin and bottom navigation fit the usable phone viewport, and the map fills the remaining height so the user can compare points without first scrolling past an analysis panel. `map-analysis-time-bar` is the user-facing `观测条件` Bar, floats inside the map and uses a Tier-A condition/instrument symbol rather than a Favorite/star metaphor. It exposes only the current layer, one key metric/state and local time; it is not a second Search/layer tab row. Activating it opens `map-analysis-focus-layer` through `SourceLiftFocusLayer.mapCoupled`. `map-layer-selector` controls at most one colored analytical raster/forecast overlay at a time so incompatible legends are never blended.
- `map-time-control` owns the shared `ObservationContext.selectedAt` inside the analysis focus layer: exactly one visible current local date/time value, direct scrub preview, compact metrics and map feedback remain together. The slider retains its programmatic value without rendering duplicate hero, helper or input copies of the same selected time. The upper observing-condition control surface must compact/reflow rather than acquire an internal vertical scroll; its quiet close action uses one 44px transparent hit region around an optically centered 18–22px `×`, without a visible oversized button box. MapScene supplies one viewport-bounded compact timeline projected by the BFF from the same per-spot hourly SkyReport truth: each frame contains per-spot cloud, moon and selected-time SkyOpportunity signals, plus polygons only for the requested dynamic layer; static layer metadata is not duplicated per frame. Dragging updates the displayed time, frame polygons, metrics and dependent point summary immediately without a request or client-side weather/opportunity calculation; valid release commits the sole Observation Context and may prefetch/request the new window, while cancellation returns to the last committed value. Activating the date/time may open one precise picker using the same state. `LIGHT` is source-dated/static for its declared period and is visibly described as such; `TOTAL_CLOUD`, `OPPORTUNITY` and dependent point summaries may change with selected time. A static layer must never animate fabricated hourly change, and a whole-night TripDecision must never be presented as the selected-time condition.
- `map-marker-card-coordinator`, `map-selected-spot-callout`, route context and accessibility list expose one selected formal spot. The first valid marker activation selects and opens the compact callout. The callout contains only the spot name and a small comparison set such as light class, parking availability, distance/drive estimate and tonight observing condition; its press-in boundary changes immediately, and only a valid press-out/keyboard/accessibility activation enters `spot/detail`. Drag-away, map-pan competition, cancellation, disablement or invalid release does not navigate.
- Direct map input is pan, pinch and tap. The Finder result list provides the non-gesture point-selection alternative. The map does not render persistent left/right pan arrows or `+`/`−` zoom controls; automatic camera movement cannot fight a user-initiated pan.
- `spot-segment-tabs` contains Overview/Guides/Site and preserves the formal spot identity while each section owns loading/error/retry. Selection moves one stable-geometry active indicator and replaces the content with the target 160ms opacity/state-swap grammar; reduced motion uses an immediate or at-most-100ms opacity equivalent. The prior section is hidden from accessibility and interaction as soon as the new section commits, and rapid retargeting cannot expose overlapping interactive sections. `spot-night-entry` is one flat whole-row route immediately after `spot-tonight-decision` and before `spot-segment-tabs`, remains present regardless of the active Detail section, and pushes the astronomy child `spot/sky` with the same `spot_id`, date/time, timezone and data/algorithm revision. Its Tier-A horizon/constellation icon, `今晚夜空` label, secondary summary derived from that same committed local time and trailing chevron form one accessible action; it is not a large full-width primary button and does not reuse the Favorite star. Back returns to the same detail section/scroll position. A date/time change refreshes Tonight/Sky dynamic data without re-requesting immutable facilities/media unless their freshness policy requires it.
- `spot/sky` is the astronomy-information parent for `sky/detail`. `sky-orientation-entry` pushes `sky/detail` only after binding the current formal `spot_id` and the single committed Observation Context. The child shows the inherited place and local time without creating another store. Back restores the astronomy page's exact committed time, useful scroll position and logical opener focus; returning never resets to “now”, remounts a different spot or loses the selected target/mode context.
- `spot-tonight-decision` consumes the server-owned `TripDecision`, which references the same server-owned `SkyOpportunity` used by the Spot Night timeline. `SkyOpportunity` owns per-slice sky evidence and the primary/backup continuous windows; `TripDecision` accepts only a formal spot and applies access, openness, road, legal and safety blockers before any suitability judgment. Any applicable trip blocker produces an effective “不建议” conclusion; a high sky score, explanatory score or user preference cannot override it. Neither Detail nor Spot Night may recreate these rules or substitute an all-night average for the primary window.
- `spot-night-weather-evidence` consumes the BFF's single normalized weather composition. The ordinary conclusion uses the declared primary timeline and labels an Open-Meteo substitution as a visible primary fallback; it never merges models by choosing the clearest result. The professional matrix exposes total/low/mid/high cloud, applicable model-consistency label and provider validity without converting missing fields to zero. Active material official alerts are visible hard blockers. If the official alert feed is unavailable or expired, sky-only evidence may remain readable but the formal-spot trip conclusion cannot be positive and must explain that the safety input is unavailable. Provider/model names, run/valid time, limitations and all contributors to a fused value remain reachable through the existing SourceLift owner rather than through debug IDs or product-version copy.
- `spot-header-actions` keeps page-level actions out of content cards. `spot-favorite-action` is the accessible star state beside the spot name, uses no visible enclosing button border while preserving the full 44px hit region, and exposes pressed/selected/busy semantics independent of its animation. Inactive night mode is an unfilled white outline; activation optimistically fills pale yellow while the dominant main star rotates/scales and at most three smaller differently sized satellites enter from outside its non-interactive effect stage, settle and stop. Their short tapered tails remain faint and static while active; combined satellites stay visually subordinate to the main star. The bounded ritual runs once, does not loop or glow, retargets from its live state under rapid taps and queues nothing. Deactivation restores the outline and fades tails/satellites; reduced motion removes travel/rotation and preserves a short fill/opacity state change. Observation uses the same geometry and only warm-red roles. A failed commit retargets visibly to the authoritative relation and emits `notification-feedback`; Finder `想去` and Detail must remain consistent. `spot-navigation-action` is one quiet trailing `去这里 →` text action in the identity/header distance-route row, performs the same safety/blocker check before external handoff and never repeats inside `spot-facility-evidence`. `spot-night-entry` appears once in the upper Detail hierarchy at its fixed decision-to-tabs position and is not duplicated as a generic content-card action. The identity header does not carry generic `地点资料可用`/`今晚条件会变化` chips; their meaningful evidence belongs to Tonight, source, freshness and facility content. Existing DESIGN spacing tokens separate Detail cards; facilities content owns facts, not page-level action chrome. Finder `想去` and `spot-favorite-action` retain the same underlying favorite relation when dynamic summaries are stale or unavailable.
- `sky-time-scrubber`, `sky-map-canvas`, `sky-professional-matrix`, `sky-target-list` and `sky-orientation-entry` share one versioned spot/time context. Every rendered astronomy value is attributable to that context and exposes reachable source, validity/update time and partial/stale/unavailable meaning; missing facts are never replaced with prototype values. Tier-A semantic icons improve scanning for twilight, cloud, moon, rain, wind, sources and targets, while the aligned track/matrix remains the value owner and no metric becomes an isolated decorative card. Sensor changes presentation orientation only; it does not alter astronomical truth or the committed time.
- `sky-orientation-sensor` is sensor-follow-only. After explicit permission it consumes the platform orientation stream, exposes calibration/accuracy state and updates the canvas direction continuously; it has no manual toggle, stepper, direction drag or separately editable heading. Before permission, `sky-orientation-recovery` uses one concise, rounded, stable-geometry panel with a compass/orientation icon, one friendly title, one short privacy/usage sentence, one compact primary action and a quiet defer route; it does not repeat policy prose, display an oversized full-width administrative CTA or expose a simulation warning as normal product content. Denied, calibrating and unavailable reuse that geometry and change the minimum necessary copy/action. When the stream is denied, unavailable, stale or below the accepted quality boundary, the canvas stops claiming a current heading and the truthful state plus `sky-orientation-object-list` remains operable; the client never synthesizes motion or silently falls back to manual input. The top status is one short named state rather than a two-line badge.
- `source-lift-focus-layer` remains the reusable interaction family only where a compact source control must temporarily reveal a dense locally complete task without changing page scroll geometry. Its current Mini Program use is `mapCoupled` for `观测条件`; Finder no longer uses `panelOnly`. The family states remain idle → lifting → focused → restoring/cancelled and own source anchor, geometry-preserving placeholder, safe-area destination, scrim, explicit close route, Back/Escape, focus return, interruptible reversal and reduced-motion substitution. `mapCoupled` rapidly fades the lifted `观测条件` source into its compact information panel while translating/resizing—but never fading, cloning or remounting—the same physical map into a companion dock; this upper panel never becomes a scroll container. Finder instead stays parallel to the live map through its own Bottom Sheet extent state.
- This named Map analysis focus pattern supersedes the prior permanently adjacent `map-analysis-time-panel` composition and its claim that Map relocation was inactive. Precise date/time selection may still use a smaller route-owned picker sharing `map-time-control`; the existing formal Spot Night time-focus behavior remains owned by `spot/sky` and is not changed by this Map contract.
- A continuous manipulation, its current value and the primary visual or numerical feedback it changes must remain together in one mobile viewport or one bounded, control-owned scroll surface. A journey must not require scrolling away from the control, manipulating it, and scrolling back to inspect its primary effect.
- When direct co-location cannot satisfy that invariant, use one self-explanatory functional trigger plus the named `source-lift-focus-layer` family or a native Modal with the same ownership. A dropdown inside an active focus layer is an anchored disclosure owned by that layer, not another Modal: it must overlay rather than reflow the owner's scroll geometry, own bounded overflow when necessary, close before its parent, and remain mutually exclusive with sibling disclosures.
- An icon may stand alone visually only when its platform convention and meaning are unambiguous in context; every icon-only control still owns a programmatic accessible name, role and state. Otherwise the trigger pairs the icon with a concise label. Secondary explanation is available through an explicit operable disclosure and is never hover-only.
- Mini Program component/icon implementation follows one Build / Reuse / Buy boundary: reuse the existing semantic-asset component/adapter as the single production extension point, but do not reuse inactive Soft Instruments asset styling as Sky Canvas truth. Generic Search/Filter/Chevron/Clock/Compass/Route/Facility/Cloud/Rain/Wind/Moon symbols may come from one mature lightweight platform/license-compatible icon library projected into local mode-aware assets; Sky Canvas-specific moon-phase/horizon/galaxy/radiant symbols may be bounded project assets through the same adapter. A bounded self-implementation remains allowed when it preserves every applicable `DESIGN.md` token, mode, interaction and accessibility semantic. No option is mandatory merely because it is open source or already installed. A heavy second UI system, an unthemeable dependency, duplicate component behavior, per-screen icon drawing or a second token/icon truth is prohibited without a bounded project-owned exception.
- `observation-mode-control` is rendered only on the standalone Settings child route and explicitly enters or exits the closed warm-red observation presentation without navigating away from Settings. `display-mode-switcher` preserves route, focus order, selection and information hierarchy across day/night/observation; leaving observation returns to the exact prior mode/context. Spot Night consumes the active presentation but does not own a second entry or mode store.
- Settings presents display mode first, then permission/privacy and reminder ownership before data actions, with secondary spot-selection and accessibility preferences remaining reachable below. `nearby-location-preference` writes the existing `ASK_ONCE | MANUAL_ONLY` preference; orientation access and precise contribution coordinates remain per-use controls and must not be rendered as durable global grants. `departure-condition-reminder` and `contribution-status-reminder` are revisioned identity-scoped intent fields in the same `UserPreferences` owner; UI acknowledgement says only that intent was saved and never claims WeChat subscription delivery without a platform receipt. `account-data-export` consumes a server-generated identity-scoped versioned snapshot and exposes a real platform file/share result; it cannot compose the authoritative export from local cache. `account-delete` requires explicit destructive confirmation, an idempotent server receipt and session-invalidating readback before local reset; its copy discloses retained de-identified moderation/audit integrity evidence. Local cache clearing is a separate non-account action and must not fabricate export/deletion completion.
- `profile-link-open-copy` always retains Copy when Try Open is gated, denied or fails. URL save and open reject dangerous schemes and invalid/unsupported destinations without losing the draft.
- Import controls preserve edited fields across parser failure/retry. `import-spot-association` treats `spot_id` and `spot_proposal_id` as disjoint states; submission of a proposal never creates a formal point before its independent review.
- `notification-feedback` is the sole shared Mini Program presentation family for user-visible permission, stale, offline, provider/recovery and acknowledgement feedback. Its semantic tone (`info`, `warning`, `error`, `success`) is independent from its placement. The `inline` placement participates in the local owner's normal flow and carries persistent/actionable states without auto-dismissing the only recovery route. The `floating` placement is reserved for non-critical acknowledgement/light feedback, overlays inside the active phone safe area, does not alter page/Map/SourceLift/scroll geometry or steal focus, and cannot cover the current primary action. Both placements share one compact icon/title/optional-body/at-most-one-trailing-action/optional-dismiss anatomy, target tokens, named 44px actions, non-color-only severity, stable-geometry state-swap motion and reduced-motion substitution. A local owner renders at most one full notification; concurrent states use deterministic `error → warning → info → success` order plus an accessible residual count/queue, and resolving the current item reveals the next without losing its meaning. Map permission/stale/provider, Spot Night offline and My/Plan/Settings stale/provider states must use this family rather than page-local banner markup.
- `page-state-recovery` is the shared recovery family for INITIAL/LOADING/READY/EMPTY/PARTIAL/STALE/ERROR/PERMISSION_DENIED. It never substitutes realistic fake values for missing data and never hides usable static content because a dynamic layer failed.

## Responsive, Motion And Accessibility Contract

- The production surface consumes the 750rpx target with 320/375/430 CSS-pixel checks, safe areas and large-text 2×2 fallbacks. At normal text the Map first-viewport budget keeps one Search row, quick filters, a usable map and bottom navigation visible together; the Finder Sheet is initially `closed`, and `peek` never consumes so much height that the selected spot/map relationship becomes unreadable. Enlarged text may wrap quick filters and summaries but cannot eliminate a usable map or hide the Sheet handle/advanced commit/revert actions. The focused observing-condition controls must reflow/compact at every supported width and large-text mode without internal scrolling, clipped controls or a second visible selected-time value. Finder/filter/result, the Map Finder Bottom Sheet and other page owners may retain bounded vertical scrolling for reachability, but every user-phone vertical scroll owner hides scrollbar chrome and consumes zero scrollbar layout width without clipping content or disabling touch/wheel/keyboard scrolling. A visible scrollbar on an external DRA/browser review canvas is outside the phone product surface and does not excuse a scrollbar inside it. Page-level horizontal scrolling is forbidden; only an explicitly owned bounded row or data matrix may scroll horizontally. When such a horizontal indicator is necessary it overlays rather than consumes layout height/width and uses a white semi-transparent treatment legible in the active mode.
- Every actionable control owns at least an 88rpx/44px hit region, has a programmatic accessible name/role/state, communicates selection/risk with more than color, and has a keyboard/accessibility-list equivalent where the native map/canvas is not directly operable. Finder and Observing Conditions compact choices center a 60–64rpx visual capsule inside that stable hit region and use the exact compact typography, lighter periwinkle roles and 36–40rpx optically half-clipped top-right star from `DESIGN.md`; the visible star mass occupies roughly one quarter of the capsule without covering its label, and the star, selected border and label/programmatic state form one non-color selection grammar without changing layout. Selection/deselection motion is interruptible and reduced-motion-safe. Observation preserves the geometry but replaces yellow/periwinkle with closed warm-red roles. Pointer hover never creates an extra outline or enlarges geometry; keyboard focus remains visible and hugs the visible capsule rather than the invisible hit box.
- Notification content height is driven by its actual compact content rather than fixed empty space. At 320/375/430 and large text, title/body/action reflow without clipping, oversized controls or horizontal overflow; a floating notification retains invariant owner geometry, and a single inline notification must preserve a useful primary surface rather than replacing Map/content with blank space. Day/night/observation use semantic-role parity, with observation restricted to its closed black/warm-red roles and no white transition frame.
- Press, sheet, state and mode transitions follow the DESIGN target; gestures are interruptible and retarget from live position. Reduced motion replaces spatial travel with immediate or at-most-100ms opacity changes while preserving state feedback.
- Day/night/observation retain semantic role parity. Observation is a closed black/warm-red palette; media is opt-in, skeletons cannot flash white, and system/native boundaries must avoid an unowned bright transition.

## Verification Ownership

- The real WEAPP route owners plus `tools/miniapp/run-wechat-devtools-session.mjs` own current-candidate production journeys and responsive/accessibility/fault checks; `tools/miniapp/**` owns isolated build, WeChat DevTools lifecycle, candidate fingerprinting, evidence collection and teardown. The removed browser/H5 acceptance directory has no successor proxy.
- Formal input preflight proves only resource identity/completeness. Production conformance requires current Taro build plus WeChat DevTools/runtime observations, deterministic data/adapter checks, and design/interaction/accessibility assertions on the same final candidate.
- Representative physical-device/sensor/field validation is useful external evidence but is not allowed to silently replace the machine-verifiable current contract. Any truly unavailable external prerequisite must be declared as an External Confirmation and cannot be hidden in a complete claim.
