import { createBsc5pSkyCatalogProvider } from "./sky-scene-catalog-provider.ts";
/** The deterministic lane uses the same locally published stars and frame owner. */
export const createTestSkyCatalogProvider = createBsc5pSkyCatalogProvider;
