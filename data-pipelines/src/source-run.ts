export interface SourceBatch{source:string;run:string;version:string;licenseStatus:string;checksum:string;fields:Record<string,unknown>;requiredFields:string[];epoch?:string}
export function qualityGate(batch:SourceBatch,now:string){const missing=batch.requiredFields.filter(key=>batch.fields[key]===undefined);const staleOrbit=Boolean(batch.epoch&&Date.parse(now)-Date.parse(batch.epoch)>48*3600_000);const passed=missing.length===0;return {lineage:[`${batch.source}:raw:${batch.run}`,`${batch.source}:normalized:${batch.version}`,passed?`${batch.source}:quality:passed`:`${batch.source}:quarantine`],checksum:batch.checksum,licenseStatus:batch.licenseStatus,productionEligible:passed,quarantined:!passed,missingFields:missing,confidenceAdjustment:staleOrbit?-0.35:0,directProviderPassThrough:false};}
