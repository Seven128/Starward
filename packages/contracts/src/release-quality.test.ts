import{describe,expect,it}from"vitest";import{evaluateReleasePromotion,evaluateRestoreEvidence}from"./platform-boundary";
describe("release quality",()=>{it("blocks OTA for native changes without new binaries",()=>{expect(evaluateReleasePromotion({currentRuntimeVersion:"1",candidateRuntimeVersion:"1",nativeModulesChanged:["amap"],schemaReversible:true,channel:"staging"}).promotion).toBe("blocked-native-binary-required")});it("computes restore objectives without treating Redis as truth",()=>{const r=evaluateRestoreEvidence({targetAt:"2026-07-20T01:10:00Z",latestDurableAt:"2026-07-20T01:01:00Z",startedAt:"2026-07-20T01:20:00Z",completedAt:"2026-07-20T02:01:00Z",databaseVerified:true,objectVersionsVerified:true,referencesVerified:true,permissionsVerified:true,redisRestored:false});expect(r.objectivesMet).toBe(true);expect(r.redisRequiredForTruth).toBe(false)});});
