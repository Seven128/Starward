# I21 implementation binding R9

This immutable technical binding preserves the complete selected R6 canonical resources, Fact manifest, Inspector, Oracle, environment and Design Authority identities. It does not regenerate that closure or overwrite R8.

Only the current `source.miniapp.sky` digest changes in the feasibility input. The current implementation selects exact matching hourly, catalog and actionable-target frames for the displayed instant, including transient preview and cancellation, and pauses unavailable target projection rather than borrowing committed targets. The existing product/technical Source and owning Context govern these semantics; this binding introduces no selected visual Fact, condition, proof-method or permitted-realization change.

The handoff retains the R6 manifest and a 2,000,000-byte publication ceiling. Publication/preflight establishes input integrity, not production conformance or physical alignment.

Authority Delta Assessment: `consistent_with_current_authority`; the existing R6 assessment and unchanged Design Authority closure remain referenced. No system adoption or new design precedent is introduced.

Downstream adoption remains owned by the same Long Task protected Authority Revision. This resource does not accept the implementation or fulfill any device obligation.
