# B 组实际 round-0 回看与第一轮修订

已用 `view_image` 逐张查看 round-0/e1-B-{1,2,3}-{map,my}.png，全部为主任务实际 Figma 导出。没有读取 A。以下是生成者定位问题与判断，未填盲评分数，不代表用户偏好或机械检查之外的通过证据。

## 六张图的观察

| 图 | 实际看到的结果 | 处理 |
| --- | --- | --- |
| B1 地图 | 地点身份突出，开放和设施扫描简单，文本没有明显过小；底部 y=669 的“天文信息”孤立露出，没有说明时间 | 在同一 retained 天文章节头配唯一已选日期时间，ruler 不再重复标签；不为 medium 新建内容树 |
| B1 我的 | 账户、计划、反馈、日常入口清楚，短说明可读。下半空白是当前只有四项职责的结果 | 保持当前内容密度，不编造模块填满页面 |
| B2 地图 | 单一浅灰事实矩阵稳定，但停车/厕所先于开放时间，不符合进入条件优先的阅读顺序 | 矩阵内先开放时间、再设施；夜间进入未知仍紧邻 |
| B2 我的 | 计划地点和草稿/审核的层级最完整，与地图矩阵形成较好的跨页对应；但计划/反馈整区缺少可进入提示，容易被看成静态资料 | 在同组右侧补现有 chevron；保持整区控制和 44px 下限 |
| B3 地图 | 类别列有秩序但略占宽度，当前短 fixture 未见截字；同样孤立露出天文标题 | 与 B1 使用同一天文章节头修订；保留方向的标签列实验 |
| B3 我的 | 左侧“09 月 / 07”与右侧“2026-09-07”重复，计划显得偏松；计划与反馈缺入口提示 | 日期统一为左侧 2026 / 09.07，删重复日期行，计划高138→112、反馈92→84，补 chevron |

所有六张顶部 x≈312 的垂直点串来自微信胶囊 `•••` 在狭窄文字宽度内换行；顶部 system status 的字符串也会丢失或漂移。属于真实渲染暴露的技术问题：本轮用原生小圆与电量框表达系统 chrome，保留44+44区，不更换产品图标库。这与下面的正式视觉修订一同导出，但单列技术原因，不隐藏发生过的问题。

主任务另指出 B3 我的“2 条已保存”可能在 y≈500 被遮挡。已以 `detail:original` 单张重看，并读同轮 JSON：说明 x60/y496/h21，父 Copy y470/h47，Row y458/h76，后续 divider y534、Import y535；说明下缘517距离下一行18px。原始图重看可读全句，未复现兄弟表面遮挡；不以机械检查代替视觉结论，第一轮仍须检查该行末字与下缘。本次没有为未复现的裁切随意增高所有行。

## 第一轮的边界与预期

正式视觉修订为 1/2；仅新增本目录 design.js，未覆盖原始脚本、execute-round-0.js、PNG 或 JSON。色彩、地图 PNG 裁切、marker 锚点、主体面板高度、字体尺度、所有业务 fixture、主要职责不变。

预期变化：顶部不再出现下垂点；我的可进入项目更清楚；B3 日期只出现一次且组间更紧；B1/B3 的天文章节即使仅露出首行，也展示它的时间上下文。下一轮实际 PNG 必须确认这些变化是否成立，尤其是天文上下文首行与固定栏没有新遮挡。未用静态截图证明 ruler 连续拖动或面板手感。

## 推荐

当前建议 **B2 关联矩阵** 用于后续状态和尺寸覆盖，状态 `recommended-unapproved`。理由是它两页的相关事实都集中为一个安静的共享面，而非每个指标单独卡片；地图的缺失状态和我的真实审核状态都保留清晰对应。它并不比 B1 更节省面积，长中文仍需后续小屏检查。B1 可保留为更轻的清单方向，B3 的额外类别列应在长文实际压测后再判断价值。

这里不替用户选稿，也未宣称满足预先量表阈值。手机原始比例阅读、人工盲评、真实 Figma 第一轮导出与交互检查仍分别待执行。

## 第一轮实际复核

已逐图用 `view_image` 查看同目录 e1-B-{1,2,3}-{map,my}.png 的六张真实第一轮导出。顶部点串不再溢出；B2/B3 我的入口箭头可见；B3 日期已单处表达，主页链接说明完整可读；B1/B3 地图天文章节头与选中时间同处一行，没有被底栏覆盖。B2 的矩阵已先显示开放时间，再显示设施。

未发现必须动用第二轮的新增视觉问题，因此核心正式视觉修订明确停止在 **1 轮**。推荐仍为 B2 `recommended-unapproved`；这个停止决定不等于预先量表通过或人工认可。后续 coverage 脚本独立保存，不覆盖核心结果。
