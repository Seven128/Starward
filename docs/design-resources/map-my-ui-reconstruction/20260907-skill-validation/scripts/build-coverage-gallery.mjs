import fs from 'node:fs/promises';
import path from 'node:path';
import {decodePng,hash} from '../../../../../.agents/skills/starward-design-resource/scripts/resource-utils.mjs';
const dir=path.resolve(import.meta.dirname,'..'),source=path.join(dir,'experiment-1/B/coverage/evidence-r1'),site=path.join(dir,'review-site');
const boards=JSON.parse(await fs.readFile(path.join(source,'boards.json'),'utf8')),assets=JSON.parse(await fs.readFile(path.join(dir,'assets/icon-sources.json'),'utf8'));
for(const [id,file,sourceUrl,license] of [['osm-reference','osm-reference.png','https://www.openstreetmap.org/#map=13/22.48268/114.55571','OpenStreetMap contributors / ODbL'],['site-photo','shenzhen-observatory-2021.jpg','https://commons.wikimedia.org/wiki/File:Shenzhen_Observatory2021.jpg','Charlie fong / CC BY-SA 4.0 / 2021; not current site access evidence']])assets.push({id,path:'assets/'+file,sha256:hash(await fs.readFile(path.join(dir,'assets',file))),source:sourceUrl,license});
const records=[];for(const b of boards){const reference=async ext=>{const file=path.join(source,b.id+ext),bytes=await fs.readFile(file);if(ext==='.png'){const decoded=decodePng(bytes);if(decoded.width!==b.width||decoded.height!==b.height)throw Error('PNG size mismatch');await fs.copyFile(file,path.join(site,b.id+ext));}return{path:path.relative(dir,file).replaceAll('\\','/'),sha256:hash(bytes),revision:b.revision};};const {fingerprint,...board}=b;records.push({...board,screenshot:await reference('.png'),snapshot:await reference('.json'),assetIds:b.page==='map'?b.state==='large-photo'?['osm-reference','site-photo']:['osm-reference']:[],codeMap:'controls-code-map.json',adoption:'recommended-unapproved; stress-test extension, not a user selection'});}
await fs.writeFile(path.join(dir,'coverage-resources.json'),JSON.stringify({schema:1,assets,boards:records},null,2)+'\n');
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
await fs.writeFile(path.join(site,'coverage.html'),`<!doctype html><html lang="zh-CN"><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>主题与状态实图</title><style>body{font:16px/1.6 system-ui;margin:24px;background:#e8e8e8;color:#222}main{display:flex;flex-wrap:wrap;gap:24px}figure{margin:0}img{display:block;max-width:100%;height:auto}figcaption{max-width:430px;margin-bottom:8px}a{color:#3440a0}</style><h1>主题与状态实图</h1><p>推荐未采用方向的覆盖检查。均为Figma原生导出，页面数值为测试示例；静态图不证明WEAPP手势、地图provider或手机运行通过。200%文字按当前项目合同暂停，320宽使用标准字号长中文。</p><p>地图 © <a href="https://www.openstreetmap.org/copyright">OpenStreetMap贡献者</a>；实景为 <a href="https://commons.wikimedia.org/wiki/File:Shenzhen_Observatory2021.jpg">Charlie fong / 2021</a>，<a href="https://creativecommons.org/licenses/by-sa/4.0/">CC BY-SA 4.0</a>，画板内裁切使用。不是当前开放状态证明。</p><main>${records.map(b=>`<figure><figcaption>${esc(b.page)} · ${esc(b.state)} · ${esc(b.mode)} · ${b.width}×${b.height}<br><a href="${esc(b.id)}.png">原图</a> · <a href="https://www.figma.com/design/${esc(b.fileKey)}/?node-id=${esc(b.nodeId.replace(':','-'))}">实际节点</a></figcaption><img loading="lazy" width="${b.width}" height="${b.height}" src="${esc(b.id)}.png" alt="${esc(b.page+' '+b.state+' '+b.mode)}"></figure>`).join('')}</main></html>`);console.log(`24-board gallery and independent coverage manifest: ${records.length}`);
