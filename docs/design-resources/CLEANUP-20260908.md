# 新版采用资源对应的旧稿清理

2026-09-08 用户授权：重新核对新版 Stitch 采用资源，删除明确被替代的旧设计，避免误删。

## 已从项目删除

| 原目录 | 已跟踪文件 | 原体积 | 替代依据 |
| --- | ---: | ---: | --- |
| `map-my-ui-reconstruction/` | 1360 | 83.41 MiB | 旧地图/我的页 A/B 实验；当前分别使用 `wechat-miniapp/map/ADOPTED.md` 和 `wechat-miniapp/my/ADOPTED.md` |
| `wechat-miniapp/map/20260907-stitch-b-refinement/` | 1558 | 172.38 MiB | 地图组件初稿至反馈13的重复实现；当前为 `map/adopted/spot-information/`，外部尺度继续保留 `map/adopted/medium-day/` |

共2918个已跟踪文件退出工作树。最终原稿、提示词、运行身份、天文研究和字号比较已复制到对应采用包的 `provenance/`，副本哈希核对一致；当前原型的CSS/JS、图片、字体和共享组件不随旧迭代删除。

原设计资源目录约500.60 MiB，清理并保留必要来源后约245.21 MiB。数字是工作树文件体积，不代表Git历史数据库或磁盘压缩后的占用。

## 本轮保留范围

- 所有当前 `wechat-miniapp/**/adopted/` 以及共享 `observation-time`。
- `medium-day` 仍承担采用索引明确保留的地图外部尺度；其内部旧信息面板不能覆盖新版三档组件。
- `miniapp-field-signal-i21-*`、`miniapp-selected-source-*` 等全产品混合包：包括尚未完成本轮重做的方位天空等页面，不按其中含旧地图/我的页而整包删除。部分包还被 `apps/wechat-miniapp/src/authority/delivery-carrier.json` 和冻结来源文件引用，生产迁移未完成，不能顺手改写或拆坏其绑定。
- 早期地图多主题/状态包及其全产品来源闭包：含本轮未覆盖的夜间、观测红光、权限和降级状态。保留不是重新采用，其中已被新版替代的页面表达不再具有当前设计权威。
- 已提交、设置及其他尚未重做范围的资料；业务代码、测试、构建绑定和Git历史未删除。

## 检查与恢复

全仓库查找被删两目录的引用：没有生产代码、Context或Skill运行时依赖；采用包中的来源/研究/上一轮链接已更新或撤除，`.gitattributes` 的旧实验路径规则已移除。旧设计开发方案中的 `<run-id>` 例子属于历史格式，不是可运行依赖。

删除前后核对68个当前 `preview/` 与共享文件的SHA-256，内容一致；当前采用包的本地HTML、import和Markdown资源链接扫描无缺失。Context manifest另行校验。

自动审批拒绝递归永久删除，返回 `blocked by policy`。因此本轮以可恢复移动实现项目/Git工作树删除，仓库外备份位于 `E:/Dev/design-resource-retired/Starward/superseded-map-my-20260908/`；未声称这些备份已永久销毁。已跟踪旧文件也仍能从Git历史恢复。
