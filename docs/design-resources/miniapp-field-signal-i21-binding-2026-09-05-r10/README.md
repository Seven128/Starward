# I21 implementation binding R10

This immutable technical binding preserves the R9-selected canonical resources,
complete Fact manifest, Inspector, Oracle, environment and Design Authority.
R9 remains unchanged. Only the `source.miniapp.sky` implementation digest is
updated in feasibility Source, with its corresponding handoff input identity.

The production sky owner now rejects missing or invalid canvas measurements,
invalidates dimensions after query/draw errors, hides the unmeasured canvas and
allows the existing retry action to restore measured geometry. Existing
projection, sensor-only interaction, shared location/time and unavailable-data
semantics remain controlling. No new visual Fact, condition, proof method,
required realization or product capability is selected by this packaging.

Authority Delta Assessment: `consistent_with_current_authority`. The unchanged
Design Authority and R6 assessment remain referenced; the existing full-viewport
sky and failure-recovery rules govern this implementation repair. No system
variance or reusable design precedent is introduced.

Bundle/preflight establishes input integrity only, not rendering or device
alignment. Adoption remains with the same Long Task protected Authority Revision.
