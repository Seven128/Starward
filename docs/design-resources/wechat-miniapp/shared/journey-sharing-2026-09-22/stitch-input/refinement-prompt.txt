基于刚给的HTML做同一方向的精修，只输出追加CSS代码，不重写HTML。首稿实际渲染发现：[hidden]被display:flex覆盖，导致有效分享页常驻失效提示，必须[hidden]{display:none!important}。分享票首屏太像普通纵向表单：保留白色纸张票据与现有3D图标，强化票面主标题32px、上下小日期码和淡金色纸面压纹（仅票面，不新增通用大渐变背景），计划地点作为视觉第二重点；将所属区域作为地点副文案，时段与天象两行保持自然舒展，不用每个指标一张卡。必须中文信息15px，辅助13px，line-height1.6。页面外没有黑底手机壳/阴影、不固定390宽、不显示demo切换条。分享票中的来源仍保留，但不展示工程/隐私实现说明、虚构档案编号、已完成归档、实际观测声明。成就首页保留整体构图，标题层级增强，列表按月份分组，用现有票据连接感而不是普通仪表盘。请直接给CSS，最多180行。