# Stitch 修订输入：舒适尺度与自然圆润

状态：用户已确认样板尺寸方向；现有页面整组候选待审，不是整体采用入口或生产令牌真源。上游为根 DESIGN.md 小程序档案与当前 Screen Contract；与通用 Stitch 模板冲突时遵从项目：保留语义配色、中文系统字体和因果动效，不引入循环装饰或网页营销 Hero。

## 视觉目标

从真机过小的旧稿恢复正常移动端阅读尺度。精致来自清楚层级、自然边缘、恰当材质和舒适触摸。信息可以向下滚动，不以缩字压进首屏。

## 颜色与材质

沿用当前观星点参考的白色与极浅中性分组、蓝灰文字、克制暖黄强调。照片卡保留当前采用图片素材及可读遮罩，示意素材不宣称真实场地实拍；图标沿用已采用B批3D拟物磨砂PNG。实际语义色仍由根 DESIGN.md 定义；本轮不另设调色板。

## 排版

390×844逻辑像素作为首张比较视口。正文15–16px、次要13–14px、地点名20–22px是交给Stitch推敲的候选范围，不是已确认令牌。地点身份先于距离；风险和进入条件正常可读。长内容换行，不挤窄动作。

## 组件

底部操作可见44–48px高且命中不小于44px。内容卡16–20px圆角、约16px内部留白作为候选起点；全屏large外壳贴边，圆润用于内部内容及动作。共享角色仍沿用 Button、Card、List/Cell 与 Screen Contract；本轮跨页尺寸家族不冒充可复用原生组件。

## 布局

保留原地图与观星点信息抽屉组合，small/medium/large档位、原把手拖动和媒体呈现不变；不是独立详情页。基本信息/地形/天文Tab及原出现时机、切换/定位和滚动关系全部复用当前完整采用源。固定动作、主导航、返回均不变。只允许文字、图标、间距和局部圆角的视觉尺寸差异，不以空状态替换原有完整内容。

## 交互

沿用原按压、取消、三档面板及章节语义；本轮以视觉尺度为主，尚不建立新的动效参数。后续动效逐项体验和确认。没有实际交互观察不能宣称动效完成。

## 保留边界

不增加统计、评分、会员或出行推荐。缺失不伪造数字，设计说明留在画布外。新增成就/两类分享页属于后续议题。整组采用前不替换采用入口，不修改生产代码。
