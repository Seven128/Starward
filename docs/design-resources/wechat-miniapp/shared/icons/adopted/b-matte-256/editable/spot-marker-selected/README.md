# 地图选中态 · 本地像素合成源

2026-09-13用户明确授权使用本地代码合成。`base-default-1254.png`为已采用default的原始像素；`ray-source-1254.png`为第3轮旧selected，仅提取其中三条独立光线。保留两种源文件、各光线原始/放置分件、独立透明覆盖层和合成的1254px母版。所有原始文件未修改。

`composition.json`记录来源SHA-256、三条光线的位置与缩放及实际核对。主体不缩放、不平移、不重绘；只调整光线，使其留在透明空白内并避免256px重采样影响主体边缘。1254px主体全部526,174个非透明像素的RGBA一致；256px可见主体像素也完全一致，其他差异仅位于光线范围。旧selected在该对比下改变525,789个主体像素，修正后为零。

`rays-overlay-1254.png`和`rays-overlay-256.png`可作为独立动效图层，画布与default一致。这是可继续处理的栅格分件，不是矢量或3D源，也不代表前端动效已实现。`rebuild.cjs`使用Node与sharp从保留图层重建，可不带参数仅验证母版/256px哈希，也可传输出目录；不修改已采用assets。已实际运行并匹配当前输出。
