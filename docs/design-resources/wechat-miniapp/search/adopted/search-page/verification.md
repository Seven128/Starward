# 采用资源验证与实施边界

采用是用户设计决定，不代表生产迁移完成。

采用稿已有检查：[交互测试记录](reference/interaction-checks.json)、[地址首行对齐](reference/alignment-check.json)。已观察到同树开合/快速反向、筛选横拖不误选、dialog draft取消/清空确定、键盘取消、本地无匹配、同input往返和减少动态。320/375/390/430视口无搜索资源文档横溢出；地址槽和首行均16px。

本次归档复制将图片URL收敛到本包assets，CSS/JS保持最终稿。已实际打开采用路径，确认5张图片全部加载、5张卡片存在、唯一input和交互脚本启动；重新操作分组开合与筛选dialog并保存reference截图。此前图片decode尚未完成的一次即时读取没有当作通过，加载完成后重新回读并截图。

真实Map/WEAPP、服务端过滤/权限/历史/故障恢复、其他主题尚未验证。卡片完整点击选点的产品要求始终有效，但资源目前只有按压反馈和静态地图背景。重复选择当前点仍需在生产迁移时保证medium恢复。
