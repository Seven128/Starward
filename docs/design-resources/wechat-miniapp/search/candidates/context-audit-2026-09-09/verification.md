# 候选验证与限制

2026-09-09，通过已登录官方Stitch网站生成/精修，源码从真实画板iframe srcdoc保存；首次明确生成失败后重试一次成功。精修成功信息之后经新页面刷新验证持久化新screen，而不是仅相信服务文字。

## 实际检查

- Context manifest/明确控制来源检查通过；本轮改动文档87处本地路径/标题链接检查无异常，git diff --check通过。它们不是生产验收。
- 候选JS语法检查通过。浏览器中横条实际16项，驾车默认180，旧推荐两项已移除，摄影前景改有实拍照片；仍复用五个分类。
- 实际启用驾车并提交240，横条显示驾车240分钟内；再改300后关闭，仍为240。输入29后确认，弹层未关闭、出现范围错误。清空后确认再打开，驾车禁用、默认180。
- 5张继承资产（含两个地图背景和三个照片）实际加载成功。未重新生成/购买图片。
- 320/375/390/430×844内嵌浏览器视口逐个选择、测量外框并实看截图，参数输入、五分类、长地点名和底部按钮可读，没有本次区域裁切。字体未整体缩放。截图库缩放造成裁切偏移，因此保留full原图和DOM测量，并仅裁取真实画板区域生成reference；不是重新绘图或视觉美化。
- 四个状态逐个等待其真实可见文案，记录见reference/state-checks.json。低云重试保持不可用文案，移除后低云checkbox为false。目录覆盖但无适用事件与不可用分开，真零匹配不出现地点列表；未知场景不把5个候选宣布为5个确定匹配。
- 沿用采用包的无参数即选、弹层draft、Escape/遮罩取消、焦点恢复和减少动态CSS；本轮实际重测的新增路径如上。未把继承的历史验证当成本轮全部重测。

## 未验证 / 待用户审查

审美与采用决定待用户；ADOPTED未改。缺失状态由画外下拉选择的固定样例表达，不是生产数据或业务过滤实现。部分覆盖示例说明候选范围，但不声称这些样例地点的真实天气状态。

真实WEAPP原生地图、真机IME/键盘、安全区、传感器、平台授权、后端筛选、实际路线、星表覆盖、远端服务均未验证。本轮没有生产开发；浏览器键盘数字编辑不证明手机软键盘避让通过。

## 第二轮检查

筛选底板与间距略缩小；驾车时间/路线公里数切换，各自保留输入；确认、取消与清空使用同一草稿。

本轮针对性浏览器脚本：`check-controls-feedback.js`（任务材料目录）。搜索/地图四宽度已检查；My居中与Plan勾选已进行几何和交互检查。仅证明候选浏览器原型，非WEAPP真机结果。

## 第三轮实际检查

驾车范围重做为轻薄两行：启用项与时间/距离同排，下一行自然数值，不再使用蓝框大卡。44px独立目标与参数确认/取消保留。

在本地设计预览完成 320/375/390/430 宽度检查与截图审视。Search 保留时间/公里参数确认、取消与越界反馈；My 检查 badge/结束时间几何中心；Sky 检查单击、居中、来源/无介绍、长内容滚动、红光和 Escape；Map 检查四宽度无图圆角与角落点击不关闭，并回归上一轮切换/拖动/滚动。未进行 WEAPP 真机、生产资料接口或大星表点选测试。

第四轮：check-feedback-04.js 实际通过320/375/390/430宽度、44px输入目标与28px可见面、四位公里数、真实字体加载、字级、玻璃样式、居中/长文滚动/红光/Escape。查看390搜索/弹窗及320弹窗完整截图。未测WEAPP字体/模糊性能。

第五轮：check-feedback-05.js通过四宽度；普通输入尺寸、1000公里提交与取消、公共材质加载、弹窗缩字/居中/长文/关闭重开/暖红、My窄屏和不透明降级。check-glass-05.js产生折射/仅透光/移动背景对照，实际查看边缘变化与主体透光。首轮Search截图落在动画中，已等待动画完成重新截取，最终图为 *-final-05-*.png。未进行WEAPP真机材质验收。
