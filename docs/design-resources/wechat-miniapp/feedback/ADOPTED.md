# 观星点反馈与远端多草稿 · 当前采用资源

> 2026-09-23：对应 Stitch 旧版本项目已删除；本文件声明的当前采用源及必要依赖继续有效。历史项目 ID 只用于来源追溯，续改从[当前 Stitch 状态与入口](../stitch-status.md)核对。

唯一有效入口：[观星点反馈与远端多草稿](adopted/spot-feedback/README.md)。2026-09-08 用户确认采用并替换旧设计。生产实现、真实服务与真机验证须分别核验，不从浏览器演示推断。

2026-09-24 当前官方WEAPP逻辑320宽抽样：新增观星点和正式观星点反馈两页共用文档字段，修复了夜间/红光下仍沿用日间白色页签、分隔、输入与页脚按钮的问题；日间仍保持原构图。原图分别见任务A10/A14记录。此为四个页面与主题组合的首屏及当前数据状态验证，不代表完整表单、审核流、原生输入层或真机通过，也不扩大日间资源采用范围。

2026-09-24 红光后续章节补查：两页均点击“地点/到达/设施/说明”并查看当前资料状态；正式反馈的已上传照片在红光下以照片身份文字代替高亮图片，移除按钮保留可见“×”和44px命中区，日间照片仍可见。新增点位说明章节的经纬度占位字和正式反馈差异旧值改用红光字色。开发器曾在清缓存后复用无样式的子包页面实例，该批截图不计；关闭并以当前仓库确切路径冷开后复测。原图及范围见本任务ledger，未据此宣称真机或完整表单通过。

2026-09-24 红光媒体交接：新增点位“说明”章节的“添加现场照片”已在官方WEAPP模拟器显示应用内黑红亮屏预警，取消后仍回到说明及原经纬度输入；修前取消曾出现Tab仍选说明而正文回到地点的错位。正式观星点反馈样本当前处于审核中，无法在该身份/状态下运行上传入口，媒体提示仅有代码与开发检查，目标运行时仍未验证。原生授权、相册/相机和真机未据此验收；采用范围不变。

2026-09-25 生产提交边界：正式反馈现复用同一账户、同一请求体在回执不明时的幂等键，并用同步忙锁阻止同帧连点；网络不确定结果提示“提交结果未确认”，保留输入供原样重试。修前函数回归复现两项失败，修后定向及完整开发检查通过。当前本机样本已有一条待审反馈，确切仓库项目重新构建并清编译缓存后，官方WEAPP仅核对了同点“审核中”只读页（387×836原图见任务A11记录），不能把这次代码回归写成模拟器新提交、正式服务或真机通过；资源采用范围不变。

2026-09-25 正式反馈照片边界：生产实现将创建意图、建立会话、完成和移除的回执不明重试绑定同一账户、操作及输入；页面从媒体交接开始锁定并发，保留已确认的上传会话，会话回执不明与完成失败分别给公共状态面板恢复，未完成照片禁止提交。官方WEAPP在隔离 MEMORY_TEST 服务与受控故障下验证正常上传、服务端201回执丢失后用同图新临时文件名续传、完成503后续传、放弃未完成照片且保留另一张已成功照片。原图为394×853；系统相册、真机、正式持久化与冷启动续传仍未验证。此为生产实现状态记录，未改变采用设计或扩大采用范围；细节见任务 A11 台账。
