# 天文事件列表与详情 · 当前采用资源

> 2026-09-23：对应 Stitch 旧版本项目已删除；本文件声明的当前采用源及必要依赖继续有效。历史项目 ID 只用于来源追溯，续改从[当前 Stitch 状态与入口](../stitch-status.md)核对。

本文件是唯一有效采用入口。当前承载为[2026-09-13共享天文事件Modal](../shared/astronomical-event-modal/README.md)，已获用户确认；原event-pages目录的catalog仍是当前原型依赖，旧独立页面不再作为当前承载。生产实现、真实服务与真机验证分别完成，不从浏览器演示推断。

## 2026-09-13 需求与资源修订

用户明确把独立事件列表/详情改为[共享大型Modal](../shared/astronomical-event-modal/README.md)，由地图流星入口浏览、由计划编辑单选；同壳左右切换。上述旧稿只保留未变的卡片/信息层级依据，其独立路由、加入计划按钮和多选承载不再指导本次开发。新视觉已于2026-09-13由用户确认采用，原稿与可编辑交互源均已保存；生产迁移已由共享组件完成，原路由仅保留兼容宿主。

当前整合入口为[完整Map/Plan中的共享Modal](../shared/astronomical-event-modal/review.html)，保留原事件活动期、日期条、来源和当地条件边界。简化Map/Plan宿主已退出当前资源，不以其缺失功能缩减原页面义务。

2026-09-25 生产状态补证：共享事件弹窗的流星雨逐夜日期条仅在已有观测位置、能够发起当地条件查询时提供；无观测上下文时展示常年参考日（UTC）和取得位置后的提示，不把不可查询的日期写成当地观测日期。官方当前仓库 WEAPP 模拟器在无上下文与恢复上下文两种状态查看了实际页面；有上下文时切换日期，观测日期和几何时段更新，地图原观测上下文未被改写。此为现有采用产品语义的修复和局部运行时证据，不更改视觉资源采用范围；真机与其它数据状态仍按任务记录单列。

同一边界续查：无观测位置的日月食详情现在使用目录“食甚日期（北京时间）”，不以默认时区冒称“事件当地日期”；有地点时仍呈实际当地日期和食相。官方当前仓库 WEAPP 在本机隔离无上下文状态查看日全食原始画面并恢复原会话，单例测试和证据见任务记录。采用范围不变。

2026-09-26 兼容行为补证：当前源码的隔离内存服务预置两个历史事件关联，官方仓库 WEAPP 经“详情→编辑”实际验证了取消弹窗保留关联、只改备注保存后仍保留两项、确认单选并保存后替换为一项、确认清除并保存后变为零项。另通过目录发布流程移除其中一个事件，复现并修复了旧计划仅改备注也被服务端拒绝的问题；修后退出目录的事件显示资料暂不可用，详情及编辑页说明原关联保留、不显示内部事件ID，原关联仍可随其他字段保存，新选择仍须属于当前目录。两批均从 API 独立读回，地点、观测时段和出发安排保持；弹窗确认本身不提前写入已保存计划。此为历史多关联合同的局部模拟器证据，不代表正式持久化、真机或整页视觉验收，未扩大视觉采用范围。
