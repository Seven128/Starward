# 拟议差异与采用前缺项

本资源是待评审候选；仅有已选 B 的方向延续，不等于每项具体值已获采用。生产代码、DESIGN.md 精确 token 与页面采用入口未迁移。

## 视觉表达

延续白色贴底抽屉、平铺事实、轻分隔与深色云观星动作。地点标题19px、章节文字14px及其他字号/颜色/几何的精确值均可在本轮原始 HTML 查询；这些候选值不能反向覆盖 DESIGN.md。导航使用淡底区域，三项底部动作分别呈现；应由用户实际查看判断是否保留，而非视为全部细节已经确认。

Stitch 使用 Font Awesome、平台字体回退及 Tailwind 色值/模糊效果，与生产语义图标及视觉 token owner 存在差异。采用前应将决定收敛到共享规范和现有组件，不能引入第二套生产图标/颜色权威。辅助状态和章节未选中文字偏淡需进一步处理，未宣称对比度全面通过。

## 确定问题

- 地图图片无可靠投影元数据，生成的 marker 仍落在海面。Stitch 对陆地定位的文字声明与实际画面不符。本页图只承载参考场景，开发需由正式点位坐标和真实地图 owner 决定相机及 marker，不复用截图像素坐标；采用前应取得对应真实地图参考。
- 技术副本已纠正默认输入框边框、署名位置和循环脉冲，并补足搜索和辅助操作命中区。原始导出仍保留这些原始缺陷，须区分文件用途。
- 当前只是一张中档静态文档截面。其他档位、有效媒体、主题、内容变化及交互状态仍由 Screen Contract 控制，未被本稿取代。章导航、把手、主导航等静态元素不能证明真实可操作性和无障碍语义。
- 字体/CDN依赖尚未收敛成可完全离线还原的最终包；需要真实目标平台的字体与布局核对。

## 采用方式

用户确认具体页面资源后，按项目 Context 中的资源规则同步处理相关差异与入口。先解决上面的准确性和资源缺项，再将确认的页状态记录为采用范围；未覆盖内容沿用现有 owner。不会为了这张图改掉地图/面板/天空的业务与交互要求。
