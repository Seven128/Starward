# 地图反馈11：重画流星与单圈淡化

390×844、日间标准字级、既有B紧凑尺度。只修改想去图形/动画/背景切换以及月相/气象数据组底色；业务字段不变，生产未迁移。

旧流星由多个独立绝对定位元素拼接，方向变化后尾迹与星体失去整体关系。本轮用同一32×28 SVG表达：主星中心(17,14)，尾迹从左上渐细延伸且根部在主星后；两颗小流星从属。标签仍在装饰上层。

移除旧rolling-star/tail/satellite节点，只有meteor.js控制旋转。进度约束0..1映射0..-360°，本次仅一次完整转圈（750ms）；取消回0，反转从live进度接管，不叠加圈数。用户上限最多三圈。背景进度独立控制360ms交叉淡化，transform恒none；主星/夜空稳定后显出附属流星，避免拖尾绕转或飞离星体。减少动态效果时直接切换。

月相和气象内组背景#fdfdfe（近白极浅灰）、border0，原文字不减淡。分享雾紫、云观星夜空继续沿用。

Stitch一次生成成功，无生成重试。新画板《地图·中档·想去流星重构与单圈淡入精修》，初始srcdoc仅动态壳，另导出完整已渲染DOM并真实预览；两者都保存。Stitch仍延续旧点位、设施/Tab等业务偏差，未照搬。

源码meteor.js/css、scenes.js/css；精确视觉参数仍待评审，明确规则已同步Context/DESIGN。
