# 观星点信息组件 · 三档已采用

2026-09-08 用户确认“小、中、大三个可以收工”。采用反馈13最终修正版，包含顺时针一圈及副星避开主拖尾的最后修正。2026-09-15外部能力方案继续采用这套布局，但更新了其中的数据语义：天气只显示和风实际返回的小时与总云量，分层云/模型一致性退出；缺失范围由问号说明；近期天气另列前1–2个地区自然日事实与有条件影响；无覆盖使用统一暂无数据，异常同时提供顶部通知与局部重试。生产实现与验证状态以当前任务记录为准。唯一地图采用入口为[ADOPTED.md](../../ADOPTED.md)。

## 开发必须读取

- [可运行、可编辑原型](preview/index.html?extent=medium)：同一套HTML/CSS/JS，无需重新生成；small、medium、large通过extent参数选择。开发严格还原这些已采用资源，并遵循Screen Contract的真实数据/交互边界。
- 基准截图：[小档](reference/small.jpg)、[中档](reference/medium.jpg)、[大档](reference/large.jpg)、[大档多图](reference/large-media.jpg)、[天文](reference/astronomy-shared.jpg)、[想去选中](reference/favorite-active.jpg)。390×844逻辑视口，截图原样保存；实际编码尺寸由文件给出。
- 动效与图标：preview/meteor.js和meteor.css；背景/动作顺序：preview/scenes.js；相册/拖区：preview/gestures.js；章节/日期：preview/chapters.js；最终样式同时受其余级联CSS控制，不只复制最后一个文件。
- [来源、提示词与历史验证](provenance/README.md)。采用对象为最终Codex交互修订；Stitch原稿和更早反馈稿属于来源，不是另一份当前开发基准。

## 已确认内容与动效

三档为同一内容树，只改变可见高度；白色紧凑全宽拖区跟随名称滚出，大档顶部直角。图片仅大档按存在性展开；单图宽幅、多图横拖，设施两张无框照片卡上下排列。共享查看器图片/左右箭头垂直居中、文字在下；拖图时隐藏辅助内容，支持翻页、下拉缩小及原路返回。

基本信息首屏隐藏章节导航，到天文边界再显现；只一级同文档章节，第二章起大标题置于卡片外。月相/气象为全宽白卡内的紧凑同类数据组，内底#FBFBFC、无框；月相亮黄/灰，每个有效最小刻度下方显示对应图标。日期目标−7/+15观测夜，天文与计划时间保持可选；天气时间尺只留下真实返回小时，不补内部缺口。问号说明数据范围原因，不把无天气小时写成无天文数据。

底部顺序想去/云观星/分享，三按钮可见高32px、命中高44px、图文间距6px。默认浅蓝天白云，云观星居中使用浅夜空，分享浅暖杏；全部无框。想去激活立即顺时针恰好360°，820ms微缩至.94；两颗副流星位移冲入并渐显，不等待背景；主星黄绿渐细拖尾，副星头部避开主尾迹。取消接管实时进度逆向返回；背景仅原位交叉淡化，两个夜空的稀疏星点不同步明暗，减少动态效果静态呈现。

## 事实与验证边界

有正式记录才有面板；完整/部分信息按真实字段呈现，未知不等于没有设施，示意值不得成为生产fallback。地点/气象样例为隔离假数据；图片仅为参考素材，不是该点实拍；示意地图、海面点标、模拟系统栏和原生胶囊不属于生产像素还原对象。素材许可/来源随资源保留。

与设备相关的字体、安全区和原生显示须映射实际WEAPP；其他屏宽保持字号和控件尺度，以容器自适应，不能缩放整张图。按当前数据/状态和固定视口比较真实WEAPP截图，核对布局、字体、颜色、图标、图片和动态行为。样例的固定高度、日期、媒体数量不是业务上限。网页动效/截图检查不等于生产或真机验证；服务覆盖、授权/分享/导航、失败恢复、可访问性仍按现有owner实现。

本次仅收敛已确认组件，不替代未评审页面、地图图层或夜间/观测模式设计。

## 公共日期时间组件

2026-09-08按用户要求，将原型日期/日历/时间尺提取到 [共用资源](../../../shared/observation-time/README.md)，本原型与图层预览同时使用。已采用的字号、曲率与月相顺序保留；底部说明不再重复中心时间，窄屏日期目标扩足44px。原先页面内重复的日历/手势与样式已移除。当前天文截图为 `reference/astronomy-shared.jpg`；原 `astronomy.jpg` 仅保留提取前参考，其他档位基准不变。生产现用共享日期与时间尺组件，具体实现与验证见共用资源和本轮任务记录。

2026-09-24 生产天文章节按报告有效性呈现月相、气象及目标；报告请求失败且无旧值时保留错误与重试，并继续显示独立的空气质量、卫星夜光。官方WEAPP模拟器通过定向接口故障及恢复抽样，原图 `sky-panel-no-report-failure.png`、`sky-panel-report-recovered.png` 位于任务记录目录。真机、其他宽度和供应商正向AQ仍未验证；不将本次实现修复视为新视觉候选已采用。
