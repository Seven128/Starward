# 采用时核对

2026-09-08：采用反馈13最后方向/避让修正版。已从当前网页重新记录小/中/大、大档多图和天文截图，并检查真实构图；选中态截图来自同一最终修正源码。390×844逻辑视口，颜色/按钮状态测量见reference/baseline.json。

采用包preview复制最终可编辑HTML/CSS/JS与素材，直接运行无前序构建依赖；生产不可复制示意服务行为或数据。来源完整生成链与原稿在反馈13及其历史目录。motion/direction实际采样保留在来源outputs/evidence，包含历史记录与修正标识。

README/review/preview入口的本地链接检查无缺失。npm run context:validate与git diff --check通过；前者仅验证清单路径和声明。已核对当前Context覆盖正式记录/部分字段、最小数据提交与发布区分、章节/媒体/动作、月相和−7/+15日期边界；原有接口/生产迁移缺口如实保留。

本次没有WEAPP生产迁移或真实手机运行验证。Stitch项目只改名为历史探索，未永久删除其中原稿；仓库采用包为当前唯一组件设计依据。
