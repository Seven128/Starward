# 实际检查

Browser 插件真实页面：390×844 请求视口（保存的原始 JPEG 为390×843）。检查了光污染/总云量视觉、顶部圆角、时间区从可访问树移除、单选、切换恢复、日历 Escape、连续切层和面板 Escape。

两层选项卡 top 均为725.95px；光污染时间区高度0。按钮可见面36px，独立命中44px；面板外层overflow:hidden、18px顶角。先前42.4px伪元素命中已改为明确44px。

9月9日、时间index7在光污染往返后保持；日历Escape只关闭日历并返回日期入口，下一次Escape关闭图层并回到trigger。连续切换最后保持总云量唯一选中。

采用来源的Stitch原稿真实渲染与导出图已查看，残留选中marker、额外图标、外壳和独立静态尺均不作为交互稿组件来源。交互稿继续使用已采用观星点与feedback-01共享的日期时间实现，未新增依赖、未改公共组件代码。

`node --check`、`git diff --check`、`validate-context`通过；Context命令只验证其声明范围，不证明事实或产品正确性。没有宣称生产地图、真机动效、320px或读屏软件实测通过。


## 公共时间轴后续修订

新增220ms连续吸附与中断重抓；观星点采用资源同步使用。移除了观星点事实更新反向写尺位置/语义的重复控制。日期栏/日历随切片的实际当地日期跨午夜更新，内部夜分组不变；日历选择按实际日期和原钟表时间反查样本。图层卡片补充灯泡/云朵图标，用户标签改为“云量”，TOTAL_CLOUD键和数据语义不变。本次在已有资源直接修复，未新增Stitch生成或改原稿。

实测：图层25px非整格拖动释放，轨道横移-454 → -452.366 → -439.818 → -429px；带月相观星点同样为-1592 → -1593.47 → -1612.39px向-1617px吸附，消费者未打断公共动画。重抓中从-472.348到下一帧-476.45px接续，无先跳到-495px目标；取消恢复index7。减少动态效果下释放直接-495px，浏览器仿真设置已还原。9/8 23:30 → 9/9 00:00，日期与日历高亮一致；日历再选9/10得到9/10 00:00、归属9/9观测夜。观星点06:00对应9/9日期及月面照明6.3%。检查基于浏览器UI/CDP输入与真实DOM读取，不代表WEAPP真机验证。

正式采用整理：保留最终可编辑稿和两张390×843 JPEG参考，迁移后复查本地资源链接及浏览器交互；旧候选不再作为采用依据。
