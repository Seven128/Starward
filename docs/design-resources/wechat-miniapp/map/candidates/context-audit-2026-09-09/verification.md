# 实际验证记录

2026-09-09，仅设计浏览器演示。

- 实际查看采用表单/地图参考、两张Stitch原稿、最终四宽320/375/390/430审核中截图、长名称320、确认与图层截图。
- 确认前地图背景transform保持；继续编辑保留名称、照片数量与选定地址，surface仍为spot-editor；放弃后才切到另一地点。
- 草稿仅编辑；审核中动作只有云观星。审核通过后提案仍存在且未公开；发布新点后原提案撤下，正式点恢复想去等动作，重复回执不重复标记。
- 合并期间天空打开，返回正式bay；proposal标记移除，bay仅一个，重复receipt后历史快照相同。
- 保存失败保留输入，重试成功后关闭重开回填；提交失败保留输入，重试后进入审核中。
- 图层打开时信息面板隐藏，切回地点时图层移除；嵌入图层的重复homebar已移除。430面板固定390的旧限制已在候选适配，实测所有宽度下内层面板与外层视口等宽。
- 地图离屏标记允许在clip容器外，容器scrollWidth并非页面溢出；检查body与视口等宽、内层面板等宽。图钉长文本沿原资源展示，可能在地图边缘裁切，完整地点名在面板换行及无障碍名称中保留。

可复现检查：任务索引目录的 check-map-browser.js、check-map-final.js；提交失败重试补查记录于PROGRESS。构建器build-map-review.mjs仅改候选；JS语法和git diff --check通过。浏览器favicon403不影响资源。

截图：[320](reference/map-pending-320.png)、[375](reference/map-pending-375.png)、[390](reference/map-pending-390.png)、[430](reference/map-pending-430.png)、[长名称](reference/map-long-320.png)、[确认](reference/map-confirm.png)、[图层](reference/map-layer.png)。

未验证WEAPP原生地图叠层/拖动/键盘/系统返回与安全区、真实认证与存储、服务器幂等和发布事务、真实天文数据或通知。上述生产验证仍依Context约束另行开发验收，不因本演示通过而标记完成。

## 第二轮检查

小档下拖不动不关闭，上拖展开；中档可滚动；首次打开与切换待内容准备后显示；大档顶部消除可误触地图缝隙。

本轮针对性浏览器脚本：`check-map-feedback.js`（任务材料目录）。搜索/地图四宽度已检查；My居中与Plan勾选已进行几何和交互检查。仅证明候选浏览器原型，非WEAPP真机结果。

## 第三轮实际检查

无顶部图片时大尺寸也保留顶部两角；圆角外像素不再穿透为地图关闭。保留此前无闪烁替换与小档下拖不关闭。此项是当前采用几何在用户明确范围内的本地候选修正，沿用既有 Stitch 构图。

在本地设计预览完成 320/375/390/430 宽度检查与截图审视。Search 保留时间/公里参数确认、取消与越界反馈；My 检查 badge/结束时间几何中心；Sky 检查单击、居中、来源/无介绍、长内容滚动、红光和 Escape；Map 检查四宽度无图圆角与角落点击不关闭，并回归上一轮切换/拖动/滚动。未进行 WEAPP 真机、生产资料接口或大星表点选测试。
