# 观星计划 · 当前采用资源

> 2026-09-23：对应 Stitch 旧版本项目已删除；本文件声明的当前采用源及必要依赖继续有效。历史项目 ID 只用于来源追溯，续改从[当前 Stitch 状态与入口](../stitch-status.md)核对。

本文件是唯一有效采用入口。当前完整消费者为[2026-09-13完整观星计划](revisions/three-requirements-2026-09-13/README.md)，已获用户确认；下方既有源只约束尚未变更区域并提供必要共享依赖。生产实现、真实服务与真机验证分别记录，不从浏览器演示推断。

2026-09-24 代表视口补查：官方WEAPP当前仓库包在逻辑320×700与430×932，从“我的→观星计划→接下来真空→新建”均显示公共整页空态及正式点、时间、出发字段，固定取消/保存可见；320宽继续滚动到事件、提醒、备注，底部操作坐标始终固定。当前账户未来计划确为0、过去计划1，故此空态只是当前数据样本；不证明其它列表状态、Picker原生选项、正式持久化、主题或真机。原始截图与运行时逻辑宽度见本任务A14用例记录，采用状态不变。

2026-09-24 生产接入补查：正式观星点信息抽屉此前未挂载已有的 `SpotPlanEntry`，使本入口约定的“无计划→去这观星／一条→详情／多条→本地点计划列表”不可到达。现已在点位概览挂载公共入口；本机内存测试服务与官方 WEAPP 模拟器实际看到同一点两条计划进入 `spotId` 筛选列表并返回原地图选点。后来以仅改本机 GET 响应的受控零条/一条数据，在实际 WEAPP 页面分别验证入口文案及进入预选点位新建页/对应计划详情；正式持久化和真机仍无证据。已采用预览要求的全宽浅色带现由点位滚动容器保留入口全宽、其他章节仍保持文档内距；官方 WEAPP 390×844 视口测得入口宽390、高60，并看到场地、地形、天文继续滚动及多条计划来源返回。带照片的点位变体和其他视口仍待目标运行时检查，本记录不扩大设计采用范围，也不把局部几何对齐写成整页视觉验收。

2026-09-24 计划编辑补查：从“我的→全部观星计划→新建”进入独立编辑页已在当前仓库构建的官方 WEAPP 模拟器验证，表单显示正式观星点与时间、出发、事件、提醒和备注。新建页没有已选 Map Observation Context 时，生产代码现从当前地图视野建立仅供编辑的地点/日期/时区上下文，再读取可选正式点；空列表提供返回地图，失败提供重试。进一步只将本机测试会话持久状态中的 Observation Context 暂时置空并重新编译当前项目，官方 WEAPP 中从 My 新建页实际显示当天日期和“请选择正式观星点”，没有把地图中心冒充已选正式点；原始图 `plan-my-new-no-context.png` 保存在任务证据，随后原字段回读恢复并复测有 Context 的原入口。该抽样证明模拟器中的无 Context 渲染路径，不证明原生 Picker、保存持久化或真机。此为采用资源的生产状态记录，不改变采用范围。

2026-09-24 补充独立新建页未保存输入的原生确认抽样：当前官方开发器真实窗口可见“离开编辑？”系统弹窗；“继续编辑”后输入保留，重新触发后选“离开”返回我的，远端计划列表没有新增记录，本次测试专属本地草稿已精确清理。原始387×836裁切在任务证据`plan-dirty-modal-continue.png`/`plan-dirty-modal-leave.png`，不替代真机。正式点 Picker 在本机开发器通过 automator 与窗口鼠标点击均未出现原生选项层，只确认当前表单本体可见；原生选项/取消仍未验证。

2026-09-24 编辑页空错补查：本机只读代理分别把计划页正式点列表响应改成真空结果与503，官方 WEAPP 原图显示空态和错误状态分开。空态不再重复一行“暂无可选正式观星点”或保留可点的保存按钮；文案明确当前不能创建，返回地图只承诺退出本页。503状态显示“重试”，服务恢复后点击重新读到实际正式点，保存按钮恢复。最终默认8787产物已重新构建并打开当前仓库项目，正常My新建仍预选原点位。该证据属于本机受控响应的模拟器抽样，不证明正式服务自然产生零点位，也不改变资源采用范围。

2026-09-24 编辑字段校验补查：官方 WEAPP 从“我的→观星计划→新建”直接点击保存，原悬浮错误覆盖地点标题和选择行；现将地点、时间、出发地与提醒清单的输入校验放回对应表单位置，并滚动到错误字段。390×844 模拟器中缺少结束/出发时间的实测错误位于时间控件上方，未再弹出覆盖地点的悬浮消息；滚离后再次保存也重新定位到同一字段。其他校验分支、原生选择层和真机仍待目标运行时逐项验证。采用范围未改变。

2026-09-24 出发地语义补查：地图上下文的 route origin 可以只是视野中心，旧新建页却把“当前地图中心”自动填成出发地，并通过实际出发地必填校验。新建计划现默认留空，必须由用户手填或显式通过微信地图选择；已有计划和本机草稿保留原值。官方 WEAPP 受控移除并原样恢复本机新建草稿后，独立新建页显示“填写实际出发地”，补齐有效时间但不填出发地时在该字段提示且未发保存请求。原生选点与正式持久化仍待验证；采用范围未改变。

2026-09-24 旧草稿补查：历史自动写入的出发地仍按原文展示，不静默删草稿；但若没有用户显式编辑/选点记录且内容等于地图 route origin 或“当前地图中心／本次授权位置”，保存前要求重新填写或选点。官方 WEAPP 用原有本机草稿补齐时间后实测字段旁错误，手动填具体地点后错误清除；草稿随后原样恢复。该抽样未执行真正的新计划持久化，也不证明原生微信地图选择。

2026-09-13局部修订：[关联事件改为共享单选Modal](event-modal-2026-09-13/README.md)。用户已明确承载与单选要求，新增视觉已确认采用并迁移到计划生产编辑/只读入口；其余计划布局与交互继续沿用下方已采用资源。Modal临时选择不会改写地点/时段，确认只回填草稿，计划保存仍由原owner持久化。


## 2026-09-09 本对话修订收敛

当前局部依据：[计划独立编辑、到达冲突提示、提醒异常与正常居中清单勾选](candidates/context-audit-2026-09-09/CURRENT.md)。本轮最终修订在该范围替换上方旧稿；其余已采用区域保持有效。原始目录路径保留用于来源追溯，采用状态以本入口为准。共享Modal及关联字段已完成生产迁移与WEAPP模拟器交互验证；真机事件Modal仍需单列验证。

## 本轮完整增量资源

[2026-09-13完整观星计划修订](revisions/three-requirements-2026-09-13/README.md)基于上方2026-09-09采用源增量修改，保留原完整布局和交互。原简化宿主被用户否定，不可再作为当前页面依据；新修订已于2026-09-13由用户确认采用，共享事件Modal、公共Tab和B批图标已迁移到生产消费者。

