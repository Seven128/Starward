# I21 implementation binding R11

This immutable technical binding preserves the R10-selected canonical resources,
complete Fact manifest, Inspector, Oracle, environment and Design Authority.
R10 remains unchanged. Only the `source.miniapp.sky` implementation digest and
the corresponding feasibility/handoff input identities change.

The production sky owner clears its heading change-filter baseline when compass
or motion samples expire or a motion sample is invalid. An identical fresh
heading can therefore restore the existing sensor-following view. No projection,
permission, source-data, visual Fact, condition or proof method is changed.

Authority Delta Assessment: `consistent_with_current_authority`, with no observed
design variance. DESIGN.md section 6.12 and the owning sky surface's recovery and
foreground-lifecycle rules remain controlling. No new system precedent is added.

The bundle's complete descriptor is 1,610,929 UTF-8 bytes; publication uses a
2,097,152-byte ceiling without trimming any Fact or obligation. Bundle/preflight
proves input integrity only, not production rendering or device alignment.
Adoption remains with the same Long Task protected Authority Revision.
