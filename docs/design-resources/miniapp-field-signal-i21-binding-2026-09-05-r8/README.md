# I21 implementation binding R8

This immutable technical binding preserves the complete selected R6 canonical resources, Fact manifest, Inspector, Oracle, environment and Design Authority identities. It does not copy or regenerate that canonical closure or overwrite R7.

Only the current `source.miniapp.sky` digest changes in the feasibility input. The approved product/technical Source now requires a pointing-window scene: the renderer consumes the shared perspective projection, uses one basis for stars, targets and world-space guides, and does not clamp off-frustum objects to screen edges. The separate product/technical Source and owning Screen Contract retain those requirements and physical-verification boundaries; this binding introduces no selected visual Fact, condition, proof-method or permitted-realization change.

Published with the existing R6 manifest and a 2,000,000-byte handoff ceiling. Bundle publication validates input integrity only; production conformance and physical alignment remain unverified.

Authority Delta Assessment: `consistent_with_current_authority`; the existing R6 assessment and unchanged Design Authority closure remain referenced. No system adoption or new design precedent is introduced.

Downstream adoption remains owned by the same Long Task protected Authority Revision. This resource alone does not accept the implementation or fulfill any device obligation.
