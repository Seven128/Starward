# 当前小程序设计资源

当前资源仅在 [wechat-miniapp](wechat-miniapp/README.md)。2026-09-09逐页采用完成后，旧miniapp-*目录被移除；历史设计、I21绑定/校验闭包只通过Git历史回看，不再是当前实现输入。

生产中的历史delivery-carrier、媒体提取manifest与冻结验证spec描述旧来源，并不证明新UI已迁移。需要历史回放时检出对应旧提交，不能把新资源路径填进旧哈希以伪造通过。运行代码/已提取素材仍保留，未因设计资源清理删除生产能力。
