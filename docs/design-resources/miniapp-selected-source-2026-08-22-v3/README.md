# 今晚去观星 · Mini Program selected design source v3

本目录是 2026-08-22 最终选择后生成的不可变、manifest-backed 设计约束 Source。`index.html` 保留 Open Design 最终候选的精确字节；`provider/` 保存生成时 README、滚动 manifest、brand spec 与 sidecar provenance。选定状态、最终需求、方案对账和 QA 分别由本目录的 selected 文件记录。

本目录不是生产实现，也不是像素 exact target。精确视觉 token 仍归 `DESIGN.md`；正式 handoff/preflight 只证明 Source 闭包。
