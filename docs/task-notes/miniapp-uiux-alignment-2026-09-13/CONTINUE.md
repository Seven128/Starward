# 新对话继续开发说明

更新于2026-09-13。本次交接提交已有实现、设计资源、Context与交付治理修订；**产品尚未完成，不能把提交或推送成功当作验收。** 本次交接没有重新运行手机预检或修复UI。下一对话先执行下述人工卡点预检，再开始产品开发。

## 1. 用户目标、授权与恢复入口

继续完整实施[原始需求](../../requirements/miniapp-uiux-alignment-2026-09-13.md)：真实地形、最新B批图标整体迁移、共享天文事件Modal、公共Tab动效，以及整个小程序UIUX、产品逻辑、架构和实现Context对齐与缺失功能补开发。基于已有采用界面增量修复，保留完整有效内容和交互。不能只交审计报告，也不能仅修当前两个可见问题就宣布原需求完成。

用户将离开电脑，手机已经架好并对着电脑屏幕。用户已明确授权本任务需要的电脑和手机操作，包括打开/信任微信开发者工具、USB/无线调试连接、使用手机微信扫一扫电脑上的二维码，以及已授权范围内的本地开发、调试、设置和验证。**授权持续有效，不要反复请求同一许可。** 先自行执行能完成的步骤；只有实际无法代办的锁屏解锁、外部MFA/人工审批等才报告最小人工动作，不能把“可能需要”当作阻塞。此次授权不扩大原需求到无关产品、购买或生产发布，也不授权向无关联系人发消息。

用户要求Goal和可恢复索引，不使用长程任务工作流自举。原Goal曾错误标为complete，完成结论已撤回；继续时检查Goal实际状态，承接未完成目标，并在目标文本中引用本文件、[INDEX](INDEX.md)、[coverage](coverage.md)、[progress](progress.md)和原始需求。若需要新建承接Goal，保留原完成结论被撤回的事实，不能丢掉剩余义务。任务临时记录继续放本目录，持久事实只更新原Context owner。

恢复顺序：本文件 → INDEX权威入口 → 原始需求全文 → coverage全部41项 → progress更正及历史记录 → [治理续修](../../../.codex/work-items/development-quality-drift-2026-09-12/follow-up-2026-09-13.md)。coverage历史“完成”标签只代表其写明的检查层，不是独立复核；有反例必须重新打开对应条目。

## 2. 开发前实际跑通人工卡点

先读当前[真机技能](../../../.codex/skills/starward-wechat-device-verification/SKILL.md)、[真机owner](../../../project_context/areas/main/verification/wechat-device.md)及全局wechatide技能，再使用现有官方开发/真机反馈入口。读取当前状态优先，下面的环境记录都不是仍然可用的保证。

1. **电脑会话与开发工具**：检查项目、登录态、AppID、CLI服务/自动化端口与已有进程归属。复用仍可用的登录和服务；实际打开项目，处理项目信任、服务端口授权或必要登录，不停留在“已安装”。根据当前技能选择打开/自动化顺序，避免重复启动项目导致获取AppID卡住。
2. **手机可控与可见**：新鲜发现USB或已配对的无线ADB，实际执行有超时的shell、获取手机截图、确认唤醒/亮屏/锁屏/前台状态和可输入性。连接列表出现device不代表可以操控。优先复用配对，不沿用历史配对码、IP或端口；需要重配且必须人工完成时现在处理。每次输入依据当前画面，不盲用旧坐标。
3. **扫码端到端演练**：通过当前官方流程为明确的一次构建生成新预览/调试二维码，把二维码真实显示在电脑屏幕前台；用架好的手机打开微信扫一扫并实际扫一次。历史经验：Codex可能遮住二维码；用户曾通过“先返回Windows桌面，再单独激活微信开发者工具”解决。启动窗口或工具截图不证明物理屏幕已露出二维码，必须观察再扫描。
4. **手机确实进入当前小程序**：确认扫码后的页面、对应构建身份及一项实际交互；排除旧缓存包、只有扫码成功提示、调试连接成功但页面未加载。二维码过期才重新生成；二维码仍有效而远程命令超时，不要不断重建/重扫。结构化真机自动化失败但画面正常时，按技能降级到已授权的截图与ADB操作，并明确证据层。
5. **服务和网络也预检**：按现有workflow启动需要的API/fixture，检查手机能访问当前真实地址与地形资源；手机不能使用电脑localhost。现场发现WLAN地址、监听和路由。多网卡/VPN选错二维码LAN地址时优先官方切换实际WLAN，按需处理明确的防火墙范围，不能先大范围改网络。fixture缺真实数据不能被页面加载成功掩盖。
6. **无人值守持续条件**：核实供电、屏幕/锁屏设置和扫码视线能支持后续工作；必要本地设置按已有授权操作，并在本机私有位置保存原值用于恢复。涉及权限弹窗、登录确认、工具升级要求等实际走一次。预检只有完成“电脑生成 → 手机扫描 → 当前包加载 → 实际交互/服务响应”才算该链路已跑通。

预检记录只写实际通过/失败和下一动作，不保存配对码、设备标识、私人聊天、登录二维码或token。若出现必须用户操作的真实卡点，尽早一次说明，同时继续不依赖它的工作；用户离开且卡点未解决时，不冒称真机验证。现在交接没有证明这些步骤已经通过。

## 3. 已有实现与已知缺陷

已有代码不必重写：SemanticAsset的B资产映射/分包派生、SelectionTabs、AstronomicalEventModal及旧路由兼容宿主、NativeBackBoundary、地形管道/BFF契约/客户端缓存与原生GroundOverlay均已接入。实际边界和代码入口见progress、INDEX与相关Context。

两个已确认但尚未修复的缺陷：

- `apps/wechat-miniapp/src/components/semantic-asset.scss`：B类隐藏旧伪元素的规则被后续同优先级通用伪元素规则覆盖；部分消费者同时带旧图标名类，造成新PNG与旧绘制叠加。检查最终组合中的伪元素、背景、边框及消费者覆盖，修复共享绘制责任，保留没有B素材的合法主题路径。
- `apps/wechat-miniapp/src/pages/map/index.scss`：`.map-tool`外层是方形命中区，圆形视觉表面在伪元素；`.map-tool--layer-active`把浅色背景加到了外层，出现超出圆形的方块。核对default/pressed/active及平台默认值，把状态反馈落到正确表面，保留44px命中责任。

历史图像和源码自查记录在[delivery-reassessment](delivery-reassessment-2026-09-13.json)。该记录是原作者自查，两个failed和一个unverified；不能称独立审查或新候选验收。两张缺陷截图已保留供远端恢复，其余原始输出/二维码/地形下载缓存仅在本机，受.gitignore保护；progress里的其他output路径可能无法在新机器读取，缺失时重新采集。

## 4. 以收益和成本安排修复及验证

先读采用资源并实际查看当前图像、可编辑原型和状态说明，再读对应代码/消费者。普通还原修复复用已有采用稿；不要重新生成设计、运行历史改稿脚本或改低要求来迁就现状。

1. **先证明共享机制的一个真实结果**：从上述缺陷涉及的共享绘制和表面状态入手，先在当前WEAPP观察、修复、对照采用资源，再扩展到有实质差异的消费者。检查整帧及状态变化，避免只盯被修的局部。
2. **按机制覆盖，而非逐个穷举**：以渲染路径（位图/旧CSS/原生Tab/marker）、消费者覆盖、主题、原生与普通容器、状态owner、返回/取消/重开等实际差异选代表。相同机制可复用证据；新差异或新失败才扩展。已逃逸缺陷用修复前失败或有界变异证明检查能抓住错误，随后复用。不要为每个图标和全部状态笛卡尔积造测试，也不要用CSS字串存在证明视觉正确。
3. **继续完整产品义务**：依据coverage和Screen Contracts检查Map/Search/点位/图层、My、计划、贡献反馈、Sky、设置/账户/权限/恢复。对产品和技术责任检查入口、消费者、唯一状态/数据owner、持久化与取消/晚响应/失败边界；发现真实缺失实际补开发。把已落地事实与未观察行为分开，不因测试多就跳过实际输出。
4. **集中处理数据与集成缺口**：当前GLO30只覆盖大湾区中心85km，1536²派生、约110.7m分辨率、99.34%有效；SRTM仅备选，没有自动fallback。核对覆盖之外的真实缺测、坐标/半径/图例、原生叠加与既有VIIRS。当前手机fixture没有真实VIIRS格网，不能把彩色占位或API测试当作光污染真机证据。
5. **共享行为按有意义差异复核**：Map事件浏览不请求私人计划；Plan单选临时态、确认回填和保存持久化分层；旧多关联/深链兼容；同一Modal内list/detail、系统Back、关闭中拦截/快速重开/减少动态效果；公共Tab的呈现变化不另造业务选中态。保留地图相机、时间、bottomPresentation和脏草稿。
6. **审查必须改变后续动作**：按当前AGENTS对高影响共享复用/大交付取得独立审查，直接提供要求与当前实际输出，不能仅提供作者完成摘要。缺观察就观察，已知差异就修；自查不冒称独立审查。复用现有任务笔记和测试，不新增普遍强制表单、全项目评分或审批阶段。

最终检查绑定当前源码/构建/运行包/资源和相应状态，尤其防止开发者工具预览缓存。通过当前workflow的prepared generation或明确候选身份保证一致，不靠随意复制dist后宣称正式候选。修复后重跑受影响检查；无新变更/失败/疑点不重复扩大验证。当前源级探针明确输出`product_conformance: unverified`；审查工具退出0只证明记录结构有效，follow_up为空也不代表覆盖完整。

## 5. 当前不能声称已验证的范围

- 手机UI已知失败未修；整体UIUX符合性、所有义务完成、独立执行者复核均未成立。
- 历史320/375/390/430和820模拟器采集不等于视觉验收；iOS与物理平板未测。
- Android曾观察系统Back的详情→列表→Map及图层返回、部分地图叠加；快速反向/重开、读屏焦点、减少动态效果、前后台和全部账户/权限组合未逐项物理验证。
- 真实VIIRS手机显示、生产账号到审核后台的完整链路、通知供应商真实投递、物理传感器专项未完成；未真实删除用户账户。继续时按义务和风险确定需要的实证，不能为了覆盖率执行无必要的破坏性操作。
- 历史`check:miniapp:fast`曾通过：24契约、176 API（另2跳过）、443前端、137 workflow；治理修订后workflow为140，定向审查工具12测试通过。它们是历史分层结果，不证明新修复/当前设备，也不代替视觉比较。

必要义务缺证据时保持未完成，明确具体原因与可继续工作；不能“总体完成”后把核心缺陷放到限制附录。只有剩余必要工作确实处理完才关闭Goal。

## 6. 本机环境线索与恢复

- 仓库`E:/Dev/Starward`，main，origin为`https://github.com/Seven128/Starward.git`。继续前读git状态，保留新的用户改动；不自动建分支/worktree。
- Node需要24+；已知可用`C:/Users/777/AppData/Local/nvm/v24.16.0/node.exe`。PATH可能优先命中微信Node16，先核实；npm脚本走仓库`tools/run-node.cjs`。
- 开发器CLI历史安装于`E:/微信web开发者工具/cli.bat`；ADB于`C:/Users/777/AppData/Local/Android/Sdk/platform-tools/adb.exe`。当前技能记录的端口归属/启动顺序优先于旧笔记，进程ID、LAN地址和配对端点现场发现。
- 上轮API fixture已停止，下一次需启动。源项目曾保留打开；旧唯一副本已回收，不可直接复用旧二维码。先按当前workflow识别warm session/生成身份。
- 历史stay-awake原值为15，曾恢复；私有恢复文件位于`C:/Users/777/AppData/Local/Starward/wechat-device-prep-2026-09-13/restore.json`。历史Windows代理已恢复。新会话先读现状再记录自己的恢复基线，不照旧值覆盖新设置。
- 二维码、配对信息、下载缓存、原始桌面/聊天截图及构建副本不进Git；保留本机即可。当前采用设计源、生产地形发布PNG/manifest、代码和任务索引应随仓库可恢复。

## 可直接发送的新对话提示

请继续完整实施`E:/Dev/Starward/docs/requirements/miniapp-uiux-alignment-2026-09-13.md`，先读取`E:/Dev/Starward/docs/task-notes/miniapp-uiux-alignment-2026-09-13/CONTINUE.md`及其恢复索引，开Goal承接尚未完成的工作并在目标文本中索引这些文件，不用长程任务工作流自举。原完成结论已撤回，已知UI缺陷未修，请实际修复并落实已有的高效率交付质量治理，完成真实WEAPP验证，明确未验证部分。

我会离开电脑，手机已经架好对着屏幕；我已授权本任务需要的电脑和手机操作，包括开发器信任、调试连接、手机微信扫一扫屏幕二维码等。开发之前先实际跑通可能需要人工操作的环节：工具登录与信任、手机连接和可控、二维码在电脑前台可见、手机扫码进入当前构建以及服务可达。能自行完成的直接做，不要重复确认；确实必须我操作的现在集中提出。预检后继续完整需求，保留原内容和交互，不能只交报告或只修两个表面问题。
