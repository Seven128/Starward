# Starward native adapters

This directory is the Expo Modules owner boundary for platform-specific maps, orientation, AR, brightness and other high-frequency native capabilities. JavaScript features consume typed ports from `src/native`; native implementations are added only when the corresponding capability Outcome is implemented and verified.
