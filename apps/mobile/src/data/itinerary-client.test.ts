import { describe, expect, it } from "vitest";
import { createItineraryClient } from "./itinerary-client";
describe("itinerary client", () => { it("loads and commands the versioned itinerary API", async () => { const calls: Array<[RequestInfo | URL, RequestInit | undefined]> = []; const fetcher = (async (input: RequestInfo | URL, init?: RequestInit) => { calls.push([input, init]); return new Response(JSON.stringify({ schemaVersion: "starward-itinerary-v1", itinerary: { revision: 7 } }), { status: 200 }); }) as typeof fetch; const client = createItineraryClient({ baseUrl: "http://api", fetcher }); await expect(client.get()).resolves.toMatchObject({ schemaVersion: "starward-itinerary-v1" }); await client.command("refresh"); expect(calls[1]?.[0]).toBe("http://api/v1/itineraries/commands"); expect(calls[1]?.[1]).toMatchObject({ method: "POST", body: JSON.stringify({ command: "refresh" }) }); }); });
