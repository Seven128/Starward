// Compatibility export for integrations that still import the historical
// application component. Route ownership lives exclusively in Expo Router.
export { default as WebApplication } from "./RuntimeRoot";
