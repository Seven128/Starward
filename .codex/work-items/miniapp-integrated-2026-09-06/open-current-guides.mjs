import automator from 'miniprogram-automator';
import {writeFile} from 'node:fs/promises';
const p=await automator.connect({wsEndpoint:'ws://127.0.0.1:9421'});
try {await p.evaluate(()=>{const page=getCurrentPages().at(-1);let sid,parent;function scan(n){if(!n||typeof n!=='object')return;if(n.ariaLabel==='查看全部攻略')sid=n.sid;if(n.cl?.startsWith('map-panel-layer'))parent=n.sid;for(const v of Object.values(n))if(typeof v==='object')scan(v);}scan(page.data);if(!sid)throw Error('guide_entry_missing');const target={id:sid,dataset:{sid}};page.eh({type:'tap',timeStamp:Date.now(),target,currentTarget:target,detail:{}});if(parent)page.eh({type:'tap',timeStamp:Date.now(),target,currentTarget:{id:parent,dataset:{sid:parent}},detail:{}});});await new Promise(r=>setTimeout(r,2000));const result=await p.evaluate(()=>{const page=getCurrentPages().at(-1);const labels=[];function scan(n){if(!n||typeof n!=='object')return;const label=n.ariaLabel||n['aria-label'];if(label)labels.push(label);for(const v of Object.values(n))if(typeof v==='object')scan(v);}scan(page.data);return{route:page.route,labels};});await writeFile('artifacts/miniapp/guide-entry-current.json',JSON.stringify(result,null,2));console.log(JSON.stringify(result));}finally{await p.disconnect();}
