import automator from 'miniprogram-automator';
import {writeFile, readFile} from 'node:fs/promises';
const {base}=JSON.parse(await readFile('artifacts/miniapp/current-map-probe-address.json','utf8'));
const p=await automator.connect({wsEndpoint:'ws://127.0.0.1:9421'});
const pause=ms=>new Promise(r=>setTimeout(r,ms));
async function tap(match){await p.evaluate(match=>{const page=getCurrentPages().at(-1);let sid,parent;function scan(n){if(!n||typeof n!=='object')return;if(n.ariaLabel?.startsWith(match))sid=n.sid;if(n.cl?.startsWith('map-panel-layer'))parent=n.sid;for(const v of Object.values(n))if(typeof v==='object')scan(v);}scan(page.data);if(!sid)throw Error('tick_missing:'+match);const target={id:sid,dataset:{sid}};page.eh({type:'tap',timeStamp:Date.now(),target,currentTarget:target,detail:{}});if(parent)page.eh({type:'tap',timeStamp:Date.now(),target,currentTarget:{id:parent,dataset:{sid:parent}},detail:{}});},match);}
async function read(){return p.evaluate(()=>{const page=getCurrentPages().at(-1),selected=[],text=[];function scan(n){if(!n||typeof n!=='object')return;if(typeof n.v==='string')text.push(n.v);if(n.ariaLabel&&(n.cl?.includes('tick--selected')||n.ariaLabel.includes('已选择')))selected.push(n.ariaLabel);for(const v of Object.values(n))if(typeof v==='object')scan(v);}scan(page.data);return{route:page.route,selected,text};});}
await p.evaluate(base=>{const original=wx.request;const state={count:0,restore:()=>{wx.request=original;}};globalThis.__newMapProbe=state;wx.request=function(options){if(String(options.url).startsWith('http://127.0.0.1:8879/v2/map/scene')){state.count++;const header={...options.header};delete header['If-None-Match'];return original.call(wx,{...options,header,url:String(options.url).replace('http://127.0.0.1:8879',base)});}return original.call(wx,options);};setTimeout(state.restore,25000);},base);
let result;
try{const before=await read();await tap('下午09:20，第');await pause(2500);const sky=await read();await p.evaluate(()=>{wx.navigateBack({});});await pause(1800);const map=await read();result={before,sky,map};}
finally{const count=await p.evaluate(()=>{const state=globalThis.__newMapProbe;state?.restore();delete globalThis.__newMapProbe;return state?.count;});if(result)result.newMapRequests=count;await p.evaluate(()=>{if(getCurrentPages().at(-1).route!=='pages/map/index')wx.switchTab({url:'/pages/map/index'});});await pause(1000);await tap('09/06 21:00');await pause(2000);if(result)result.restored=await read();await p.disconnect();}
await writeFile('artifacts/miniapp/sky-time-new-map-runtime.json',JSON.stringify(result,null,2));console.log(JSON.stringify({sky:result.sky.selected,map:result.map.selected,restored:result.restored.selected,newMapRequests:result.newMapRequests}));
if(!result.newMapRequests||!result.sky.selected.some(s=>s.includes('09:20'))||!result.map.selected.some(s=>s==='09/06 21:20，已选择')||!result.restored.selected.some(s=>s.includes('21:00')))throw Error('time_not_preserved');

