# Root CI, image and native API changes

This is task-local implementation/verification history. INDEX/PLAN retain scope; PROGRESS and later entries supersede interrupted process records.

## H4: selected checks and small Context install

- `tools/ci/affected-checks.mjs` reads actual PR merge-base or push before/after ranges with no-renames/NUL paths. Only README and declared Context/existing Skill/work-item prose skip product work. Empty/invalid/missing diffs, unknown paths, executable/build/lock/workflow/design inputs run both jobs. The cheap job retains all live workflow/source-reference checks on full changes, rather than dropping tests whose transitive inputs are hard to enumerate.
- Existing Product CI job/check remains; heavy steps are conditional. Context workflow uses concurrency cancellation, npm cache and `npm ci --ignore-scripts --workspace @starward/context-check --include-workspace-root=false`. New minimal workspace and root lock pin the same harness 0.12.0; a real equality regression prevents version drift. No product dependencies or lock reproducibility are weakened.
- Fresh installation `tmp/context-ci-926d2fac2c40477ebfb2e41c966ae1f8` reported 40 added dependencies (39 top-level package directories; nested dependencies differ). Its installed harness 0.12.0, with no Expo/Taro, successfully ran `dist/cli.js validate-context` against the repository after reboot. An earlier guessed `bin/ty-context.mjs` path failed and was corrected using actual package.bin; it is not a failed checker result. All lightweight test modules use Node builtins/current repository source, not runtime product dependencies.
- Staging qualification now checks successful trusted main push, successful `product` job and successful `Construct production OCI image` step in the **exact triggering run attempt** through official GitHub Actions Jobs REST + gh. A successful docs-only run cannot deploy. Missing/failed API/job/step evidence fails closed. Existing protected manual staging rechecks remain. `actions: read` is the sole added permission. Primary reference: https://docs.github.com/en/rest/actions/workflow-jobs#list-jobs-for-a-workflow-run-attempt . No GitHub run/deploy was triggered locally.
- `prepare:miniapp:final-candidate` no longer repeats the design-system check already owned by `check:miniapp:fast`. Existing design checks/resources remain untouched. Removed redundant probe execution and wording/source-shape checks; the actual map lifecycle callbacks are tested through parsed TS AST/VM with normal and equivalent whitespace. Actual native tap tests remain at their existing owner.
- Post-reboot integration added Node launcher tests to lightweight CI and the launcher to both staging/production control archives. Remote promotion already invokes its entry through direct Node; including the launcher also keeps shipped package npm commands executable.
- Actual checks: 17/17 combined Node runtime, CI, Docker image and deployment workflow tests passed; 26/26 lightweight CI/Skill/workflow/runtime tests passed; after adding locked-checker consistency and transitive-input fallback, CI selector tests 4/4 passed. Git diff whitespace check passed. No claimed GitHub elapsed-time reduction; local measured dependency closure only.

## R7: Docker and shared astronomy release artifacts

- Build and production dependency stages copy only API closure manifests before locked npm ci. Both use BuildKit npm cache mounts. Required sources/launcher copy afterward, so source-only edits do not invalidate dependency layers. Runtime stays pinned/nonroot and starts compiled BFF with production export conditions.
- `packages/astronomy-core` gains production `dist/src/*.js` exports and a build config that rewrites TS relative imports, includes immutable catalog data in compiled output, and excludes tests. Existing numerical/data/asset content is unchanged. Source exports still support Taro/Metro; relative source index imports are explicit TS extensions. Root release build includes coordinate/contracts/core/API in dependency order.
- Docker runtime includes compiled astronomy core and migrations. `.dockerignore` excludes non-runtime docs/.agents/.long-task; no design file itself is changed. `tools/run-node.cjs` is builder-only. API workspace runtime start/migrate remain direct Node.
- Before reboot: final then-current Docker image `starward-miniapp-api-efficiency:local` built successfully, 87,331,500 B. Network-disabled container imported compiled production BFF and real committed Gaia catalog. This image predates final concurrent source/Node changes: final coherent rebuild, database migration/start/readiness and cache reuse checks are still required.
- New image contract regression checks complete API/core workspace closure, compiled core presence, manifest-only pre-install COPY and both npm cache mounts. Image/workflow tests passed after these edits.

## R9: bounded native forecast cache

- `apps/api/src/modules/forecast/forecast-query-service.ts` now validates optional `maxCacheEntries` (default64), prunes expired results lazily, touches reused entries and evicts least-recently-used entries on insert. Zero disables completed-result caching while existing in-flight coalescing and failed-provider retry remain. No shared cache/algorithm/provider change.
- Five forecast service tests passed, including actual HTTP route and durable forecast-runtime restart checks plus new LRU/TTL/disabled-cache/coalescing/retry cases.
- Post-reboot native API typecheck initially exposed TS5097 in source-imported shared modules; `apps/api/tsconfig.json` now permits TS extensions under its existing noEmit pipeline (matching mobile/WEAPP behavior). Typecheck and all five forecast tests then passed, session52178 exit0. This is R7 source-export integration as well as R9 verification.

## Root integration review / remaining

- Reviewed actual AstronomyService effective/public/context/representation identities, publication detail gate, provider deadline cleanup, map candidate/population assembly and complete sources. Cache/decision/deadline owner facts added to existing runtime/domain Context and implementation index, with constants kept in code. R2 adapter extraction updated the existing maintenance-boundary debt instead of declaring all transport/identity extraction complete.
- Root independently found an R5 starvation case: continuous pose replacement faster than native completion may prevent initial Canvas dimensions/visibility ever publishing. Agent is reproducing/fixing this before R10 implementation; don't treat pre-fix33 tests as final proof. Root requested continued invalidation protection for expiry/null/mode/resize/hide.
- R2 independent review found unrelated-prefix invalidation of in-flight requests plus silently successful failed disk erasure; backend agent owns fixes and tests. Their pre-fix336 tests are historical, not final validation.
- Last scoped check showed no diff in `docs/design-resources`, `.agents/skills/starward-design-resource` or DESIGN.md. Full final integration and task-owned resource cleanup remain outstanding.
- The minimal Context npm fixture still exists: automatic approval rejected its guarded exact-directory recursive removal with `blocked by policy` (no further reason). No deletion occurred; user informed. Do not silently bypass the rejection using another shell/language.
