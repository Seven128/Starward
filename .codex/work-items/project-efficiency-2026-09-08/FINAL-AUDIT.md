> Updated after user-requested reopening and actual official native tool integration. Historical failures remain in R10-NOTES and PROGRESS; current R10 evidence is R10-OFFICIAL-VERIFIED.md and R10-INTEGRATED-WORKFLOW.log.

# Final requirement and evidence audit

Scope is the unchanged INDEX/PLAN H1–H4 and R1–R10. D1–D4 remain excluded. This file records final dispositions; older pending statuses in historical notes are not current. Tests do not certify product release or physical-device acceptance.

## Item dispositions

| Item | Final implementation / decision | Evidence and verification scope |
| --- | --- | --- |
| H1 | Removed retired snapshot admission; canonical physical path and isolated child TEMP checks precede side effects. Current callers/Context use the same owner. | H123-NOTES; actual host preflight; current runner behavioral tests in FINAL-WORKFLOW.log. Covers redirected/sibling/retired paths and process environment, not a new full candidate run. |
| H2 | Platform-first UI routing; ordinary builds avoid release runbook preload; existing conversation authorization applies. Current device/UI Skills point to warm feedback and preserve actual phone/MFA/external boundaries. | Current three Skills; H123 routing-case review and metadata/reference checks. Latest16 focused Skill/CI/launcher tests and97 workflow tests pass. UI metadata unchanged; Python validator unavailable, existing YAML/frontmatter checks used as recorded in H123-NOTES. |
| H3 | Keep global.md as sole default; bounded current-owner discovery at existing Context owner, historical expansion only when relevant. | Current context-maintenance/implementation-index; final default-list returned only global.md; context validation passed. Original11 evidence snapshots hash-match FINAL-ARTIFACT-AUDIT.json. No historical design archive/deletion. |
| H4 | Minimal locked Context workspace and cache/concurrency; PR merge-base/push ranges, rename/deletion handling and full fallback; docs-only success cannot authorize staging; duplicate checks removed and implicated syntax assertions replaced by behavior. Production tar/retry output corrected during integration. | affected-checks implementation/tests; current workflows/package commands; H4-R8-REVIEW and executable archive/launcher probes; final-ci-runtime18 pass and current16 focused checks. Minimal install40 packages, no Expo/Taro. No remote CI elapsed-time or deployment claim. |
| R1 | AstronomyService owns staged decision/full scene computation, bounded shared caches/coalescing, current public facts/weather gating and caller-specific envelope identity. Map/overview do not project unused stars. | astronomy-reuse/computation-cache tests inspected; final release-code worker119 pass/2 DB-gated skips; R134 distinct-coordinate probe makes8 cold weather calls then0 new calls, map projections160→0. No universal latency percentage from noisy host timings. |
| R2 | Extracted one response-cache owner: UTF-8 budgets, bounded memory/body chunks, asynchronous coalesced persistence and small manifest; migration, corruption/failure recovery, ETag/scopes/invalidation and truthful erasure results. | Current callers/response-cache tests include large Unicode body, failed writes, late writes, selective304, pending account erasure, cleanup failure and retry.351 app tests/typecheck/WEAPP build passed. R25 actual wx storage707203 B/3 body writes/new owner-instance readback/0 remaining namespaced keys; current source hash matches that fixture. Not phone or process-restart acceptance. |
| R3 | Fetch AND body deadlines, overall/batched weather budget and independent waiter cancellation; authoritative alerts/freshness remain attributable. | provider-deadline/weather tests and R134 notes; shared cancellation/ignored abort/listener cleanup/retry covered in current worker suite. Lane6000/shared6500/map7500/client10000ms live in owners. No real provider quota used. |
| R4 | Direct spatial candidates plus compact population metadata; current spot payload overlays stale detail; publication gates retained in all paths. | Current repository/service and test assertions inspected. FINAL-R4-POSTGIS.log:2/2 pass on a fresh task database with actual PostGIS. Test covers visibility,status,assessment complete/revision/age,closed spot,outside radius,current payload; memory viewport avoids listSpots. EXPLAIN single-row evidence proves query path only. |
| R5 | Existing page retains data/pose/render commands; adjacent lifecycle owns measured surface/context,latest frame,one native draw,callback barriers and cleanup. Continuous poses cannot starve first visibility. |34 focused tests and24 old/new real-Gaia command-stream comparisons; current351 app tests/typecheck/isolated build. Actual R25 hidden callback,pose burst,resize280,page hide/show passed; current owner SHA matches. No layout redesign,FPS or hardware-heading claim. |
| R6 | Release-only Expo minify/resource shrink; actual verifier passes those flags. Preserved supported ABI settings; extracted existing pure geometry to remove native Node-crypto bundling dependency. | Comparable x86_64 APK48266844→38742755 B (-19.732%); hashes/current files and both cold-start/five-route AVD records matched in FINAL-ARTIFACT-AUDIT.json. Public manifest/ABI/signer summary and all native .so hashes equal. See unavoidable historical artifact-loss incident below. |
| R7 | Manifest-first locked dependency layers,BuildKit caches,narrow backend context,complete compiled workspace closure/migrations,pinned nonroot direct runtime. | R7-cache-probe:source change reused both installs,lock change invalidated prod install,original bytes restored. Final image f1bf7695… re-inspected at87332260 B matching R7-image-smoke-final.json; migrations/worker/readiness/capabilities passed. Task services stopped; no push/deploy. |
| R8 | Portable Node16-compatible selector uses verified current/npm Node,child PATH and workspace bins; propagated through scripts and control archive. No personal source path/global PATH change. |5 actual runtime tests incl nested child/literal argv/exit behavior,lightweight CI/workflow integration and actual default Node16→npm Node24 recovery. Final selected entry is used by all later checks. |
| R9 | Native forecast completed-result cache bounded with LRU/lazy TTL/zero-disable; original in-flight/failure lifecycle retained. | Current service and5 tests including eviction,expiry,coalescing,retry,HTTP and runtime restart; native API typecheck passed. No conflation with Mini Program astronomy cache or invented benchmark. |
| R10 | Existing miniapp:observe now selects verified official IDE tools explicitly for text/tap/input/console/network/screenshot; retains the owned persistent SDK for warm layout/condition waits. Shared redaction, readiness, route/instance/unique-selector checks, no replay and exclusive screenshot writes. | Official2.02.2608070/Skill0.3.9 login succeeded; actual click0→1 and integrated1→2,input echo,wx.request200,console/network and visually inspected screenshots. Actual Starward map read-only screenshot succeeded. Full affected workflow101/101; later help-only observer10/10. Warm SDK read28–36ms/screenshot78ms remains separately scoped. No third-party MCP dependency. |

## Cross-cutting obligations

- Ownership/dependency review: public calculation cache stays in backend AstronomyService; client representations stay in response-cache; native Canvas scheduling stays beside the existing page; native forecast remains a distinct service. Pure geometry does not import Gaia integrity/Node crypto into native UI. Release exports retain source consumers and complete production closure. Corresponding current Context owners updated, exact constants remain code-owned.
- Safety/freshness review: current publication facts precede cache use; actual weather/alert expiry clips reuse; exact selected time and catalog/fact changes invalidate identities; private origins/account contexts are not shared public calculation keys. Erasure and invalidation fence late writes,clear-cache preserves unrelated work,and failed cleanup reports failure. These are exercised invariants,not just source-word tests.
- Verification identity: final-release-code.log has351 app tests,119 backend pass/2 gated skips,contracts/typechecks. Real PG/Redis integration separately passed; final R4 live tests now have a persistent raw log. Final isolated WEAPP build wrote weapp-check and passed,canonical preview output preserved. Later changes were observer/tests/routing/task notes,not product source requiring another APK/Docker/WEAPP rebuild.
- Design freeze: current git diff for docs/design-resources,new design-resource Skill,and DESIGN.md is empty; inspected changed-file inventory contains no visual token/media/adoption/source-census edit. Existing Settings changes are error/cleanup behavior only. New adopted-but-unimplemented design remains untouched. Work remains main; no commit,branch,deploy,upload,review or remote production write.
- Current evidence retention: all11 original snapshots match; R25 source hashes and installed-APK evidence match current saved files; final Docker identity matches its actual runtime result. INDEX links current notes and this audit. Historical failures are retained rather than overwritten into success.

## R10 selected workflow and supported limits

Use existing miniapp:observe --official-ide <explicit installation> against the exact open project for supported text/tap/input, filtered console/network and native screenshots. The official adapter checks installed Skill readiness and uses bundled CLI/Electron, without changing global PATH or installing a wrapper daemon. Wrong page and ambiguous targets reject before acting; failed actions are not replayed. Screenshot bytes are checked and destination writes are exclusive. Network output drops headers, bodies, queries and unparseable raw lines. One controller per project remains necessary because independent commands cannot make page checks and subsequent interactions atomic.

For frequent layout reads, retain the existing owned SDK observer and batch known selectors; geometry-condition waiting uses actual native rectangles. Recorded warm calls28–36ms and screenshot78ms exclude initial attach; one attach took5924ms. Direct first-party fixture commands took roughly2s including startup; integrated commands add readiness/page/selector checks and therefore cost more. No universal speedup or product latency claim. The old SDK Page/log failures are preserved, and the verified newer first-party path fills those capabilities instead of requiring Computer Use or synthetic handler calls.

R10-OFFICIAL-VERIFIED.md and latest R10-NOTES record actual user login, real native click/input/HTTP/console/network evidence. R10-integrated-final.jpg visually confirms integrated input echo; R10-product-readonly.jpg proves actual Starward map observation only (spots were still loading). This is not backend readiness, physical-device or fixed-candidate acceptance. Existing changed-module tests, native WEAPP feedback and risk-triggered physical-device/fixed-candidate lanes remain distinct.

Primary-source research compared official capabilities, existing automator and third-party CLI/SDK wrappers, including compatibility/license/maintenance limits. No demonstrated advantage justified a new MCP package over this working first-party path. CloudBase governance remains separate from local IDE inspection. Full affected workflow101 tests pass; meaningful adapter regressions include business errors, primitive responses, network privacy, duplicate selectors, page drift, no replay and invalid/overwritten screenshots.
## Incidents, retained resources and limits

- During R6,Expo prebuild unexpectedly regenerated ignored Android files and lost the unfingerprinted July29 APK and original debug keystore. Cache recovery was unavailable. The loss was disclosed; the two measured APKs are newly generated comparable artifacts,not recovered originals. No production-signing claim or signing migration is made. This historical loss cannot be undone by passing later tests.
- Initial R6 AVD System UI ANR retained separately; recovered readonly host-GPU AVD passed both variants and was stopped. No physical sensors/provider/full product acceptance is claimed.
- New IDE GUI automatically registered wechatide to the isolated extraction; pre-launch handler is unknown. Keep the extraction to avoid a dangling handler. Earlier evaluation processes were stopped; after reopening, the new IDE remains open with the canonical user project and isolated feedback fixture. Do not close the user project as task cleanup. The final old-IDE fixture was closed through the official exact-project command;19420–19424 listeners are absent. No user IDE/global quit was issued.
- Docker engine is currently unavailable at its named pipe during the latest read-only audit; prior task service stops and actual image/start/migration tests remain historical evidence, not a currently live runtime claim. Saved images,APK comparisons,fixture databases and logs remain attributable artifacts; no Docker prune. An additional final-audit database exists inside the stopped task PG container. The initial final DB test used a stale historic ephemeral port and failed ECONNREFUSED; rerun with actual Docker mapping passed,first log retained.
- Automatic approval previously rejected guarded removal of tmp/context-ci-926d2fac2c40477ebfb2e41c966ae1f8 with only “blocked by policy”; it remains. No alternate-shell deletion bypass was attempted. This retained test fixture does not change product behavior.
- No physical-device acceptance,online provider latency,remote CI timing or public-release outcome is claimed. Those are outside this efficiency implementation; actual runtime checks above are scoped explicitly.

## Conclusion

Each authorized H/R item now has implemented/verified work or an explicit evidence-backed tool non-adoption decision. The limitations above do not become claims of successful tool/device/product acceptance. The implementation and integration audit are complete. Final unchanged-source checks and current resource inventory are recorded in the FINAL STATE section of PROGRESS.
