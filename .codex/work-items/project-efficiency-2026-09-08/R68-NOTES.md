# R6 / R8 implementation notes

## Latest root recovery

Root completed both actual runtime comparisons after recovering the emulator: baseline-runtime-smoke.json and optimized-runtime-smoke.json each PASSED cold start and all five native route mounts (tonight/map/trips/sky/me); installed APK hashes equal the respective saved artifacts and product process remained alive on each route. Same readonly API36 x86_64 AVD used host GPU,4096MiB,4cores; cold boot29116ms versus previous software-renderer133830ms (different emulator configurations, not app-speed improvement). APK sizes/hash evidence unchanged. Root stopped only emulator-5580 with `adb emu kill`; no snapshots saved or original AVD/app data changed. This verifies release packaging/startup/route integration under declared conditions, not backend/provider availability, physical sensors or full UI acceptance. Historical prebuild artifact loss remains disclosed below.

Optimized build64096 completed; metrics in tmp/project-efficiency-android-20260908 confirm48266844→38742755B (-19.732%), unchanged native .so hashes and public manifest/signing/ABI summary. The agent's later baseline runtime smoke **failed** at cold_start; root found baseline-runtime-smoke.json and visually inspected baseline-failed-startup.png. Product content was rendered beneath an Android System UI-not-responding modal, so the missing tab marker is not attributable to minification (this was baseline) or proven app startup failure. No optimized runtime result exists. Current process inventory has no emulator; no runtime comparison is complete. Resume with readonly/task-owned AVD, root heavy slot, existing saved APKs; do not prebuild/clean/rebuild the artifacts just to repeat runtime checks.

Task-local continuation after H1/H2/H3. Root owns PLAN/PROGRESS and Docker/CI/forecast work. This agent owns Android release-size inspection/appropriate release-only changes and portable Node/npm entrypoint work. No commit, signing/distribution change, supported-ABI deletion, design-resource/UI change, full UI acceptance run or replacement of another app/session is authorized here.

## Current

- R8 is implemented and directly verified; root/package script ownership has been returned to root. No personal runtime path, global PATH change, new dependency or node_modules patch was added. See Checks for actual results and the interrupted broader test.
- R6 has a successful, archived current baseline (48,266,844 bytes). The geometry bundling blocker is repaired and tested; release-only minify/shrink settings are enabled. Controlled optimized build session **64096** is running after root returned the sole heavy slot. Its log/result are `optimized-build.log` / `optimized-build-result.json` in the task's tmp directory. No AVD has restarted after the host reboot; coordinate with root before launching one.
- Preserve the explicit historical APK loss/prebuild incident below. No prebuild/clean may be repeated, and the new baseline must never be called recovery of the July APK.
- Root owns CI/Docker and PROGRESS. Android/native build files remain with this agent. API package-script changes were coordinated with the backend owner; no further root/package edits are pending here.

## Checks

- R8 implemented before reboot: `tools/run-node.cjs` is Node16-compatible, reads root engines, verifies current Node or npm_node_execpath, resolves workspace package bins, selects child PATH only, and preserves literal argv/exit/signal handling. Existing common root, Mobile/API/WEAPP and release-build workspace scripts use it; backend production runtime scripts remain direct node. The development runner now prefers npm_execpath over a Windows-specific sibling npm location. The Mini Program candidate fingerprint includes the new build launcher.
- Actual default-shell checks before reboot: `node --version` remained 16.13.1; plain `npm run node -- -p process.execPath` selected npm's Node24.16.0; `npm run test:node-runtime` passed 5/5; `npm run context:validate` passed; `npm run build:weapp --workspace @starward/wechat-miniapp -- --help` returned Taro usage without compiling. A bare old-Node invocation without npm_node_execpath exited 1 with actionable guidance and never started its requested child.
- Mobile TypeScript preflight initially failed on 11 TS5097 imports from source-exported astronomy modules. `apps/mobile/tsconfig.json` now enables allowImportingTsExtensions, appropriate to its inherited noEmit/Metro pipeline. The real mobile typecheck then passed, as did the existing native JavaScript-root import typecheck command. No shared astronomy implementation was changed by this agent.
- A broader plain-npm workflow test was launched before reboot but its log contains only the command header, with no test conclusion. It is interrupted/unknown, **not passed**. Previous H1/H2/H3 and direct R8 test results remain attributable to their earlier inputs only.

## Android prebuild incident — preserve during recovery

- Preflight found JBR OpenJDK 21.0.10 under Android Studio and the installed SDK at the user's default local Android directory (platform/build-tools 36, NDK 27.1.12297006). They were absent from PATH, not absent from disk. No Java/ADB/emulator process or connected device existed at the initial observation; querying devices started the normal ADB server.
- The prior generated `apps/mobile/android/app/build/outputs/apk/release/app-release.apk` existed, size 48,455,504 bytes, timestamp 2026-07-29; `output-metadata.json` was 709 bytes. No hash/backup was captured before the following operation. This historical file was never a current baseline.
- Exact command, from `apps/mobile`, with process-local Node24 PATH/JAVA_HOME/ANDROID_HOME/ANDROID_SDK_ROOT and CI=1: `node ../../node_modules/expo/bin/cli prebuild --platform android --no-install`. **No `--clean` flag was passed.** Output nevertheless said `Clearing android`, `Cleared android code`, `Created native directory`, `Updated package.json | no changes`, `Finished prebuild`. Full output is `tmp/project-efficiency-android-20260908/prebuild-baseline.log`.
- This unexpectedly replaced the ignored generated `apps/mobile/android` tree before the planned output backup. The prior APK is confirmed absent afterward. The existing `%TEMP%/starward-android-build-cache/28b352e5c5a3e1b5` directory was inspected; it contains no files, so there is no attributable cached artifact to restore there. Do not reconstruct a replacement and describe it as the original file.
- Tracked mobile diff remains only the intended package-script/runtime and TypeScript-check configuration changes at this stage; the generated native tree is not tracked. The earlier and regenerated Gradle signing configuration both specify the template `debug.keystore` / `androiddebugkey` signing for debug and release; no production signing configuration was declared. Original ignored keystore bytes were not fingerprinted, so byte preservation cannot be claimed.
- Root was notified immediately and instructed no repeat prebuild, clean or broad removal. No further prebuild/clean will run. Subsequent baseline/optimized builds use the regenerated task-owned output, with every comparison APK saved before another build. Original historical output loss remains explicitly disclosed.
- A task-owned hidden emulator was launched on port 5580 using existing `Starward_Verifier_1` with `-read-only -no-snapshot-load -no-snapshot-save -no-window -no-audio`. This uses disposable emulator state and does not save changes to the original AVD. PID is recorded in `tmp/project-efficiency-android-20260908/emulator.pid`; stop this emulator at task end. No full UI acceptance or installation into another active device is permitted.

## Reboot recovery and controlled build

- User reported a freeze and reboot. Current boot was observed as 2026-09-08 05:28:25.5 +08:00, with about 20.4 GiB free RAM at this recovery check. No Java, ADB or emulator processes were found. Old PID files are historical only.
- Old sessions 49157, 49346, 38876 and 57513 all return `Unknown process id`. Session 49157 was the first failed baseline attempt: unquoted PowerShell dotted `-Pandroid...` arguments were split and Gradle stopped with `Task '.enableMinifyInReleaseBuilds=false' not found`; its output/result are retained as `baseline-argument-failure.{log,json}`. Session 49346 was the actual corrected baseline build interrupted by reboot.
- The interrupted `baseline-build.log` ends during Metro/React Native CMake tasks; no completion marker, build-result JSON or release APK exists. No successful baseline or size comparison is claimed. The log is retained as `baseline-pre-reboot.log` before controlled retry.
- Root assigned this agent the sole heavy-build slot; Docker and emulator remain stopped. Controlled retry uses one x86_64 ABI, Gradle max-workers=2/no-parallel/no-daemon, process-local CMAKE_BUILD_PARALLEL_LEVEL=2, JVM ActiveProcessorCount=2, and a disposable init script limiting Metro to 2 workers and Ninja compilation/link pools. No prebuild, clean or broad cache removal is repeated. These limits are task-local and identical for baseline/optimized comparisons.
- Remaining: complete comparable baseline/optimized release builds and attributable isolated launch/module smoke, document size delta and limits, stop only task-owned processes, notify root when releasing the heavy slot. R6 optimization flags have not yet been enabled; do not claim this item complete.

## Existing native bundling boundary repaired

- The first controlled baseline (session 24107) failed after 544.56 seconds, with no APK. C++ tasks completed under the caps; observed free RAM stayed above 18 GiB and exactly two clang++ workers were observed. Output/result are preserved as `baseline-node-boundary-failure.{log,json}`.
- Metro failed at `node:crypto` with the exact chain `SkyScreen.tsx -> sky-model.ts -> gaia-catalog.ts -> gaia-catalog-integrity.ts`. `git show HEAD:packages/astronomy-core/src/sky-model.ts` confirms the broad facade import existed before this Goal. Directly importing the projection file would not fix it because that projection itself depends on the integrity loader.
- Native SkyScreen consumes only optical FOV, local-horizon visibility and HorizonProfile types. Moved those existing formulas/types unchanged into `sky-geometry.ts`; the existing sky-model facade re-exports the same functions and reuses its interpolation helper. Native SkyScreen changes only its import path. No Gaia asset, hash policy, star identity, numerical projection or visual behavior was modified; no Node crypto shim was added.
- The native sky test had obsolete fixture assumptions (`bright` plus `deep`, exactly five catalog objects). The real, integrity-checked Gaia dataset does not satisfy those assumptions. Native tests now exercise their actual pure-geometry consumer boundary. Equivalent catalog adapter coverage lives with the shared owner and verifies every magnitude-qualified Gaia source ID plus unknown obstruction; the seven existing Gaia tests still prove 2,048 rows, integrity/corruption handling, frame rotation, astrometric goldens and magnitude boundaries.
- Actual checks after extraction: shared Gaia/geometry tests 11/11; native sky runtime tests 5/5; full mobile TypeScript check passed. No separate full Metro export was run before the next Gradle attempt.
- Native artifact fingerprint now includes the shared astronomy source/package and Node build entrypoint; a real-file fixture test proves changes invalidate reuse while test-only changes do not. Fingerprint-focused tests 3/3 passed.
- `prepareAndroidArtifact` reads the two release optimization booleans from the app.json Expo build-properties owner and passes them through `androidGradleArguments`. This prevents existing ignored native directories from silently retaining old shrink settings, without another prebuild. Explicit true/false forwarding, invalid types, and shrink-without-minify rejection were tested; relevant argument tests 3/3 passed. Current AppID, signing configuration and four supported ABI settings remain unchanged.
- Root temporarily used the heavy slot for Docker recovery, then returned it after Docker could not start. The next controlled baseline uses the same caps and JNI intermediates, never prebuild/clean.
- After the geometry dependency correction, mobile typecheck also passes with `--allowImportingTsExtensions false`. Removed the temporary tsconfig relaxation, restoring the original tracked configuration; the proper dependency fix resolves that earlier TS5097 symptom too. Do not report a final mobile tsconfig change.

## Successful baseline and release configuration

- Repaired controlled baseline session 17493 completed: Gradle `BUILD SUCCESSFUL in 6m 3s`, 869 tasks (329 executed, 540 up-to-date), measured command duration 363.402 seconds. This is build/setup cost under explicit caps, not an application-speed measurement.
- The newly built x86_64 baseline APK is 48,266,844 bytes (about 46.03 MiB), SHA256 `01fc1ccfdfa5e3fff94628d4564e32921a3d9acac573028d94e73369ecbc41b2`. It was copied to `tmp/project-efficiency-android-20260908/baseline-release-x86_64.apk` before any optimization build and source/copy hashes matched. This is a new current baseline, not recovery of the lost July APK.
- `baseline-apk-metrics.json` records ZIP category sizes and native-library hashes. DEX compressed bytes are 15,622,426 and native-library bytes are 24,055,560. `baseline-manifest.txt` retains only package/SDK/permission/ABI fields; `baseline-signer.txt` holds only the public certificate SHA256 digest. No keystore contents were read or persisted.
- Enabled only `enableMinifyInReleaseBuilds` and `enableShrinkResourcesInReleaseBuilds` in the existing app.json Expo plugin. The installed plugin's own `validateConfig` accepted these settings. Mirrored exactly those two declared booleans into the current generated gradle.properties without prebuild; the existing source owner will reproduce them on future native generation. No signing, AppID, version, ABI, permission, Hermes or visual configuration changed.
- Root received the heavy slot after baseline success for R7 Docker verification. No optimized build or AVD launch starts until root returns that slot. Optimization remains pending artifact/runtime comparison.
