import fs from 'node:fs';
const root='E:/dev/Starward/';const edit=(p,f)=>fs.writeFileSync(root+p,f(fs.readFileSync(root+p,'utf8')));
edit('DESIGN.md',s=>s.replace('small/medium顶部圆角，large直角','无顶部图片时三档均保留顶部圆角；large有全幅顶部图片时沿用图片构图').replace('small/medium圆角，large顶部直角，无外阴影','无顶部图片时small/medium/large均保留顶部两角；large全幅图片按图片构图，无外阴影').replace('small/medium spot panel top radius32rpx，page-like large radius0','无顶部图片的三档spot panel保留顶部圆角（沿small/medium的采用几何）；有全幅顶部图片的page-like large才允许radius0'));
edit('project_context/areas/main/screen-contracts/wechat-miniapp/spot-and-sky.md',s=>s.replace('Catalog stars are rendered by measured brightness/color data without labels or target semantics; only visible `SkyTarget` objects receive display names and necessary direction/altitude/state.','Catalog stars are rendered by measured brightness/color data without persistent labels or recommendation semantics. Visible `SkyTarget` objects retain necessary direction/altitude/state. Explicit catalog-object selection for information follows the lazy detail contract below; it does not turn every background star into a recommendation target.')+`
## Explicit celestial information (2026-09-09 revision)

Point selection and the user-opened object list open the same centered dark information modal, never a bottom sheet. It inherits the selected place/time, identifies the star/planet/galaxy/nebula honestly, and shows a readable name or catalog ID, available aliases, sourced introduction, relevant known facts and unobtrusive attribution. One tap on a resolvable object is sufficient. Keep all-sky labels sparse; many rendered objects do not require permanent names or a full paragraph each. A galaxy is not mislabeled a star, constellation or event to fit an existing type.

Use a graphite blue-gray surface, restrained edge, soft shadow, compact title, clear body text and a 44px close target. Center the content-sized dialog within safe available screen space; long text scrolls inside with header/close reachable. Dim the sky, isolate background hit testing and accessibility focus, and return focus to the triggering target or object-list entry on close. Back closes this modal first and restores the same sky context; it must not dismiss the underlying place. Warm-red observation mode applies to this modal too. Do not add brightness-heavy illustrative imagery without real licensed source coverage.

Keep loading, available introduction, basic-facts-only, and retryable failure in the same stable modal. Show known identity/facts during loading; distinguish missing editorial content from request failure. No source means no invented description, measurements or borrowed image. Introductions describe the object, while azimuth/altitude explicitly belong to the displayed selected-place/time sample. Hold that sample identity while the modal is open; closing resumes existing direction/time ownership without a new location request or time reset. Image zoom availability and introduction availability are independent.

This is intended behavior, not a completed production feature. The current production scene/catalog types and canvas/target list do not supply universal hit testing plus a lazy introduction contract. The candidate demonstrates named targets with one sourced Vega introduction and basic-only alternatives; it does not establish all-star or galaxy production coverage. Scalable identity, query and caching boundaries are owned by [runtime/domain architecture](../../../../architecture/runtime-and-domain.md#celestial-object-selection-and-lazy-information).

No-image panel corners: retain both top round corners even at large extent. The rounded-away pixels at the large panel's top boundary belong to its presentation shield, not to exposed-map dismissal; the header boundary remains continuous. Smaller extents retain intentional exposed-map navigation and their established drag/scroll rules.
`);
edit('project_context/architecture/runtime-and-domain.md',s=>s+`
## Celestial object selection and lazy information

Extend existing astronomy-core catalog pipeline, BFF astronomy/application adapter and miniapp-contracts; keep projection/ephemeris on the current astronomy owner and selection/rendering in the sky client. No feature import into shared contracts, second client ephemeris, notification platform or per-star remote text generation. This section specifies later implementation, not new endpoints already shipped.

- Identity: current SkyScenePoint catalogIndex is frame/pack-local, not a durable object ID. Resolve through the matching version/hash-bound SkySceneCatalogEntry.sourceId (a string; Gaia IDs must not be coerced to unsafe JS numbers). Introduce a typed, namespace-bound canonical object reference and attributable cross-catalog aliases within the existing catalog boundary; preserve object identity independently from pack order/version and human display name. Validate crossmatches offline with provenance, epochs and ambiguity handling; never join by translated name or assume nearby coordinates identify the same object. Solar-system IDs stay under the ephemeris owner. Add galaxy/nebula kinds explicitly where required rather than abusing current SkyTarget enums.
- Picking: retain the last painted frame/version/camera transform and build a bounded spatial hit index only for actually rendered visible candidates. Pick against that displayed snapshot; reject stale-frame mappings. Apply hit tolerance and deterministic proximity/brightness ordering; offer a compact disambiguation choice for unresolved overlaps. A pinch, drag, cancelled pointer or background modal interaction must not become a tap. Do not create a DOM node or fetch for every catalog star. Sparse names and an explicit paginated/searchable accessible list complement canvas selection; neither silently makes missing objects selectable.
- Content: an object information projection returns stable reference, available kind/name/aliases/basic measurements with units/bands and provenance, localized curated introduction when available, content revision, attribution/rights and explicit availability. Existing catalog facts may supply a useful basic-only record; basic photometry is not a biography or Chinese description. Common named objects may have a small versioned editorial bundle, while the remaining catalog uses on-demand lookup. Static object facts and narratives remain separate from place/time-dependent apparent position and observing evidence. Missing parameters are omitted or marked unknown, never inferred from appearance.
- Retrieval: request only the explicitly selected identity; deduplicate in-flight requests and use bounded client LRU and server cache keyed by stable ID plus relevant catalog/content revision and locale. Pose/time updates do not refetch static prose. Version position snapshots separately by observation context/sample. Abort or ignore superseded requests on selection/close/account/location changes; a late answer cannot replace another object or reopen a closed modal. Cache known-no-content for a bounded negative TTL; transient errors get bounded retry/backoff, not permanent absence. Server adapters validate references, sanitize source markup, bound payload/text length, timeout/concurrency/rate/cost, and preserve attribution. Private observing context remains separate from shareable public object facts.
- Sources: [SIMBAD](https://simbad.cds.unistra.fr/simbad/) is a candidate for extrasolar basic data/cross-identification and bibliography, not a universal Chinese encyclopedia or solar-system ephemeris. Assess coverage, exact access/redistribution terms, deployed connectivity, request budget and cache rights before selection. Narrative editorial sources and survey image licensing are separately verified. Do not perform one external query per star/frame, bulk import without terms review, or promise introductions for every point because a catalog entry exists.
- Required later verification: real production pack/frame → stable object identity → selected-object response; reordered packs, large numeric IDs, alias ambiguity, dense overlaps, pinch-versus-tap, locale, same-ID deduplication, bounded memory/network, rapidly switched targets and late/failed/empty responses. Validate loading/basic-only/ready/error states, readable long modal content, focus/Back restoration and warm-red mode on WEAPP. A small candidate fixture does not pass these production checks.
`);
edit('project_context/areas/main/screen-contracts/wechat-miniapp/surfaces-and-controls.md',s=>s+`

2026-09-09 celestial detail revision: explicit sky-object selection uses one centered dark modal, shared by canvas and the user-opened object list, with contained long-text scrolling and background/focus isolation. It is not a bottom sheet. Close/Back restores the unchanged sky context. Loading, sourced content, basic-only absence and retryable error share stable geometry; detailed rules live in [Spot and sky](spot-and-sky.md#explicit-celestial-information-2026-09-09-revision).
`);
edit('DESIGN.md',s=>s+`

2026-09-09 user feedback calibration (Mini Program candidates, adoption remains separate): driving range uses a light two-row control with enable/name and time-distance choice above a natural numeric reading, avoiding the enclosing blue card and large boxed input. My ongoing status uses a low-saturation mist-green badge with slightly increased inner space; badge and end-time text share their visual center. Sky object information uses a centered graphite dark modal with restrained edges, readable body, contained scrolling and a reachable close action; it inherits observation-mode colors. Scope is these controls, not a new global style.
`);
console.log('Owning Context updated; intended behavior distinguished from current code.');
