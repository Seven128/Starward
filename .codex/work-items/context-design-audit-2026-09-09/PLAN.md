# 执行方案（用户确认后的完整执行内容）

## 本轮交付与顺序
1. 保存本索引/决策/计划/证据/进度，goal中写明索引。
2. 修改现有Context owners，替换冲突，不全项目重写、不制造第二权威。
3. 使用 `.agents/skills/starward-design-resource/SKILL.md` 组合 Tiny Context design-resource：Stitch优先MCP，缺操作再官方网页。逐页沿已采用方向局部修订、精修、实际查看；候选与采用分离，交用户审查。
4. 验证Context与资源；报告直接修改、待审和未做生产验证的边界。

## Context：产品
- 小程序仅事实辅助出行/观测，不输出综合出发推荐；App职责独立。安全/进入/道路/法律事实保持动作限制，撤推荐不撤安全。
- 350元独立预算，范围/实际权益/网络/用量不混同，不新增购买。
- 正式点、私有草稿、审核中新增提案、正式点+本人反馈统一身份能力：草稿本人查看/编辑/明确保存，不开放想去/分享/计划/天空；pending本人查看/天空，不开放想去/分享/计划。正式点本人反馈不覆盖正式资料或改变原权限。
- 审核通过≠正式发布；发布/合并到既有正式点有回执映射，当前面板/地图标记/记录/Observation Context/天空返回同步，去重复。私人快照保留，不改写历史。
- 地图唯一底部状态 none|spot-panel|layer-sheet|spot-editor，编辑内区分新建/编辑，不旁路布尔。未保存离开先确认，确认之前不提交选点/相机/上下文；取消保留输入/媒体/候选位置与原状态。切点/关闭不隐式保存，旧请求不能回灌。
- 五类16筛选；去两项推荐、摄影改实拍。动态按当前时刻，天象按覆盖与观测夜/地点可见性。参数驾车默认180、30—360整数，现有sheet确认/取消。剩余逐项定义证据、阈值、范围、unknown；已有合理阈值保留，不能把有资料当符合。开阔方向只表示有已记录方向，不承诺目标方向通视。
- My候选：start<=now<end 或 now<=start<=now+24h，进行中优先，再start/planId排序，最多3；跨午夜时区正确，前台和时间经过刷新，结束退出；全部计划入口一直在，失败非空。
- 预计到达晚于观测开始显示到达/迟到差值，不改时间/不禁保存；未知/失效路线明确不可核对。
- 保留计划自身编辑副本、BFF按提交地点/时间重新解析和版本快照/起点恢复保护。浏览Context只初始化；事件返回不覆盖计划。
- 计划合法日期、事件覆盖、天气覆盖分离；天气-7/+15不限制计划保存；事件更新不改用户时间。
- 云观星在既有时间附近轻量显示选定地点，不检测“远程”才显示，不为此定位、不恢复大标题。
- 清理旧推荐、到访/主题反馈、200%字、单条浮动通知、发布丰富资料全齐、旧原生职责泛化等受影响冲突。现行最多三浮动+关键错误行内恢复、核心发布标准有效。

## Context：架构/接口未来实现约束（仅描述）
- Proposal观测输入由服务端授权、状态和冻结坐标解析，不能伪spotId或MAP_POINT绕过权限；私人身份资料响应不公共缓存，纯计算与私人读模型分离。
- 稳定proposal身份、工作副本revision、冻结提交attempt、幂等receipt和formal映射；审核/发布分离。草稿/提交/必要审核记录引用媒体不得被废弃上传清理误删。
- 客户端选择+参数；服务端符合/不符合/无法判断；基于地域/查询后、业务筛选前候选集合算各字段覆盖，不用结果集反推。总云≠低云，目录覆盖而零匹配≠不可用。共享filterSpots未发现调用，记迁移清理而非线上双路径缺陷。
- 每组提醒意图/授权/发送状态分离；发送前检查计划与提醒版本；失效或出发后不发；恢复时已错过触发默认跳过记录。明确失败有限重试且不晚于出发；结果未知核对回执、不盲目重发。最多5组、各20项清单规则保留。
- 星表：重要亮星覆盖与较暗覆盖分别定义；至少Sirius/Vega/Polaris具名真实生产包→BFF帧测试，保留fixture计算测试。TOP2048与G<=5.5不是完整承诺；不能只增数量或仅免责声明掩盖重要亮星缺失。统一版本化包、来源/去重/历元/自行/亮度口径；本轮不选/下载新包，不改数据。
- 逐来源、字段、日期、模型、路线方式描述部署能力；provider网络/许可/预算独立。天气失败不删月相/日月位置/天空，不掩盖缺安全输入；概览不构造全星图，按需/缓存/截止时间复用。
- 后续验收：WEAPP地图叠层/拖动/内部scroll/键盘/返回/安全区、真实星表BFF覆盖、部署逐源可达性。本轮不执行/声称生产验收。

## 设计资源修改清单（逐页）
1. Search：16筛选、实拍文案、驾车参数、条件依据、partial/unknown/真正零匹配。保留同框过渡/横滑/五分类/两分区/卡片/44px。
2. My：进行中标签/结束时间/排序和三条，保留玻璃卡/SUV/对齐/全部入口。纠正资源已提交旧名称为现行创建与反馈。
3. Plan详情/编辑：到达冲突/未知路线、每组提醒状态、无天气远期计划演示，既有层级/五组20项/用户清单/返回栈保留。
4. Sky：选定地点+日期时间轻提示，保留全屏/轻标签/天体焦点/传感器恢复，不增加模式或定位授权。
5. Map/contribution演示：未保存切点取消、私有draft/pending、发布/合并转换与返回；复用既有表单/组件，不重做外观。
- 资源为非生产HTML/CSS/JS，展示数据明确在画外为样例；Stitch原稿与Codex交互修订分别记录。连接不可用保存具体ready input bundle，不换生成器，Context继续。
- 用户审查之前不覆盖ADOPTED，候选放各自页面任务目录。确认后更新唯一ADOPTED、Screen owner和确有变化的DESIGN；未变主题/区域保持。

## 验证/退出条件
- Context检查：一致性、有效链接、结构校验；不靠历史报告或结构通过声称产品可用。
- 实际查看资源390基准与320/375/430、长中文/地点、键盘/安全区、44px、适用reduced motion；不整体缩放。
- 交互：参数确认取消、总云有低云无、部分数据、事件真零结果、未保存切点取消、跨午夜进行中、迟到可保存、无天气可计划、提案映射和天空返回。
- 交付区分Context完成/设计待审或采用/生产未改/运行验证未做。所有资源必须可打开、可继续编辑，依赖缺口如实报告。goal不因剩余用户审查虚报全完成。
