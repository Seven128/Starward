# 已确认决策与授权

- 用户授权直接修改 Context 的产品逻辑、技术架构与实现约束说明，不需逐项再批准。
- 用户再次强调设计资源必须使用 design resource skill，完成后由用户审查；不提前替换采用入口。
- 不做生产开发实现：不改 apps/packages/workers 生产源码与API类型、数据库、生产星表、运行配置；不部署、不购买、不发送真实通知。不启动长程任务工作流自举。无子agent授权。
- 允许设计资源 HTML/CSS/JS 的交互演示，明确非生产实现。
- 移除小程序“今晚推荐”和“最佳窗口时长”：18→16项，五类分组保留。不删除原生App推荐职责或其他消费者的服务端算法。
- “摄影前景”改为“有实拍照片”。
- 动态筛选按当前选定时刻；天象按其事件区间/观测夜语义另行说明。
- 驾车上限可设置；沿用既有默认180分钟、整数30—360分钟。参数进入搜索条件，不依赖不可见旧账户偏好。点击参数项打开原筛选sheet到到达分类，sheet内编辑启用/数值，确认提交/取消恢复；不加第三层。
- 用户明确两项计划改进都纳入：进行中仍列在My；可靠路线迟到提示但不自动改时间/禁止保存。
- 小程序预算350元/自然月；沿用原小程序独立口径，不含应用和数据库IaaS，不与旧200/300累加；App200保留自身范围。用户问350是否够：已回答个人非商业试用有条件可行，不保证全能力/稳定性，网络、权益和字段覆盖独立验证。
- 保留现有视觉方向、My玻璃卡、全屏天空、Map/My主导航；局部修订不重新探索A/B、不重建设计系统。
- 设计日间范围为主，天空沿用现有天空/观测红光；标准字号320/375/390/430、安全区44px，200%字体仍暂停。
- Context先改确定规则；新视觉审查前旧资源未修改区域继续有效，冲突区域标明产品规则已变、视觉待修订，不用旧图覆盖新规则。
