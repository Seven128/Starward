import fs from 'node:fs/promises';
const root='E:/dev/Starward/';
async function edit(path,fn){const p=root+path;await fs.writeFile(p,fn(await fs.readFile(p,'utf8')));}
await edit('project_context/areas/main/screen-contracts/wechat-miniapp/surfaces-and-controls.md',s=>s.replace('`large` alone enables its vertical scroll','`medium` and `large` enable its vertical scroll').replace('Only that actual visible band may manipulate extent, while press/tap without drag changes nothing and panel content never starts that gesture.','In medium/large, only that actual visible band may manipulate extent; press/tap without drag changes nothing. In small, upward vertical dragging on the clipped document expands to medium instead of scrolling its content. Downward dragging at small is clamped: no movement and no dismissal. The fixed action rail remains independently actionable.').replace('a second map, whole-panel drag,','a second map, unrestricted whole-panel drag in medium/large,'));
await edit('project_context/areas/main/screen-contracts/wechat-miniapp/spot-and-sky.md',s=>s+'\n## Panel continuity and bounded deep-sky imagery\n\n2026-09-09 user revision: small is the hard lower drag stop, not a swipe-to-dismiss state. Its clipped document accepts upward expansion to medium; medium/large use the continuous document scroll owner, with extent dragging confined to the visible header band. Explicit Back and intentional exposed-map activation retain their existing dismissal semantics. Large joins the navigation/header boundary with no exposed or hit-testable map seam; header and safe-area pixels are not map-dismiss targets.\n\nOpening or retargeting a panel must not expose an empty shell, close/reopen flash or intermediate old selection. Keep a coherent visible presentation until the requested identity/content or its truthful loading state is ready, then commit together; superseded work cannot replace a newer selection or reopen a dismissed surface. Design iframe preparation is a prototype accommodation, not a requirement to duplicate production content trees.\n\nCloud-sky zoom must reveal progressively resolved, attributable galaxy/nebula survey imagery where coverage exists, not merely enlarge rendered star dots or a blurred bitmap. Use one angular camera/projection for star catalog, solar-system targets and sky-registered imagery; zoom changes field of view, not coordinates, time or sensor-direction ownership. Image footprints, celestial reference frame, epoch and reprojection must be explicit. Archived, processed survey images are not current weather, live camera output or a naked-eye visibility promise. Missing coverage/resolution stays missing; no generated imagery may substitute for scientific detail.\n\nThe zoom limit follows useful survey resolution plus measured rendering/pointing limits; the preliminary 1–3× control exploration is not a final cap. At high magnification, sensor jitter and retention of the intended target require evaluation before selecting any hold/focus interaction; do not silently add manual heading control. Retain coarse imagery while finer tiles load, bound concurrent requests and memory, cancel superseded work, and expose material loading/error/maximum-resolution state without clearing the sky. Night/observation themes must also constrain image luminance and preserve a clear processed-image source disclosure.\n');
await edit('project_context/areas/main/screen-contracts/wechat-miniapp/map-and-finder.md',s=>s.replace('| 驾车时长 | Current route from the explicit valid origin to this point, within the chosen integer minutes; default 180, allowed 30–360; straight-line distance is never a substitute |','| 驾车范围（时间 / 距离） | One driving route from the explicit valid origin to this point; exactly one active mode. Time defaults to 180 minutes (integer 30–360); distance defaults to 100 km (integer 1–1000). Limits are product input bounds, not provider coverage promises. Compare the selected mode with its own current route duration/distance evidence; never substitute straight-line distance or infer kilometers from minutes |').replace('Editing enabled state/minutes and 清空','Editing enabled state, time/distance mode and the two remembered limits, or 清空,').replace('Clearing resets disabled/default-180 in the draft.','Clearing resets disabled/time-mode/180-minutes/100-km in the draft. Switching mode preserves each draft value without converting between units; only the active mode participates in matching and counts as one condition. Invalid active input blocks confirmation with an adjacent unit-specific error. Cancelling restores committed mode, enabled state and limits together.'));
await edit('DESIGN.md',s=>s.replace('按Screen Contract编辑启用状态与分钟数','按Screen Contract编辑启用状态、时间/距离模式及对应数值').replace('除实际可见header band以外的Panel body/content/media或泛化viewport top-edge均不发起extent drag。','中/大档除实际可见header band以外的Panel body/content/media或泛化viewport top-edge均不发起extent drag。小档裁切正文上拖展开到中档，下拖在小档硬边界不动、不关闭；中/大档正文均可滚动。').replace('Panel vertical drag、large content scroll','Panel vertical drag、medium/large content scroll')+'\n### 2026-09-09 局部审查偏好校准\n\nSearch 筛选可见底板、图标与留白略收紧，保持至少44px互不重叠的独立命中区，不缩放整页。My 进行中使用低饱和浅灰绿底、清晰深灰绿小字和舒展内边距，与时间形成轻层级；这不是全局绿色主题。省略入口用等大几何圆点，在完整点击区内双向居中，不依赖字体基线。计划清单用端正方框与居中等粗圆端SVG勾，外框不旋转、不被长文字挤压；长行按首行对齐，点击范围仍覆盖完整行。具体候选值与来源保留在各页候选包，待用户审查，未自动采用。\n');
await edit('project_context/architecture/runtime-and-domain.md',s=>s+'\n## Cloud-sky progressive image assessment\n\nThe requested deep-sky zoom separates rendering engine, survey data rights/coverage, and tile delivery. HiPS is the preferred format to evaluate; Aladin Lite is the first browser feasibility candidate, with Stellarium Web Engine as the planetarium-oriented comparison. Neither is adopted as a production dependency. Keep current geometry/catalog responsibilities while checking WebGL/WASM/DOM assumptions against the actual WEAPP runtime; a desktop browser success does not establish native Mini Program support or authorize a web-view route. Preserve the one selected location/time and pose transform, and avoid a second astronomy authority.\n\nBefore implementation selection, validate a representative galaxy and nebula at multiple fields of view, correct registration/handedness, coarse-to-fine loading, request cancellation, texture/memory bounds, low-memory/context loss, high-magnification sensor stability and target-device frame timing. Determine exact-version engine licensing separately from each survey’s attribution, distribution and access terms. No bulk mirror or paid hosting is authorized by the CNY350 monthly ceiling; measure bytes per browsing session and monthly demand before claiming budget fit. Unavailable deployment connectivity/coverage or unsupported runtime remains an explicit dependency for this feature, not a blocker for independent pages.\n');
