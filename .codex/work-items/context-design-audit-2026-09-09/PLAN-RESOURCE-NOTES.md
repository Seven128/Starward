# Plan候选集成续作信息

采用入口：docs/design-resources/wechat-miniapp/plan/adopted/plan-page/。已读取app.mjs、plan-model.mjs、style.css、index.html、review.html、run.json，实际查看detail.png和adopted-editor.png。

Stitch本轮项目1643718854829580633，已提交详情+编辑两屏，当前已观察到Thinking；输入与实际附图见候选stitch-prompt.md。保留原项目旧屏，不修改采用目录。生成后读取新iframe的srcdoc及稳定screen ID，实际查看再决定局部精修。

## 必须处理的本地资源集成

- 采用app使用 `/events/`、`/map/`、`/my/`绝对路径，但当前5329服务器只服务 `/docs/design-resources/`。候选中改写资源引用为 `/docs/design-resources/wechat-miniapp/...`，不能因此改采用文件或伪称跨页可用。事件内部仍可能有绝对路径，实测时逐项核对；最小范围修复候选的路由适配，避免新建另一生产路由器。
- 复用现有编辑深拷贝、route.draft/base、历史栈、事件关联、保存后详情、五组/二十项约束。当前计划列表ordered仅按开始时间划分过往，候选应按结束时间保证进行中不列过往。
- 样例时钟与My保持2026-09-10 00:30上海。p1跨午夜23:30—01:30，p2/p3未来时间，far=2026-10-10。若为迟到详情用另外明确fixture，不要让进行中样例的出发前提醒还处于待发送。画外控制选择不同fixture，避免时间/发送状态自相矛盾。
- 路线已知且有效：预计到达=出发时间+样例耗时，比较开始时间，迟到40分钟可保存；修改出发时间可消除冲突。未知/失败/过期均“暂无法核对到达时间”，不沿用过期估计。来源与更新时间是明确样例，不调用真实服务。
- 远期天气缺失与天文可计算分开。日期输入不因天气覆盖限制；保存不等待路线/天气。天文内容未计算时诚实待获取，不能为演示捏造天文结果。
- 每组提醒意图/授权/发送状态分别展示：未授权、待发送、结果待确认、明确失败、已过时/已跳过。清单始终独立可勾选。未知发送结果不提供盲重发；可查看状态说明/核对回执的演示。不要建立全局通知开关。
- My候选点击记录真实planId；后续联动需将My实际fixture传入Plan候选，并保存返回来源，不依赖行号或沿用不匹配的采用fixture。不覆盖My未提交用户编辑。
- 全部候选仅浏览器设计资源，JS修订明确归Codex；原始Stitch文件不混入本地修复。

## 要验证

四宽度320/375/390/430、长地点/清单、可见与命中44px、底栏不遮挡、reduced motion；迟到仍可保存/不自动改时间、未知路线、远期无天气、五组二十项、取消保留、事件往返保留副本、My按真实ID往返、进行中列表归属。键盘真实WEAPP/生产授权发送未验证不得宣称通过。
