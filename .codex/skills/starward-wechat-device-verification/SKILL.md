---
name: starward-wechat-device-verification
description: "Orchestrate Starward WeChat Mini Program physical-device work: targeted development feedback, Android USB or paired wireless ADB, official preview, permission/location interaction, and settled-candidate verification. Select the evidence mode, reconnect the authorized target, run existing project commands, and preserve session, privacy and cleanup boundaries."
---

# Starward WeChat Device Verification

Keep this Skill thin. Context owns durable meaning, project scripts own execution, and each run owns only disposable evidence.

## Compose the official WeChat DevTools Skill

Use the imported official `wechatide-skill` as the primary control plane for WeChat DevTools. Read only its root plus the scene needed for the current operation (normally initializer, compiler, automator, debugger or previewer); do not duplicate its tool schemas here.

If an authorized Codex MCP registration works but a direct `wechatide` CLI tool reports `mcp_token_required`, treat this as credential propagation between two clients. Reuse the existing local MCP configuration through a private runtime wrapper; never print the token, place it in command text or persist it in the repository/Context. A token-free `--help` success is not an authenticated tool readiness check.

- On first import, after a DevTools update, or when the official skill is missing, run the bundled official installation check. The menu `导出 SKILL` and the path printed by `wechatide` are equivalent sources. Copy the complete versioned directory from the DevTools installation into the agent skills directory, overwriting an older same-named copy, then reload skill discovery. Never modify the installation copy or vendor it into this repository.
- Skill import, MCP authorization and optional CLI authorization are separate. The MCP configuration must be recopied from DevTools `设置 → 安全` into the client after installation/update when required. Never infer that a successful CLI preview, open 9420 port or prior client authorization supplies the MCP token; never discover the token from clipboard, local files, logs or process arguments. Keep the client name `Codex` stable for this workflow.
- Once the official status gate passes, use the official tool for project opening, compilation/refresh, runtime info, element/page automation, screenshot, filtered Console/Network, preview and true debugging. Project commands continue to own Starward generation fingerprints, candidate binding, device privacy and cleanup.
- Distinguish the IDE command/MCP service, simulator capture and page automator. When open/compile and screenshot work but runtime/page/element/remote waits for an automator response, keep the exact project open and inspect the installed `cli auto --help` before choosing recovery. DevTools 2.02.2609102 accepts the owned `--auto-port` path used below; Stable 2.02.2608070 does not advertise or retain that flag, so a reported CLI `auto` success must not be recorded as a 9420 owner without an actual listener/read-only runtime result. Reuse the version-matched official MCP path or its registered fallback, and do not rebuild, refresh the generation or infer a token failure. On a fresh project, the first runtime read may time out while compilation settles even though a subsequent simulator capture and runtime read succeed. A post-`remote` timeout or loss of page-automator responses can be the waiting or attached true-debug scene; inspect the phone/QR boundary before restarting or closing anything.
- Check the returned `autoPort`; a stale owner may keep the requested/default port and push the new project onto another port the MCP route does not use. Close the stale project and stop only its verified owned automation process, then rebind the unchanged candidate. Treat page, runtime and viewport operations as separate readiness checks.
- Treat `已连接` as true-debug transport evidence only. Attempt fresh official `systemInfo`, `currentPage` and scoped Console/Network, then obtain a separately sourced phone-display observation. DevTools 2.02.2609102 with Skill 0.3.10 can suspend desktop page-automator responses after QR true-debug attaches; a timeout in that state does not negate the visible connected transport. Preserve the session and use the independent, identity-guarded ADB screenshot lane for phone pixels, plus narrowly scoped debugger output when available; record runtime identity as unverified when the official read remains unavailable. `simulator_screenshot` is a desktop-simulator capture even during true-debug; label it accordingly and never use it to contradict the operator's phone observation. Decide whether the phone content root is absent, a native component failed, or the product deliberately rendered loading/empty/recovery UI only from phone-scoped evidence.
- For an authorized phone scan, first render only the current official QR sharply at a readable stable size, then use a fresh guarded phone screenshot before opening WeChat's scanner. Verify the activity after the scan: a helper that no longer sees the scanner may mean the QR was recognized immediately and the Mini Program launched. End a failed debug session before scanning a replacement QR. Never infer generation identity from a still-open prior phone page.
- A running QR viewer is insufficient when another host window covers it. Bring the current QR window into the actual foreground before the guarded scanner input. Treat `bind --confirm official_update_completed` as an operator assertion only: if the next guarded Mini Program capture fails or the scanner returns to Launcher, the scan remains unverified and that session cannot support product evidence until a fresh Mini Program capture succeeds.
- If the operator explicitly authorizes host-side phone control and leaves the phone positioned for the visible QR, the agent may perform the scan through the identity/foreground-guarded ADB lane instead of pausing for the operator. Keep the QR at its native readable size, open the scanner only from verified WeChat Launcher/scanner state, and require the resulting Mini Program activity plus current phone pixels before proceeding. A first attempt that merely returns to Launcher is not success; after rechecking the same QR is still current, retry it once. Never inspect chats or other WeChat content to find the scanner.
- Before an unattended authorized scan, distinguish wireless ADB connectivity, display readiness, Android keyguard state and current input focus. Do not interpret an omitted OEM `mInteractive` field as `false`; treat it as unknown and combine wakefulness/display state, `mHalInteractiveModeEnabled` when present, keyguard state and the focused window/application. The expected resumed Activity alone does not prove that taps reach WeChat. Use one-way `KEYCODE_WAKEUP` only when wake evidence is needed; never use toggling `KEYCODE_POWER`, which can lock an already usable phone. A supported wake or dismiss request may be tried once; never enter, infer or bypass a device credential. If the phone remains locked, keep the prepared generation, finish independent local checks and wait for an ordinary unlock. Then regenerate only an expired QR for the unchanged generation, explicitly focus the verified WeChat Launcher and resume the guarded scan. Change stay-awake behavior only through an owned reversible helper that records and restores the exact prior value.
- Derive every ADB tap from the fresh captured pixel dimensions retained by the guarded session. Android `wm size` can report a larger physical panel plus a smaller active override; multiplying normalized coordinates by `Physical size` then misses the visible control even though ADB reports success. Prefer the project `tap` command, which binds input to the captured active size and rejects display drift. If a bounded scanner bootstrap must operate before generation binding, use the current override size when present, otherwise the current screenshot size; capture and inspect each intermediate WeChat surface because menu geometry and labels can change.
- A screenshot viewer may resize the image. Compute normalized coordinates entirely from the original capture or entirely from the resized view; never divide a position read from the resized rendering by the original height. That mistake can send a panel-handle swipe to the native Map and produce misleading product behavior.
- If the exact generation renders in the desktop runtime but repeated true-debug scans reach only a zero-message relay shell or TabBar-only phone page, generate one official ordinary-preview QR for the unchanged prepared bytes. Use the resulting phone pixels to decide whether the product runtime itself renders. Keep the result labelled ordinary preview; it cannot be promoted to true-debug or settled-candidate evidence. Bind the feedback generation only after the exact preview is visibly running.
- Build automation selectors from the compiled phone WXML, not only the Taro source. Arbitrary source attributes such as `data-control` can be removed by the WEAPP compilation path while `class` and `aria-label` remain; after `no such element`, inspect one bounded `outerWxml` or `querySelectorAll` result and switch to a retained semantic/class selector. A missing source-only attribute is an automation compatibility finding, not evidence that the product element or action is absent.
- Keep tools named `simulator_*`/`get_simulator_*` scoped to the desktop simulator unless their contract explicitly says otherwise. Do not join their Console, Network, base-library or screenshot output to phone runtime identity as if all came from one surface.
- Treat official Console/Network `grep` as a line selector, not a redactor. A matching line can still contain Authorization headers, fixture/session tokens, request bodies, exact coordinates or response identities. Never request broad `.`/`error|fail` network output during a credentialed run or relay raw matching records. Prefer runtime/page assertions and endpoint-specific status probes; when a log is indispensable, constrain it to a non-sensitive event and reduce it in memory to fixed fields before any user-visible output or durable record. If an over-broad read occurs, stop using that filter and invalidate any disposable local credential when possible.
- On a physical phone, `127.0.0.1` and `localhost` target that phone. USB, ADB and true-debug do not forward the host API. Status-0 loopback requests establish a development-origin problem but neither prove nor disprove a separate blank content layer; select the owned LAN/remote-IP lane when live host data is required.
- For the USB route, do not equate a missing serial with an unusable device. The project driver first proves exactly one authorized USB transport with `-d`; when an OEM returns `(no serial number)`, it binds the current `get-devpath` to its `transport_id` and uses `-t`. Reconnection/path drift invalidates old capture authority. A successful `get-state` does not prove shell/screenshot/input readiness. If USB repeatedly stalls or becomes `offline`, follow the owning transport-choice guidance below instead of repeating unchanged cable/trust/driver resets or declaring a phone root cause.
- Treat USB-role selection as a re-enumeration boundary. Finish file-transfer selection and debug authorization, wait for one stable authorized transport, return the target Mini Program to the foreground, and only then capture. If the USB settings page is foreground, the safety check must reject the screenshot; record that as foreground setup, not a product blank screen or device-identity failure.
- Automatic true-debug can follow the desktop account's paired device. For a different authorized developer account/device, retain the desktop author session and use QR true-debug. After an IDE update, verify one running executable/version, its exported Skill version and the exact prepared-project window before reinstalling, refreshing or changing auth.
- Do not use Computer Use for this workflow unless the user explicitly asks for it. If official MCP is unavailable, preserve the candidate and diagnose the exact import/version/login/token boundary before choosing the registered GUI fallback.

## Load authority before acting

From the repository root, read these current owners in order:

1. `project_context/development-workflow.md`
2. `project_context/areas/main/verification/wechat-device.md`
3. `project_context/areas/main/verification/development-loop.md` when deciding whether the changed boundary needs a phone
4. `package.json` plus the invoked `tools/miniapp/device-*.mjs` entry when command behavior matters

Also inspect `git status --short --branch`. Preserve unrelated dirty state. A verification-only request does not authorize product edits. In an already authorized implementation task, repair relevant findings within that scope and rerun the affected checks; do not request the same authorization again or alter product behavior merely to manufacture a passing device run.

## Fast reconnect after a saved pause

When a recovery index already names a current feedback run and generation, resume that exact candidate before any build or refresh:

1. Read the latest recovery entry and the run's `feedback.json`; verify the named generation directory and fingerprint still exist. Do not rerun installer, Skill export, preview generation, build, ADB repair or QR creation merely because the prior processes were closed.
2. Confirm the configured IDE executable/version and official Skill status once. Reuse the existing MCP registration and private credential propagation. Ask for a new token only after an authenticated status/tool call proves the saved credential is rejected.
3. Ensure one exact prepared project and one automation owner. Check 58647 and 9420, close any identified stale project, and stop only its verified owned automation process. On DevTools 2.02.2609102, prefer starting a clean project through `cli auto --project <exact-project> --auto-port 9420 --trust-project`; opening the same project through MCP first can deadlock the later `cli auto` at AppID permission fetching and block the IDE RPC queue. If that exact stall occurs, stop the pending calls, close only the verified owned DevTools processes, and let `cli auto` open the unchanged candidate itself. Require returned `autoPort=9420`; an exit code with another returned port is not ready for the current MCP route.
4. Prove the desktop page channel with one read-only page query. Invoke `remote` once. If it times out after DevTools enters the QR/waiting scene, keep the candidate and process intact and wait for the physical scan; do not resend `remote`, run a new project `refresh`, rebuild Taro source or close the window. An expired/failed QR may require the operator to use DevTools' visible `重试`; its official recompile/upload repackages the same prepared project and is allowed. It is not a new project generation and cannot change a build-time API constant. Recheck the recorded generation bytes before binding after such a retry.
5. For a phone account different from the desktop author, use the displayed QR. `已连接` advances only the transport boundary. Then read `systemInfo`, `currentPage` and page `root.cn`, and obtain the operator's current phone observation. Keep every `simulator_*` result labelled desktop-only.
6. Use ADB only when an OS/display action is actually needed. Its absence does not block QR true-debug or official runtime/page reads. For the owner's NIO Phone, reuse the authorized TLS wireless route before repeating USB recovery; follow [Android transport choice and reconnect](../../../project_context/areas/main/verification/wechat-device.md#android-transport-choice-and-reconnect). After the authorized target is connected, pass `--transport wireless` to project doctor/capture/input commands; the default remains USB. Do not expose or persist its endpoint.

If a fresh QR scan immediately shows `连接断开` plus `服务器忙`, zero confirmed/sent/received messages and `cmdId 1002, errCode -1`, classify it as a true-debug attachment failure before page startup. End that phone session before another scan. A green `已连接`/`服务器正常` overlay with all three message counters at zero and repeated `cmdId 1005, errCode 1` is likewise only relay/control-layer evidence; it does not prove phone runtime attachment or application startup.

Treat remote structured automation as a separate layer after runtime attachment. A visible current page plus green server state, nonzero received-message traffic and zero pending messages proves that true-debug reached the phone runtime; it does not make `automation_runtime_info` successful. This host has observed Nightly 2.02.2609102 + Android WeChat 8.0.78 render and exchange traffic while structured commands time out and add `cmdId 1006, errCode -50011` to the phone overlay. Preserve the session and classify only structured remote automation as unavailable; use the already authorized guarded ADB/vConsole or desktop debugger for the needed bounded evidence. Do not rescan, rebuild, change VPN/USB or downgrade the whole true-debug result unless those separate layers fail their own checks.

In multi-journey structured acceptance, keep identity-clearing reset separate from journey isolation. Between journeys, clear request/response/query state and reset the canonical product store while preserving the current session and installation identity whenever later journeys consume data created earlier. Bind a moderation/resume journey to the exact production-UI submission by immediate authenticated API readback; keep raw tokens and identifiers in memory and write only hashes/state to evidence. Check anonymous user continuity after intervening journeys. For a prepared formal-spot journey, an already-open Map panel is reusable only when its inspected Observation Context matches the prepared spot ID; otherwise close it and select the target through the production finder.

Do not disable a required VPN/TUN to repair LAN mode. In Nightly 2.02.2609102, an unpinned LAN-debug address defaults to the first non-loopback IPv4 returned by the DevTools runtime; a TUN or Hyper-V adapter can precede the physical WLAN even while the phone and host share Wi-Fi. Use the official QR panel instead of changing route metrics: enable `局域网模式`, open its adjacent help control, and use `切换IP` for the physical WLAN address. The tool regenerates the QR, but the chosen address is not reliably retained across a new project window or DevTools process. Recheck it before every newly created true-debug session when virtual adapters remain present. Before the scan, verify the DevTools listener is bound on all interfaces, Windows Firewall admits only the required inbound TCP range on the WLAN's actual profile, and the phone can complete a bounded protocol request. Android Toybox netcat does not support `-z`; a real bounded connection is only a transport probe. For HTTP, read a bounded response status or first response bytes because timing out an intentionally open socket can make bare `nc` exit nonzero after it already connected. If the explicitly selected WLAN path still fails, continue at the listener/firewall/AP-isolation boundary without repeating QR scans or changing product code. Keep endpoints and adapter addresses out of durable records.

Wireless reconnect uses the existing pairing and the intended phone's current connection endpoint; the pairing port/code is only for pairing. Keep discovery identifiers and credentials private, preserve a working WeChat WAN attachment, and require bounded shell plus scoped fresh phone capture before claiming input readiness. If the phone shows only a wireless-debugging switch, inspect its details row before requesting other setup. Pairing success does not establish sustained stability; never bypass the existing device/foreground/input guards to obtain a screenshot.

For a deliberate pause before the phone scan, preserve the feedback run and generation but close the owned project/automation session. Record that processes must be reopened; this prevents a stale 9420 owner from becoming the next session's first failure.

## Select one mode

For simulator-readable state, native layout or screenshots, first use the optional warm observer routed by [development feedback](../../../project_context/development-workflow/development-feedback.md). Start phone work here when the changed invariant needs physical-device evidence; a simulator result cannot establish it.

- Choose **Development Device Feedback** for a first runnable physical-device boundary or a coherent batch affecting native map/gestures, capsule or safe area, permissions, GPS, compass/rotation, phone lifecycle, physical network/TLS/domain behavior, or a platform difference. Ordinary logic, copy, deterministic state, and simulated provider cases should stay in cheaper checks unless a physical invariant is at risk.
- Choose **Settled-Candidate Device Verification** only for one fixed clean candidate after cheaper checks and relevant targeted feedback are stable. Use this for the complete applicable phone journey and evidence-level observations.
- State why a phone is needed or why it is not. If the requested outcome depends on a genuine mode or authorization choice that cannot be inferred, ask one concise question before the side effect.

Never use targeted development feedback as fixed-candidate evidence. Never use simulation to prove GPS, physical direction, phone permissions, or hardware behavior. One Android device establishes only the declared owner-device sample.

## Development Device Feedback

1. Run the affected cheap project checks and wait for a coherent Taro WEAPP output.
2. Check readiness with `npm run miniapp:device:feedback -- doctor`. Before phone delivery, the generator rejects a compiled loopback API origin and any detected malformed/non-HTTP(S) API base. When a phone needs host fixture data, build with the current reachable physical-LAN origin and verify the generated bytes contain that complete absolute origin, not a truncated scalar or loopback default; a DevTools recompile cannot change a build-time constant.
   It also rejects Taro experimental CompileMode `*-templates.wxml` output. The project keeps CompileMode disabled because successful adjacent Taro 4.2.1 builds can assign incompatible template prefixes. Clean the generated root before rebuilding; do not repair a candidate by copying generated JavaScript/WXML from another generation.
3. Start with `npm run miniapp:device:feedback -- start --project <absolute-project>`. Treat the returned generation and bundle fingerprint as the candidate identity for this feedback run.
   Open a decisive rebuild under its generation's fresh prepared-project path and wait for the first DevTools compile to settle. Require the expected route/DOM and at least one complete visible frame before true debug. A single blank simulator capture can be a native-Map compositor miss, while repeated TabBar-only phone pixels are still a phone-scoped failure.
4. A completed public command establishes only official invocation, not delivery to the intended phone/account. If automatic delivery selected another account/device, or the command reports `manual_required`, run `preview --feedback <run>` to create the official ordinary-preview QR for the unchanged generation. Let the user inspect and choose `信任并运行` if prompted, then scan the returned `qrCode` from the intended authorized WeChat account. If the CLI path is unavailable, use the version-visible official Preview action for `preparedProject` (for example `预览` or `使用当前页进行真机预览`); never depend on one historical menu label or bypass trust with an undocumented flag. Run `bind --feedback <run> --confirm official_update_completed` only after the user explicitly confirms that this generation is visible on the intended phone. The owner removes QR bytes after binding and owns any refresh/stop cleanup; report a DevTools-locked stale generation as pending instead of hiding it.
5. For official 真机调试, use `wechatide-skill` automator `automation_viewport_action` with `action=remote` against the exact prepared project, handle its QR/async result under the official polling rules, then use official runtime/debugger tools to inspect the attached condition. `auto_preview` and ordinary preview QR are delivery paths, not true-debug evidence. Use the existing scoped `npm run miniapp:device -- capture|capture-permissions|capture-location|tap|swipe|back` commands only for the independent ADB channel. The project `remote|inspect` commands are compatibility diagnostics for an already owned endpoint; after `device_test_official_sdk_unavailable`, switch to official MCP or its registered GUI fallback instead of retrying. Review a fresh screenshot before every input. The agent owns host-side ADB/service/driver recovery; the user performs only physical QR scanning, cable/USB-mode changes, on-phone trust, device rotation, MFA or other actions unavailable from the host. Stay within the admitted Mini Program/permission input scope.
6. After another coherent source batch, use `refresh --feedback <run>`. A refresh is a new generation and may restart the Mini Program or lose page-stack/in-memory state. The old ADB session, screenshot, and input authority are invalid; never reuse them even if an old file remains visible.
7. Finish with `stop --feedback <run>`. Confirm that only the feedback run, its private generations, scan material, and nested device session were removed; source project and phone data remain.

Before attributing a native picker failure to device permission, compare the exact invoked API with the compiled `requiredPrivateInfos`; related location calls are separate declarations. Keep each native `showModal` action label within WeChat's four-character limit and share repeated consent configuration across consumers. Deterministic tests should guard both rules, while a phone must still prove picker/modal launch, cancellation and return because desktop mocks do not enforce every native constraint.

On official CLI absence, timeout, login/service failure, disconnect, non-foreground phone, or scan delay, diagnose the reported boundary before resuming. Reuse authorized local readiness checks and repairs; ask for human action only for an actual unavailable login capability, phone scan, MFA or remote approval. Do not use private WeChat APIs, raw CDP, root, Appium, global TLS/VPN changes, or hidden endpoints as fallback.

For Android Back verification, prove each presentation shape on the page that actually owns it. On the current Taro/WEAPP runtime, evidence from a `navigateTo` editor does not transfer to an editor embedded in a Tab root page: `enableAlertBeforeUnload` can protect the former while producing no prompt for the latter. An invisible `PageContainer` is not a general Back interceptor, and moving a complex embedded editor into `PageContainer` merely to acquire a native Back-stack entry can change native layout/compositing and render the form blank or cropped. Preserve the adopted presentation unless product design explicitly changes. Verify the embedded editor's page-owned close, tab-change, target-change and other explicit exits; keep root-page Android system Back open when the platform supplies no reliable interception mechanism, and report a real-page route as a product/design alternative rather than silently substituting it.

## Settled-Candidate Device Verification

1. Use the existing build/deployment owner to prepare one settled external phone project. Record its revision/config/AppID lane/API origin/base-library/build inputs and WEAPP fingerprint without exposing secrets.
2. Use `npm run miniapp:device -- doctor`, then `start --project <absolute-external-phone-project>`.
3. Keep candidate inputs unchanged and automatic refresh off. Exercise the applicable cold-start, navigation, permission, GPS/map, Observation Context, weather/astronomy, network recovery, background/return, and state-recovery journeys described by Context.
4. Treat ordinary preview, phone development/debug state, desktop remote attachment, and instrumented builds as distinct conditions. A result observed under one condition cannot be relabelled as another.
5. Any relevant bundle/config/AppID/API-origin/base-library/build drift invalidates the session and dependent observations. Stop, create a new candidate, and rerun the complete applicable journey.
6. Finish with the existing `miniapp:device -- stop --session <directory>` and the separate bundle owner's cleanup.

Do not read chats, unrestricted logs, clipboard, storage, exact location, QR content, or device identifiers. Do not alter global phone permissions or WeChat data to manufacture a state.

## Report the run

Keep diagnostic-path status separate from product findings. Report exactly these headings, with no private artifact content:

- `Candidate identity`: mode, revision/build lane, generation where applicable, and bundle fingerprint
- `Invocation result`: official command/manual handoff and ADB/SDK stages actually completed
- `Observed product behavior`: visible behavior only, without interpreting it as broader evidence
- `Verified`: conditions directly established in this run
- `Unverified`: unobserved device/platform/debug/network/lifecycle conditions
- `Invalidated`: stale candidate, session, screenshot, or observation scope
- `Cleanup`: owned local resources removed and any user-owned phone/tool action still outstanding

Development feedback must remain labelled `development_feedback`; never report it as passed or accepted. Do not write screenshots, locations, identifiers, run receipts, or issue lists into this Skill or durable Context.

A fresh screenshot or recording is only captured. After evaluating the frame or sequence against the current owner and named runtime conditions, retain the actual verdict, including unverified or failed, with its next action. Phone evidence keeps the device-session binding owned here and its private storage rules. Optional `miniapp:conformance-review` notes may organize repository-safe observations without mandatory dimensions or DevTools prerequisites; they cannot upgrade development feedback or simulator observations into device acceptance.
